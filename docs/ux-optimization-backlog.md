# Trusted Bums UX Optimization Backlog

_Last updated: 2026-06-18 by Codex daily UX consultant automation._

## Executive Read

Current `main` head `57231bf75e9900c11aea964ec9999517a831d1ca` narrows the UX queue to two still-open mobile presentation issues. Exact-head hosted `QA` `27710960865`, deploy `27710961582`, and `E2E Smoke` `27711014094` all passed on 2026-06-17, and a sourced local public smoke rerun passed `4/4` on 2026-06-18 against `http://127.0.0.1:8080`.

Two older UX items are now resolved in shipped source and should stay closed. The homepage client-only CTA wording is now explicit in [Index.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/Index.tsx:292), which retires `[TB-0064]`. The client opportunity helper now gives `Client Member` users a role-accurate fallback instead of sending them toward a finance-only route in [ClientOpportunityNew.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientOpportunityNew.tsx:1494), which retires `[TB-0096]`.

`[TB-0082]` was closed too early and has been reopened. The homepage header got safer mobile spacing, and this low-risk local pass now gives the `/bums` route its own wrapping mobile header treatment; keep the row open until pushed source and hosted visual proof confirm it. `[TB-0065]` also remains open because the first consent layer still dominates the first mobile viewport.

## Active Recommendations

### P1 - [TB-0082] Keep public mobile header actions fully readable at `390px`
- Evidence: A fresh sourced Pixel 7 capture on `http://127.0.0.1:8080/bums` showed the `For Clients` switch overlapping the brand area and the `Apply as a Bum` CTA clipped off the right edge on shipped head `57231bf`. Local source now changes [BumLanding.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/BumLanding.tsx) so the `/bums` header can wrap, shrink the logo on mobile, and place the route switch, accessibility control, and Bum CTA on a full-width mobile action row. Exact-head hosted proof is still pending for this local fix. Current W3C reflow guidance still expects content to remain usable without loss of information or functionality at narrow widths: [WCAG 2.2 Reflow](https://www.w3.org/TR/WCAG22/#reflow).
- Why it matters: The Bum acquisition path still looks cramped at the first decision point. When the main CTA is visibly clipped, the page reads as unstable before the visitor reaches any of the trust or workflow framing below.
- Recommendation: Push the local `/bums` header treatment and verify it with mobile visual evidence before closing `TB-0082`.
- Acceptance criteria: At roughly `390px` width, `/bums` shows a fully readable `Apply as a Bum` CTA, a visible route-switch cue back to the client path, and `Accessibility settings` with no clipped or overlapping labels; a completed exact-head hosted visual artifact confirms the fix.

### P2 - [TB-0065] Reduce the first-layer consent banner footprint on mobile
- Evidence: A fresh sourced Pixel 7 capture on `http://127.0.0.1:8080/?consent=reset` still shows the initial consent layer keeping the headline visible but pushing the main client CTA below the fold on first visit. Current source still renders that layer with `max-h-[78vh]`, two text blocks, and a stacked action group in [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:93). Sourced local public smoke passed `4/4`, so the consent flow still works functionally; the issue is presentation weight. Current ICO guidance still says consent information and choice mechanisms should be accessible and easy to use, while W3C reflow guidance still expects usable narrow-width layouts: [ICO consent in practice guidance](https://ico.org.uk/for-organisations/direct-marketing-and-privacy-and-electronic-communications/guidance-on-the-use-of-storage-and-access-technologies/how-do-we-manage-consent-in-practice/) and [WCAG 2.2 Reflow](https://www.w3.org/TR/WCAG22/#reflow).
- Why it matters: The first mobile conversion moment is still dominated by compliance chrome instead of the product message and primary CTA. That makes the consent step feel heavier than the buyer-intent workflow it interrupts.
- Recommendation: Keep equally prominent accept and reject choices, but tighten the first-layer copy and action layout so the hero headline and primary client CTA remain visible on first mobile visit. Keep the detailed category explanations and switches behind `Customize`, and route copy or layout changes through Trust or Legal review.
- Acceptance criteria: On first visit at mobile widths, the first consent layer preserves equally prominent choice controls while keeping the hero headline and main client CTA visible without scrolling; detailed category explanations stay behind `Customize`; and the revised presentation is reviewed by the Trust or Legal owner.

## Watchlist

- Exact-head hosted `Visual UI Audit` rerun `27742677438` on `57231bf` was still `in_progress` at review time, so the freshest current-head mobile proof is still the sourced local Pixel 7 captures plus the green exact-head smoke chain.
- The local `mobile-chrome` public visual audit now fails on a strict-locator collision because `/` intentionally exposes three matching `Create Client account` buttons after the client-label standardization. Treat that as QA coverage drift, not as a new public UX regression.
- Fresh public-funnel aggregate evidence still could not be rerun from this shell because `GA4_PROPERTY_ID` and `BING_WEBMASTER_API_KEY` remain unset after sourcing `.env.qa`, and session recordings, support exports, sales-objection notes, and customer-feedback exports were still unavailable.

## Access Requests And Evidence Gaps

Material missing access, credentials, analytics, screenshots, customer data, or other evidence needed for a stronger UX review. Mirror durable requests in `docs/consultant-access-needs.md`.

- Provide a completed exact-head `Visual UI Audit` artifact on `57231bf`, especially for `/`, `/bums`, the signup-intent state, the consent-first-layer state, and the consent-settings state, so public visual proof is not left half-hosted and half-local.
- Restore local `GA4_PROPERTY_ID` and `BING_WEBMASTER_API_KEY` in consultant shells if public-funnel prioritization should rely on fresh aggregate analytics during the daily UX run instead of source plus screenshots.
- Provide session recordings, support-ticket exports, sales-objection notes, customer-feedback exports, or narrated role walkthroughs for Admin, Client Admin, Client Finance, Client Member, and Bum flows so the current UX queue can be paired with real friction evidence instead of only route and screenshot review.

## Agent Inputs

- Date of run: 2026-06-18.
- Files, tests, routes, screenshots, sources, and commands reviewed: `docs/agents/automation-prompts/trusted-bums-daily-ux-consultant.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/ux-optimization-backlog.md`, `docs/codex-edit-log.md`, `git rev-parse HEAD`, `git status --short`, `git log --oneline --decorate -12`, `git diff af944fe..57231bf -- src/pages/Index.tsx src/pages/BumLanding.tsx src/components/ConsentManager.tsx src/pages/client/ClientOpportunityNew.tsx src/layouts/ClientLayout.tsx`, exact-head source review of [Index.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/Index.tsx:292), [BumLanding.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/BumLanding.tsx:57), [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:93), and [ClientOpportunityNew.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientOpportunityNew.tsx:1494); `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 20 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27742677438 --repo Pidpoddev/trustedbums --json ...`; `curl -I -L --max-time 20 https://trustedbums.com`; `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`; unsourced `corepack pnpm run build:dev` and `corepack pnpm run preview -- --host 127.0.0.1 --port 8080`; sourced `set -a && source .env.qa && set +a && corepack pnpm run build:dev`; sourced `set -a && source .env.qa && set +a && corepack pnpm run preview -- --host 127.0.0.1 --port 8080`; sourced `QA_BASE_URL=http://127.0.0.1:8080 QA_EXTENSION_API_EXPECTATION=skip corepack pnpm exec playwright test tests/e2e/staging-smoke.spec.ts --project=chromium --grep 'loads public pages|lets anonymous visitors manage privacy choices|validates the signup intent dialog before Clerk handoff|validates the separate Bum signup path before Clerk handoff'`; sourced `QA_BASE_URL=http://127.0.0.1:8080 QA_EXTENSION_API_EXPECTATION=skip corepack pnpm exec playwright test tests/e2e/visual-ui-audit.spec.ts --project=mobile-chrome --grep 'public marketing and privacy states render cleanly'`; sourced Playwright screenshots for `http://127.0.0.1:8080/`, `http://127.0.0.1:8080/bums`, and `http://127.0.0.1:8080/?consent=reset` under `test-results/ux-consultant-2026-06-18-sourced/`; live Supabase project confirmation plus tracker row re-read and refresh for `TB-0065` and `TB-0082`; and current W3C plus ICO guidance review for reflow and consent presentation.
- Checks that could not run and why: the first unsourced local preview rendered the `Trusted Bums configuration needed` Clerk fallback because `VITE_CLERK_PUBLISHABLE_KEY` was not exported into the Vite build, so that pass was discarded as environment proof rather than product proof. The current exact-head hosted `Visual UI Audit` rerun `27742677438` was still `in_progress` during this review. The local `mobile-chrome` public visual audit failed on a strict-locator collision because exact-head `/` now exposes three matching `Create Client account` buttons after the client-label standardization. Fresh GA and Bing aggregate reruns were not possible because `GA4_PROPERTY_ID` and `BING_WEBMASTER_API_KEY` remained unset after sourcing `.env.qa`. No session recordings, support exports, customer-feedback datasets, sales-objection notes, or narrated role walkthroughs were available in-session, so prioritization remained route-, source-, screenshot-, and hosted-run-backed rather than behavior-backed.
