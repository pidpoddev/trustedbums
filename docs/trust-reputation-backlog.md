# Trusted Bums Trust And Reputation Backlog

_Last updated: 2026-06-08 by Codex daily trust and reputation consultant automation._

## Executive Read

Current-state postscript: current release evidence is green on `441fd92`; older hosted preflight blockers are no longer the release state. Trust and reputation follow-up should stay focused on canonical host, DNS/control-plane proof, security headers, metadata, sender reputation, and the separate `rcdl.tplinkdns.com` TLS evidence gap.

The stale host-redirect warning is closed. Current live checks on 2026-06-08 show `http://trustedbums.com`, `http://www.trustedbums.com`, and `https://www.trustedbums.com` all 301 to `https://trustedbums.com/`. The remaining trust gaps are narrower and still current: the public HTML shell still emits only generic root-relative metadata, the live site still serves without baseline browser-security headers, the public site still exposes a downloadable pre-store extension zip with broad permissions, and this session still could not execute a mailbox-backed DMARC review.

Independent external-DNS validation remains partly blocked rather than failed. `https://trustedbums.com` returned `HTTP/2 200` from the runner and from the latest hosted GitHub smoke preflight, but the required external DNS target `https://rcdl.tplinkdns.com` still failed TLS verification on 2026-06-08 with `curl: (60) SSL certificate problem: unable to get local issuer certificate`. That is an evidence-path problem until a dashboard or independent monitor confirms otherwise, not a confirmed production outage for `trustedbums.com`.

## Active Recommendations

### P1 - Run a current mailbox-backed DMARC review and prepare DMARC enforcement
- Evidence: [supabase/functions/dmarc-reports/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/dmarc-reports/index.ts) still provides admin-authorized DMARC aggregate parsing, and [docs/shared-mailbox-operations.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/shared-mailbox-operations.md) plus [docs/dmarc-report-review.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/dmarc-report-review.md) still document mailbox scope for `bums@trustedbums.com`. This run still had no authenticated admin path to invoke the function. Google’s current sender guidance still says enforcement on non-compliant Gmail traffic ramped up in November 2025, and Gmail BIMI still requires DMARC `p=quarantine` or `p=reject` with `pct=100`. Sources: [Email sender guidelines FAQ](https://support.google.com/a/answer/14229414), [Email sender guidelines](https://support.google.com/a/answer/81126?hl=en-na), [Set up BIMI](https://support.google.com/a/answer/10911320?hl=en).
- Why it matters: Without current aggregate evidence, Trusted Bums cannot prove all legitimate senders align on SPF or DKIM, cannot safely move beyond monitor-only DMARC, and cannot qualify for BIMI. That keeps spoofing resistance and sender trust weaker than they should be for a B2B domain.
- Recommendation: Execute the deployed DMARC review against `bums@trustedbums.com`, inventory every legitimate sender that uses `trustedbums.com`, fix alignment drift, and plan a move from `p=none` to `p=quarantine` or `p=reject` with a named rollback owner.
- Acceptance criteria: Recent aggregate reports are reviewed; legitimate senders align through SPF or DKIM for the From domain; unexplained failures are remediated or explicitly accepted; and `_dmarc.trustedbums.com` advances to `p=quarantine` or `p=reject` with `pct=100`.

### P1 - Replace generic root-relative metadata with route-aware absolute metadata
- Evidence: Live HTML for both `https://trustedbums.com/` and `https://trustedbums.com/privacy-policy` still ships the same shell-level `<title>Trusted Bums</title>`, `<link rel="canonical" href="/" />`, `og:url="/"`, and root-relative image tags. The checked-in shell in [index.html](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/index.html) matches that output. Public routes still include `/`, `/login`, `/privacy-policy`, and `/legal/:slug` in [src/App.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/App.tsx), while [src/pages/PrivacyPolicy.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/PrivacyPolicy.tsx) and [src/pages/LegalDocumentPage.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/LegalDocumentPage.tsx) compute page-specific content but still do not emit route-specific head tags. [public/sitemap.xml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/sitemap.xml) already uses absolute `https://trustedbums.com/...` URLs.
- Why it matters: Buyers, crawlers, reputation reviewers, and link-preview systems still see generic homepage metadata on trust-heavy routes like privacy and legal pages. That weakens legitimacy signals even though the canonical host redirect is already live.
- Recommendation: Add route-aware head metadata that emits absolute canonical, Open Graph, and Twitter tags for each public route, and document the existing live canonical-host redirect rule if it is controlled outside the repo.
- Acceptance criteria: Public pages emit absolute canonical URLs and social tags; privacy and legal routes publish route-specific titles and descriptions; and a live recheck shows route HTML differs appropriately between homepage and trust pages.

### P1 - Add baseline production security headers, including HSTS
- Evidence: Live `curl -I -L --max-time 20 https://trustedbums.com` on 2026-06-08 returned `HTTP/2 200` with `cache-control`, `expires`, `vary`, `content-type`, and `server`, but no `Strict-Transport-Security`, `Content-Security-Policy`, `Referrer-Policy`, `Permissions-Policy`, `X-Frame-Options`, or `X-Content-Type-Options`. [public/.htaccess](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/.htaccess) still handles crawler files and SPA rewrites only. [src/pages/Index.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/Index.tsx) loads Cloudflare Turnstile, so CSP must intentionally allow required third-party origins. Current OWASP guidance still treats HSTS, CSP, framing protection, referrer policy, permissions policy, and content-type hardening as active browser defenses, and Microsoft’s current SmartScreen guidance still points to TLS and page trust signals as part of unsafe-site decisions. Sources: [OWASP Secure Headers Project](https://owasp.org/www-project-secure-headers/index.html), [Unexpected SmartScreen block or warning on website](https://learn.microsoft.com/en-us/troubleshoot/microsoft-edge/development/unexpected-block-warning).
- Why it matters: Enterprise buyers, browser reputation systems, and security reviewers still see a site with implicit rather than deliberate browser hardening.
- Recommendation: Add explicit host-layer HSTS and baseline browser-security headers, with a CSP that intentionally covers Clerk, Supabase, and Turnstile dependencies before rollout.
- Acceptance criteria: HTTPS responses include HSTS with at least one year of `max-age` and `includeSubDomains`; framing, referrer, permissions, and content-type policies are explicit; CSP is present or staged in report-only mode with required third-party origins accounted for; and live production headers are rechecked after deploy.

### P1 - Remove the public pre-store extension zip until permissions are narrowed
- Evidence: Live `curl -I -L --max-time 20 https://trustedbums.com/downloads/trustedbums-extension.zip` returned `HTTP/2 200` with `content-type: application/zip` on 2026-06-08, confirming the download is still public. [chrome-extension/trustedbums/manifest.json](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/chrome-extension/trustedbums/manifest.json) still requests `cookies`, `storage`, and host permissions for LinkedIn, `*.supabase.co`, `api.trustedbums.com`, `trustedbums.com`, and Clerk hosts. Google Search Console’s current Security Issues guidance still treats harmful downloads as part of the security-review path, and Chrome’s current extension guidance still says to request only permissions relevant to the main function because warning-triggering permission changes create install friction. Sources: [Security issues report](https://support.google.com/webmasters/answer/9044101), [Permission warning guidelines](https://developer.chrome.com/docs/extensions/develop/concepts/permission-warnings), [Permissions](https://developer.chrome.com/docs/extensions/reference/permissions-list).
- Why it matters: A publicly downloadable pre-store extension with broad permissions creates reviewer distrust, increases download-reputation risk on the main domain, and weakens the site’s high-trust B2B posture.
- Recommendation: Pull the zip off the public site until the distribution plan is final, then reduce permissions to the narrowest production set and lock extension API CORS to the final extension origin.
- Acceptance criteria: The public site no longer exposes the pre-store zip except through an intentional controlled path; extension permissions are reduced to the smallest shipped set; and extension API CORS is restricted to the final production extension origin.

## Reputation Checks

- Browser and endpoint-security reputation: No live SmartScreen, Defender, Safe Browsing owner dashboard, VirusTotal, URLVoid, or equivalent scanner verdicts were available in-session.
- Google Search and Safe Browsing: No live Search Console Security Issues, Manual Actions, or owner-verified Safe Browsing path was available in-session.
- DNS, HTTPS, and security headers: `https://trustedbums.com` returned `HTTP/2 200` from the runner on 2026-06-08. `http://trustedbums.com`, `http://www.trustedbums.com`, and `https://www.trustedbums.com` all redirected to `https://trustedbums.com/`. The latest hosted GitHub `E2E Smoke` preflight for run `27112837432` also passed DNS, HTTPS, and app-shell checks against `https://trustedbums.com` before failing on missing extension API configuration. The required external DNS target `https://rcdl.tplinkdns.com` still failed TLS verification from this runner with `curl: (60) SSL certificate problem: unable to get local issuer certificate`, so that path remains an evidence blocker rather than a confirmed site outage.
- Email authentication and DMARC: Mailbox-backed review infrastructure still exists, and the shared-mailbox docs still record mailbox-scoped Microsoft Graph access for `bums@trustedbums.com`, but no current aggregate DMARC output or live admin invocation path was available in-session.
- Public contact and mail trust: [supabase/functions/submit-contact/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/submit-contact/index.ts) still enforces Turnstile verification, origin allowlisting, and rate limiting. [supabase/functions/send-website-email/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/send-website-email/index.ts) still restricts callers to service-role or internal-secret proofs before it can send email.
- Tracked admin email links: [supabase/functions/email-track/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/email-track/index.ts) still restricts tracked click destinations to `https` URLs on `trustedbums.com` or `www.trustedbums.com`. The remaining evidence gap is a safe seeded valid-delivery click that proves redirect plus event recording in one live pass.
- Search and social metadata: Public sitemap and robots files point to `https://trustedbums.com`, but runtime page metadata is still one global root-relative block from `index.html`.
- Extension reputation: The public site still exposes the extension zip, and the manifest still asks for `cookies` plus broad host access.
- Public credibility: Privacy and legal pages are present and reachable, which helps legitimacy, but they still render generic homepage head metadata instead of page-specific trust copy in the HTML head.

## Watchlist

- [supabase/functions/dmarc-reports/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/dmarc-reports/index.ts) still accepts a mailbox string in request input. The real control remains Exchange mailbox scoping plus admin authorization, so keep this under review until a current positive admin read path is exercised again in-session.
- Keep `email-track` in post-deploy smoke coverage. The source still looks correctly allowlisted, but this run did not have a safe seeded delivery id to prove redirect plus event recording together.
- [supabase/config.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/config.toml) still lists many `verify_jwt = false` edge functions. Some are expected because they use public or custom-auth flows, but this remains a trust-sensitive deployment inventory until provenance and intended exposure are clearer.

## Current Standards And Time-Sensitive Notes

- Google Search Console still says security issues can lead to warning labels in search results or browser interstitials, and harmful downloads are part of that review path. Source: [Security issues report](https://support.google.com/webmasters/answer/9044101).
- Gmail sender guidance still says enforcement on non-compliant traffic ramped up beginning in November 2025; messages that miss authentication and sender requirements can see temporary or permanent rejection. Sources: [Email sender guidelines FAQ](https://support.google.com/a/answer/14229414), [Email sender guidelines](https://support.google.com/a/answer/81126?hl=en-na).
- Gmail BIMI support still requires DMARC policy `quarantine` or `reject` and `pct=100`; `p=none` does not qualify. Source: [Set up BIMI](https://support.google.com/a/answer/10911320?hl=en).
- Microsoft’s current SmartScreen website-block guidance says URL reputation, page content, TLS/security setup, and other trust signals affect unsafe-site decisions. Source: [Unexpected SmartScreen block or warning on website](https://learn.microsoft.com/en-us/troubleshoot/microsoft-edge/development/unexpected-block-warning).
- Chrome’s current extension guidance still favors only the permissions needed for the extension’s main functionality and notes that warning-triggering permission changes can disable an extension until the user accepts them. Source: [Permission warning guidelines](https://developer.chrome.com/docs/extensions/develop/concepts/permission-warnings).
- OWASP Secure Headers guidance still treats HSTS, CSP, framing protection, referrer policy, permissions policy, and content-type hardening as active defense-in-depth controls for modern web apps. Source: [OWASP Secure Headers Project](https://owasp.org/www-project-secure-headers/index.html).

## Access Requests And Evidence Gaps

- Need Google Search Console access for `trustedbums.com` to review Security Issues, Manual Actions, indexing warnings, and sitemap processing in-session.
- Need Bing Webmaster Tools, SmartScreen or Defender feedback visibility, and any active reputation-scanner dashboards the business already uses.
- Need DNS, registrar, hosting, and any CDN or WAF dashboard access to verify DMARC/SPF/DKIM records, live response headers, anti-abuse controls, and the TLS chain or intended ownership for `rcdl.tplinkdns.com`.
- Need one authenticated admin path to execute the deployed `dmarc-reports` function against `bums@trustedbums.com` during the run, or equivalent mailbox-backed DMARC review evidence.
- Need current Microsoft 365 or other sender-auth and postmaster dashboards to verify sender inventory, DKIM/SPF alignment, and policy drift before advancing DMARC enforcement.
- Need one safe seeded tracked-email delivery id to prove the `email-track` allowed-host redirect plus click recording path in a live smoke check.

## Agent Inputs

- Date of run: 2026-06-08.
- Files and docs reviewed: [docs/agents/automation-prompts/trusted-bums-daily-trust-reputation-consultant.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/automation-prompts/trusted-bums-daily-trust-reputation-consultant.toml), [docs/agents/consultant-team-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/consultant-team-rules.md), [docs/agents/company-wide-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/company-wide-rules.md), [docs/agents/consultant-access-needs.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/consultant-access-needs.md), [docs/agents/business-access-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/business-access-rules.md), [docs/shared-mailbox-operations.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/shared-mailbox-operations.md), [docs/dmarc-report-review.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/dmarc-report-review.md), prior [docs/trust-reputation-backlog.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/trust-reputation-backlog.md), [docs/codex-edit-log.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/codex-edit-log.md), [index.html](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/index.html), [public/.htaccess](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/.htaccess), [public/robots.txt](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/robots.txt), [public/sitemap.xml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/sitemap.xml), [public/downloads/trustedbums-extension.zip](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/downloads/trustedbums-extension.zip), [src/App.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/App.tsx), [src/pages/PrivacyPolicy.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/PrivacyPolicy.tsx), [src/pages/LegalDocumentPage.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/LegalDocumentPage.tsx), [src/pages/Index.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/Index.tsx), [chrome-extension/trustedbums/manifest.json](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/chrome-extension/trustedbums/manifest.json), [supabase/config.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/config.toml), [supabase/functions/submit-contact/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/submit-contact/index.ts), [supabase/functions/send-website-email/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/send-website-email/index.ts), [supabase/functions/email-track/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/email-track/index.ts), and [supabase/functions/dmarc-reports/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/dmarc-reports/index.ts).
- Commands and checks reviewed: `git status --short`, `git log --oneline -n 12`, `git branch --show-current`, targeted `rg`, targeted `sed`, `curl -I -L --max-time 20 https://trustedbums.com`, `curl -I -L --max-time 20 https://trustedbums.com/robots.txt`, `curl -I -L --max-time 20 https://trustedbums.com/downloads/trustedbums-extension.zip`, `curl -I -L --max-time 20 https://www.trustedbums.com`, `curl -I -L --max-time 20 http://trustedbums.com`, `curl -I -L --max-time 20 http://www.trustedbums.com`, `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`, `curl -sL --max-time 20 https://trustedbums.com`, `curl -sL --max-time 20 https://trustedbums.com/privacy-policy`, `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 10 --json databaseId,workflowName,displayTitle,headSha,status,conclusion,createdAt,updatedAt`, and `/Users/macdaddy/bin/gh-trustedbums run view 27112837432 --job 80013874698 --log-failed`.
- External guidance reviewed: [Security issues report](https://support.google.com/webmasters/answer/9044101), [Email sender guidelines FAQ](https://support.google.com/a/answer/14229414), [Email sender guidelines](https://support.google.com/a/answer/81126?hl=en-na), [Set up BIMI](https://support.google.com/a/answer/10911320?hl=en), [Unexpected SmartScreen block or warning on website](https://learn.microsoft.com/en-us/troubleshoot/microsoft-edge/development/unexpected-block-warning), [Permission warning guidelines](https://developer.chrome.com/docs/extensions/develop/concepts/permission-warnings), [Permissions](https://developer.chrome.com/docs/extensions/reference/permissions-list), and [OWASP Secure Headers Project](https://owasp.org/www-project-secure-headers/index.html).
- Checks that could not run and why: No live Search Console, Safe Browsing owner dashboard, Bing Webmaster Tools, SmartScreen or Defender portal, registrar, CDN or WAF dashboard, or third-party scanner dashboard was available in-session. No mailbox-backed DMARC review ran because the session had no authenticated admin invocation path for the deployed `dmarc-reports` function. No seeded valid-delivery tracked-link smoke could run because this session had no safe non-production delivery id. The required external DNS target `https://rcdl.tplinkdns.com` still failed TLS verification from this runner, so external DNS-context validation remained partial.
