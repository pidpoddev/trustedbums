# Trusted Bums Trust And Reputation Backlog

_Last updated: 2026-06-21 by Codex daily trust and reputation consultant automation._

## Executive Read

`https://trustedbums.com` remains in a clean primary trust state on exact head `5af32edeb0cc1290cdbae808207e75276d22a4d6`. GitHub `QA` run `27885457568`, DreamHost deploy run `27885457565`, and hosted `E2E Smoke` run `27885474019` all completed `success` on 2026-06-20 UTC for this head. Fresh runner checks on 2026-06-21 still show `HTTP/2 200`, current security headers, distinct canonical and `og:url` metadata for key public trust routes, a live XML sitemap, Bing verification in the initial HTML, `Last-Modified: Sat, 20 Jun 2026 15:11:30 GMT`, and the old public extension ZIP still blocked with `HTTP/2 403`. The focused trust test pack passed `26/26`.

The only active trust recommendation is back on the named external DNS target. Ryan's current run instruction explicitly restores `https://rcdl.tplinkdns.com` as the external DNS surface when that context is needed, but the host still fails local TLS validation and still returns `403` when forced over either HTTPS or HTTP. That means the tracker and shared rules must treat `TB-0024` as active again, while still keeping the broken external host separate from the healthy primary-host release proof.

## Active Recommendations

### P1 - Repair or formally retire `rcdl.tplinkdns.com`
- Evidence: Exact head `5af32ed` is green on hosted `QA` `27885457568`, DreamHost deploy `27885457565`, and hosted `E2E Smoke` `27885474019` against `https://trustedbums.com`, but fresh 2026-06-21 runner checks still show `curl -I -L https://rcdl.tplinkdns.com` failing certificate validation, `curl -k -I -L https://rcdl.tplinkdns.com` returning `HTTP/1.1 403`, and `curl -I -L http://rcdl.tplinkdns.com` returning `HTTP/1.1 403`. Live linked SQL also showed `TB-0024` had been closed on 2026-06-20 even though the current owner instruction still names this host for external DNS context.
- Why it matters: A named but broken external DNS target creates trust-evidence drift across consultant runs, release notes, and operator checks. It can be mistaken for a broader public trust outage or certificate problem even while `trustedbums.com` itself is healthy.
- Recommendation: Either restore a valid certificate chain plus intended routing on `rcdl.tplinkdns.com`, or remove that host from the current consultant instructions, tracker scope, and trust runbooks in the same change set. Until one of those outcomes lands, keep `TB-0024` open and explicitly separate from `trustedbums.com` release proof.
- Acceptance criteria: `curl -I -L https://rcdl.tplinkdns.com` succeeds from the runner with a valid certificate chain and an intended `200`/`301`/`302` response, or Ryan removes the host from the current external-DNS contract and the tracker plus mirrored docs are updated in the same session before `TB-0024` is closed again.

## Reputation Checks

- Browser and endpoint-security reputation: No owner-verified Safe Browsing, SmartScreen, Defender, VirusTotal, or equivalent scanner dashboards were callable in this session. The primary public host is healthy; the explicitly named external DNS host is not.
- Google Search and Safe Browsing: No live Search Console Security Issues, Manual Actions, URL Inspection, or Safe Browsing owner views were callable. Production still serves the Google site-verification TXT record and crawler-visible metadata on the primary public routes.
- Bing and search-discovery trust: Production still serves the Bing verification tag, `robots.txt`, and XML sitemap on exact head `5af32ed`. Current report-level Bing traffic data could not be rerun because `BING_WEBMASTER_API_KEY` stayed missing after sourcing `.env.qa`.
- DNS, HTTPS, and security headers: Fresh 2026-06-21 runner checks show `https://trustedbums.com` returning `HTTP/2 200` with HSTS, CSP, frame, content-type, referrer, and permissions headers, plus `Last-Modified: Sat, 20 Jun 2026 15:11:30 GMT`. The deployed CSP still includes Turnstile, `clerk.trustedbums.com`, Supabase, Microsoft Graph, Google Analytics, Google Tag Manager, and Microsoft Clarity. `https://rcdl.tplinkdns.com` still fails local TLS verification, and forced HTTPS plus plain HTTP both return `403`.
- Email authentication and DMARC: Fresh DNS still resolves `_dmarc.trustedbums.com` to `v=DMARC1; p=quarantine; rua=mailto:bums@trustedbums.com; pct=100;`, root SPF to `v=spf1 include:spf.protection.outlook.com -all`, Microsoft `selector1` DKIM, Clerk `clk` DKIM, and `clkmail` SPF. The `44/44` aligned DMARC aggregate result remains historical from the 2026-06-09 mailbox review because this session still had no callable mailbox-read invocation path.
- Public contact and mail trust: [supabase/functions/submit-contact/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/submit-contact/index.ts) still enforces Turnstile verification, origin allowlisting, honeypot skip, and submission rate limiting. [supabase/functions/send-website-email/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/send-website-email/index.ts) still restricts callers to service-role or internal-secret proof and now defaults contact notifications to the Trusted Bums owner group plus `bums@trustedbums.com`. `src/test/websiteEmailFunction.test.ts`, `src/test/emailTrackingSecurity.test.ts`, `src/test/bumExtensionDownload.test.ts`, `src/test/dmarcReportsFunction.test.ts`, `src/test/adminSharedMailbox.test.ts`, `src/test/serviceRoleAuthorization.test.ts`, and `src/test/claimDeclineWorkflow.test.ts` all passed in this run.
- Tracked admin email links: [supabase/functions/email-track/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/email-track/index.ts) still allows only `https` destinations on `trustedbums.com` or `www.trustedbums.com`. The last retained seeded click proof remains historical from 2026-06-20 and was not rerun in this session.
- Search and social metadata: Fresh production HTML still returns distinct `<title>`, canonical, and `og:url` values for `/`, `/privacy-policy/`, `/legal/terms-of-service/`, and `/bums/`. [public/robots.txt](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/robots.txt) and [public/sitemap.xml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/sitemap.xml) remain live on the primary host.
- Extension reputation: The old public ZIP still returns `HTTP/2 403`. [chrome-extension/trustedbums/manifest.json](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/chrome-extension/trustedbums/manifest.json) remains narrowed to LinkedIn, the project-specific Supabase host, `trustedbums.com`, and Clerk hosts.
- Public credibility: Privacy and legal routes remain reachable with distinct initial metadata, and the primary production host is serving the current exact-head route map without a new trust regression.

## Watchlist

- [supabase/config.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/config.toml) still shows many `verify_jwt = false` functions. Supabase's current Edge Function docs still say the platform check is on by default but also document valid patterns where teams disable it and enforce auth in code instead. Current source review still shows Clerk-session, admin-role, or service-secret checks on the trust-sensitive surfaces inspected here, so the remaining trust question is runtime proof of the intended auth path rather than the flag by itself.
- User-reported mail-placement friction still exists for a real claim-decision email: on 2026-06-18, Ryan relayed that Akshay found the CoreWeave test claim email in junk before replying `approved`. The remaining trust gap is why the original client email landed in junk, which still lacks message trace, delivery classification, or safe recipient-header evidence.
- No exact-head `Visual UI Audit` run exists yet for `5af32ed`. Trust-specific public-route curl and metadata checks were rerun directly in this shell, but screenshot-backed current-head public proof is still one hosted artifact behind the primary QA and deploy surfaces.
- Current owner-console trust reporting is still partially blocked by local env drift: raw shell had none of the trust-report variables exported, and sourcing `.env.qa` restored only `QA_BASE_URL` plus `QA_EXTENSION_API_TOKEN`, not `BING_WEBMASTER_API_KEY`, `GA4_PROPERTY_ID`, or `GOOGLE_APPLICATION_CREDENTIALS`.

## Current Standards And Time-Sensitive Notes

- Google Search guidance still says indexing triage should check Manual Actions and Security Issues before assuming a crawl-only problem. Sources: [Manual actions report](https://support.google.com/webmasters/answer/9044175?hl=en), [URL Inspection tool](https://support.google.com/webmasters/answer/9012289?hl=en).
- Google Search Central still says sitemaps can be surfaced through Search Console, the Search Console API, or a `Sitemap:` line in `robots.txt`, and Google still treats crawlable internal links as discovery signals while treating manipulative link schemes as spam-policy risk. Sources: [Build and submit a sitemap](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap), [Google Search Essentials](https://developers.google.com/search/docs/essentials).
- Gmail sender guidance still expects SPF, DKIM, and DMARC on the sending domain to improve inbox placement and reduce spam or rejection risk. Source: [Email sender guidelines](https://support.google.com/mail/answer/81126?hl=en).
- Bing still recommends IndexNow, direct URL submission, crawlable internal links, and healthy sitemap submission as supported discovery paths. Sources: [URL Submission](https://www.bing.com/webmasters/help/URL-Submission-62f2860b), [Bing Webmaster Guidelines](https://www.bing.com/webmasters/help/webmaster-guidelines-30fba23a).
- Microsoft's current SmartScreen guidance, updated last month, still ties warnings to phishing or malware signals, download reputation, TLS posture, and user feedback. Sources: [Microsoft Defender SmartScreen overview](https://learn.microsoft.com/en-us/windows/security/operating-system-security/virus-and-threat-protection/microsoft-defender-smartscreen/), [Microsoft Edge support for Microsoft Defender SmartScreen](https://learn.microsoft.com/en-us/deployedge/microsoft-edge-security-smartscreen).
- Supabase's current docs, updated last week, still say Edge Function `verify_jwt` is enabled by default, API keys belong in the `apikey` header rather than the `Authorization` header, and teams can deliberately turn the platform check off when they enforce their own auth mode in code. Sources: [Function Configuration](https://supabase.com/docs/guides/functions/function-configuration), [Securing Edge Functions](https://supabase.com/docs/guides/functions/auth), [Authorization headers](https://supabase.com/docs/guides/functions/auth-headers).
- Supabase's current changelog still carries the March 2026 hosted rate limit on recursive or nested Edge Function calls and did not surface a new June 2026 trust-specific breaking change for the current contact, tracking, mailbox, or claim-reply functions. Source: [Supabase changelog](https://supabase.com/changelog).
- OWASP Secure Headers still treats HSTS, CSP, framing protection, referrer policy, and permissions policy as active browser-trust controls worth keeping in deployed checks. Source: [OWASP Secure Headers Project](https://owasp.org/www-project-secure-headers/).

## Access Requests And Evidence Gaps

- Need owner access to Google Search Console Security Issues, Manual Actions, sitemap processing, and Safe Browsing context for `trustedbums.com`.
- Need Bing owner-console views for Site Explorer, URL Inspection, IndexNow Insights, and any Microsoft-side reputation messages that are not available through the current API path. Either restore `BING_WEBMASTER_API_KEY` in the local trust shell or route those checks through an approved dashboard workflow.
- Need Microsoft SmartScreen or Defender feedback visibility and any reputation-scanner dashboards already used by the business.
- Need message trace, delivery classification, and safe recipient-header evidence for the Akshay/CoreWeave claim email that landed in junk before expanding the trust backlog beyond the current mail-reputation evidence gap.
- Need a direct callable mailbox-read path or retained parsed DMARC aggregate store if future runs are expected to revalidate mailbox-backed DMARC evidence instead of citing 2026-06-09 historical results.
- Need dependable project-scoped Supabase trust reads. This session used linked CLI SQL to read the current `TB-0024` row, but it did not have fresh live function inventory, runtime logs, or scanner-dashboard proof.
- The named external DNS target is active again for consultant context because Ryan explicitly asked for it in this run. That surface now needs either infrastructure repair or a same-session owner decision that removes it from the current trust contract.

## Agent Inputs

- Date of run: 2026-06-21.
- Files and docs reviewed: [docs/agents/automation-prompts/trusted-bums-daily-trust-reputation-consultant.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/automation-prompts/trusted-bums-daily-trust-reputation-consultant.toml), [docs/consultant-team-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/consultant-team-rules.md), [docs/agents/consultant-team-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/consultant-team-rules.md), [docs/company-wide-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/company-wide-rules.md), [docs/agents/company-wide-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/company-wide-rules.md), [docs/consultant-access-needs.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/consultant-access-needs.md), [docs/agents/consultant-access-needs.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/consultant-access-needs.md), [docs/business-access-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/business-access-rules.md), [docs/shared-mailbox-operations.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/shared-mailbox-operations.md), [docs/dmarc-report-review.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/dmarc-report-review.md), [docs/codex-edit-log.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/codex-edit-log.md), [public/robots.txt](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/robots.txt), [public/sitemap.xml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/sitemap.xml), [chrome-extension/trustedbums/manifest.json](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/chrome-extension/trustedbums/manifest.json), [supabase/config.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/config.toml), [supabase/functions/send-website-email/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/send-website-email/index.ts), [supabase/functions/submit-contact/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/submit-contact/index.ts), [supabase/functions/email-track/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/email-track/index.ts), [supabase/functions/admin-shared-mailbox/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/admin-shared-mailbox/index.ts), [src/test/websiteEmailFunction.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/websiteEmailFunction.test.ts), [src/test/emailTrackingSecurity.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/emailTrackingSecurity.test.ts), [src/test/bumExtensionDownload.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/bumExtensionDownload.test.ts), [src/test/dmarcReportsFunction.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/dmarcReportsFunction.test.ts), [src/test/adminSharedMailbox.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/adminSharedMailbox.test.ts), [src/test/serviceRoleAuthorization.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/serviceRoleAuthorization.test.ts), and [src/test/claimDeclineWorkflow.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/claimDeclineWorkflow.test.ts).
- Live repo, tracker, and hosted checks reviewed: `git rev-parse HEAD`; `git log --oneline --decorate -n 12`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 40 --json databaseId,workflowName,displayTitle,headSha,status,conclusion,createdAt,updatedAt`; `supabase db query --linked -o json "select tracking_id,status,priority,title,updated_at,source_key,evidence_links from public.admin_scrum_items where tracking_id = 'TB-0024';"`; raw shell env presence check; sourced `.env.qa` env presence check; `curl -I -L --max-time 20 https://trustedbums.com`; `curl -I -L --max-time 20 https://trustedbums.com/sitemap.xml`; `curl -I -L --max-time 20 https://trustedbums.com/downloads/trustedbums-extension.zip`; `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`; `curl -k -I -L --max-time 20 https://rcdl.tplinkdns.com`; `curl -I -L --max-time 20 http://rcdl.tplinkdns.com`; metadata checks against `/`, `/privacy-policy/`, `/legal/terms-of-service/`, and `/bums/`; `dig +short TXT _dmarc.trustedbums.com`; `dig +short TXT trustedbums.com`; `dig +short TXT selector1._domainkey.trustedbums.com`; `dig +short TXT clk._domainkey.trustedbums.com`; `dig +short TXT clkmail.trustedbums.com`; and `corepack pnpm exec vitest run src/test/websiteEmailFunction.test.ts src/test/emailTrackingSecurity.test.ts src/test/bumExtensionDownload.test.ts src/test/dmarcReportsFunction.test.ts src/test/adminSharedMailbox.test.ts src/test/serviceRoleAuthorization.test.ts src/test/claimDeclineWorkflow.test.ts`.
- External guidance reviewed: [Build and submit a sitemap](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap), [Google Search Essentials](https://developers.google.com/search/docs/essentials), [Manual actions report](https://support.google.com/webmasters/answer/9044175?hl=en), [URL Inspection tool](https://support.google.com/webmasters/answer/9012289?hl=en), [Email sender guidelines](https://support.google.com/mail/answer/81126?hl=en), [URL Submission](https://www.bing.com/webmasters/help/URL-Submission-62f2860b), [Bing Webmaster Guidelines](https://www.bing.com/webmasters/help/webmaster-guidelines-30fba23a), [Microsoft Defender SmartScreen overview](https://learn.microsoft.com/en-us/windows/security/operating-system-security/virus-and-threat-protection/microsoft-defender-smartscreen/), [Microsoft Edge support for Microsoft Defender SmartScreen](https://learn.microsoft.com/en-us/deployedge/microsoft-edge-security-smartscreen), [Function Configuration](https://supabase.com/docs/guides/functions/function-configuration), [Securing Edge Functions](https://supabase.com/docs/guides/functions/auth), [Authorization headers](https://supabase.com/docs/guides/functions/auth-headers), [Supabase changelog](https://supabase.com/changelog), and [OWASP Secure Headers Project](https://owasp.org/www-project-secure-headers/).
- Checks that could not run and why: No owner Search Console, Safe Browsing, SmartScreen or Defender portal, registrar, CDN/WAF dashboard, or third-party reputation scanner dashboard was callable in this session. No direct mailbox-read invocation path was callable, so DMARC mailbox evidence stays historical. No exact-head `Visual UI Audit` workflow exists yet for `5af32ed`. `corepack pnpm bing:webmaster traffic` remains blocked after sourcing `.env.qa` because `BING_WEBMASTER_API_KEY` is missing locally. `corepack pnpm ga4:report -- --preset=overview --start-date=7daysAgo --end-date=today` remains blocked after sourcing `.env.qa` because both `GA4_PROPERTY_ID` and `GOOGLE_APPLICATION_CREDENTIALS` are missing. This run did not claim fresh scanner-dashboard, runtime-log, or mailbox-read proof beyond the current public-host, DNS, metadata, tracker, and targeted test evidence listed above.
