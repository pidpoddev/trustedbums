# Trusted Bums Trust And Reputation Backlog

_Last updated: 2026-06-09 by Codex Trust & Reputation ticket work._

## Executive Read

Current live trust evidence improved materially after the prior trust run. Direct runner checks on 2026-06-08 Eastern and hosted runs on 2026-06-09 UTC show `https://trustedbums.com` returning `HTTP/2 200` with HSTS, CSP, frame, content-type, referrer, and permissions headers, while DreamHost deploy run `27178512660` and hosted `E2E Smoke` run `27178530411` both passed on commit `9f42bf4`. The current GitHub `QA` run `27178512695` failed on a backlog-string regression test in `src/test/scrumQueueRegression.test.ts`, not on a live trust or production-reachability signal.

The trust backlog is now narrowed to one active item: extension distribution hardening after removing the public pre-store zip. `TB-0057` is complete as of 2026-06-09: `dmarc-reports` version 3 is deployed, the mailbox-backed review parses Google ZIP plus Microsoft/Yahoo GZIP aggregate reports without errors, the expanded parsed set shows 44/44 aligned messages with 0 full SPF/DKIM alignment failures, and `_dmarc.trustedbums.com` is live at `p=quarantine; pct=100`. `TB-0058` is complete in source and production: public route metadata now comes from a compact manifest, `scripts/render-route-metadata.mjs` generates initial HTML for acquisition, trust, and legal routes during build, tab titles stay under the manifest 32-character limit, GitHub QA run `27225576267` passed, DreamHost deploy run `27225575337` passed, and production `curl -sL` checks show route-specific metadata on no-slash public URLs. `TB-0059` is partially complete in production: the old public zip URL now returns `HTTP/2 403` after commit `17a2d49`, GitHub QA run `27228362035`, and DreamHost deploy run `27228362614`; the remaining work is live deployment of the Bum-only Supabase download function, extension permission narrowing, and final extension API CORS restriction.

Independent external-DNS validation remains partly blocked rather than failed. `https://rcdl.tplinkdns.com` still failed TLS verification from this runner on 2026-06-08 with `curl: (60) SSL certificate problem: unable to get local issuer certificate`. That is still a control-plane evidence gap until DNS or hosting ownership is confirmed, not evidence that `https://trustedbums.com` is currently down or distrusted.

## Completed Recommendations

### P1 - [TB-0057] Deploy the DMARC gzip parser fix and advance enforcement after expanded review
- Evidence: The live `dmarc-reports` function was deployed to Supabase project `vaoqvtxqvbptyxddpoju` as version 3 on 2026-06-09 with the gzip-before-ZIP parser fix from [supabase/functions/dmarc-reports/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/dmarc-reports/index.ts). A fresh authenticated admin invocation against `bums@trustedbums.com` for 90 days and 100 messages scanned 100 messages, found 17 likely DMARC reports, parsed all 17 reports, summarized 44 reported messages, recorded 44 aligned passes, and found 0 full SPF/DKIM alignment failures. The report set included Google ZIP attachments plus Microsoft/Outlook and Yahoo `.xml.gz` attachments with `application/gzip`, and the live response returned no parse errors. DNS now resolves `_dmarc.trustedbums.com` as `v=DMARC1; p=quarantine; rua=mailto:bums@trustedbums.com; pct=100;` from DreamHost ns1/ns2/ns3 and the default resolver. Root SPF remains `v=spf1 include:spf.protection.outlook.com -all`, Microsoft DKIM selector `selector1` resolves, and Clerk/SendGrid `clk` DKIM plus `clkmail` SPF records resolve.
- Result: Complete. The prior monitor-only rollback record was `v=DMARC1; p=none; rua=mailto:bums@trustedbums.com; pct=100;`; Ryan is the rollback owner if post-quarantine reporting exposes legitimate sender drift.
- Follow-up watch: After at least one clean reporting window under quarantine, consider `p=reject; pct=100` if no legitimate full-fail sources appear. Keep parsed aggregate storage on the improvement queue so future Trust Agent runs can trend sender drift instead of re-reading mailbox history.

### P1 - [TB-0058] Serve route-specific metadata in the initial HTML response for public trust routes
- Evidence: Source now keeps public metadata in [src/data/publicRouteMetadata.json](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/data/publicRouteMetadata.json) with `maxTitleLength` set to 32, uses that manifest in [src/components/RouteMetadata.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/RouteMetadata.tsx), and generates static route HTML with [scripts/render-route-metadata.mjs](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/scripts/render-route-metadata.mjs). `corepack pnpm run build` rendered 14 route metadata files. Local production preview on `127.0.0.1:8080` returned distinct initial titles, descriptions, canonical URLs, and `og:url` values before JavaScript for `/`, `/privacy-policy/`, `/legal/terms-of-service/`, `/legal/security/`, `/bums/`, and `/login/`. GitHub QA run `27225576267` and DreamHost deploy run `27225575337` passed on commit `9121b66`. Production `curl -sL` checks returned route-specific initial metadata for `https://trustedbums.com/`, `/privacy-policy`, `/legal/terms-of-service`, `/legal/security`, `/bums`, and `/login`.
- Result: Complete in source, local production preview, hosted QA, DreamHost deploy, and production. DreamHost `.htaccess` rewrites no-slash public route requests such as `/privacy-policy` and `/legal/terms-of-service` to their generated static `index.html` files before the SPA fallback.
- Follow-up watch: Keep all future public acquisition, trust, privacy, and legal routes in `src/data/publicRouteMetadata.json`; do not rely only on client-side `RouteMetadata` for crawler-facing pages.

## Active Recommendations

### P1 - [TB-0059] Remove the public pre-store extension zip until permissions are narrowed
- Evidence: Live `curl -I -L --max-time 20 https://trustedbums.com/downloads/trustedbums-extension.zip` returned `HTTP/2 200` with `content-type: application/zip` on 2026-06-08, confirming the download is still public. [chrome-extension/trustedbums/manifest.json](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/chrome-extension/trustedbums/manifest.json) still requests `activeTab`, `cookies`, `storage`, and host permissions for LinkedIn, `*.supabase.co`, `api.trustedbums.com`, `trustedbums.com`, and Clerk hosts. Google Search Console’s current Security Issues guidance still treats harmful downloads as part of the site-review path, and Chrome’s current extension guidance still says to request only permissions relevant to the extension’s main function. Sources: [Security issues report](https://support.google.com/webmasters/answer/9044101), [Permission warning guidelines](https://developer.chrome.com/docs/extensions/develop/concepts/permission-warnings).
- Why it matters: A publicly downloadable pre-store extension with broad permissions creates reviewer distrust, increases download-reputation risk on the main domain, and weakens the site’s high-trust B2B posture even if the file is not prominently linked.
- Recommendation: Pull the zip off the public site until the distribution plan is final, then reduce permissions to the narrowest production set and lock extension API CORS to the final extension origin.
- Current status: [public/.htaccess](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/.htaccess) now forbids `/downloads/trustedbums-extension.zip`, the repository no longer stores the raw zip or base64 package, [supabase/functions/bum-extension-download/trustedbums-extension.zip.enc](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/bum-extension-download/trustedbums-extension.zip.enc) stores only the encrypted package, [supabase/functions/bum-extension-download/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/bum-extension-download/index.ts) requires a verified Clerk session and approved `BUM` profile before decrypting it, and [src/pages/client/ClientTrainings.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientTrainings.tsx) exposes the download only inside the authenticated Bum Training & Assets page. Commit `20d3cce` passed GitHub QA run `27229361411` and DreamHost deploy run `27229361652`; Supabase MCP deployed `bum-extension-download` version 2 with custom Clerk verification. Production `curl -I -L --max-time 20 https://trustedbums.com/downloads/trustedbums-extension.zip` returns `HTTP/2 403`, live function requests without a bearer token or with a malformed bearer token return `HTTP/2 401`, and GitHub contents API now returns `404` for the raw zip on `main`.
- Remaining gap: Verify the live approved Bum download allow path and a real non-Bum deny path before treating the Bum portal download button as fully accepted. Extension permission narrowing and final extension API CORS restriction are still open acceptance items.
- Acceptance criteria: The public site returns a controlled non-public result for `/downloads/trustedbums-extension.zip` unless distribution is intentionally re-enabled; extension permissions are reduced to the smallest shipped set; and extension API CORS is restricted to the final production extension origin.

## Reputation Checks

- Browser and endpoint-security reputation: No live SmartScreen, Defender, Safe Browsing owner dashboard, VirusTotal, URLVoid, or equivalent scanner verdicts were available in-session.
- Google Search and Safe Browsing: No live Search Console Security Issues, Manual Actions, or owner-verified Safe Browsing path was available in-session.
- DNS, HTTPS, and security headers: `https://trustedbums.com` returned `HTTP/2 200` from the runner on 2026-06-08 with `Strict-Transport-Security`, `Content-Security-Policy`, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, and `Permissions-Policy` present. The latest hosted `E2E Smoke` run `27178530411` on commit `9f42bf4` also passed on 2026-06-09 UTC. `https://rcdl.tplinkdns.com` still failed TLS verification from this runner with `curl: (60) SSL certificate problem: unable to get local issuer certificate`, so that fallback path remains an evidence and ownership gap rather than a confirmed primary-site outage.
- Email authentication and DMARC: Current mailbox-backed review is available and was re-exercised against `bums@trustedbums.com` on 2026-06-09 after deploying `dmarc-reports` version 3. Parsed reports showed 44/44 aligned passes and 0 full failures across Google ZIP plus Microsoft/Yahoo GZIP aggregate reports, and `_dmarc.trustedbums.com` now resolves as `p=quarantine; pct=100`.
- Public contact and mail trust: [supabase/functions/submit-contact/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/submit-contact/index.ts) still enforces Turnstile verification, origin allowlisting, and rate limiting. [supabase/functions/send-website-email/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/send-website-email/index.ts) still restricts callers to service-role or internal-secret proofs before it can send email.
- Tracked admin email links: [supabase/functions/email-track/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/email-track/index.ts) still restricts tracked click destinations to `https` URLs on `trustedbums.com` or `www.trustedbums.com`. The remaining evidence gap is a safe seeded valid-delivery click that proves redirect plus event recording in one live pass.
- Search and social metadata: [public/sitemap.xml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/sitemap.xml) already uses absolute `https://trustedbums.com/...` URLs. Public route metadata now has generated initial HTML coverage for root, Bum landing, login, privacy, and legal routes, with runtime head updates reading the same manifest.
- Extension reputation: Production now blocks the old public extension zip URL with `HTTP/2 403`; the source includes a Bum-gated download function, but that function still needs live deployment. The manifest still asks for `cookies` plus broad host access.
- Public credibility: Privacy and legal pages are present and reachable, and generated route metadata now gives those pages distinct initial titles, descriptions, canonical URLs, and `og:url` values before React runs.

## Watchlist

- [supabase/functions/dmarc-reports/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/dmarc-reports/index.ts) still accepts a mailbox string in request input. The real control remains Exchange mailbox scoping plus admin authorization. Current positive admin read proof exists for `bums@trustedbums.com`; keep the mailbox argument under review if more tenant mailboxes are added.
- Keep `email-track` in post-deploy smoke coverage. The source still looks correctly allowlisted, but this run did not have a safe seeded delivery id to prove redirect plus event recording together.
- [supabase/config.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/config.toml) still lists many `verify_jwt = false` edge functions. Some are expected because they use public or custom-auth flows, but this remains a trust-sensitive deployment inventory until provenance and intended exposure are clearer.

## Current Standards And Time-Sensitive Notes

- Google Search Console still says harmful downloads and other security issues can trigger warning labels in search results or browser interstitials. Source: [Security issues report](https://support.google.com/webmasters/answer/9044101).
- Gmail sender guidance still requires authenticated mail and recommends DMARC reporting, Google DMARC setup guidance recommends staged policy rollout using `pct`, and BIMI still requires DMARC policy `quarantine` or `reject` with `pct=100`. Microsoft documents DMARC aggregate report XML as likely GZIP-compressed, which matches the current parser fix. Sources: [Email sender guidelines FAQ](https://support.google.com/a/answer/14229414), [Email sender guidelines](https://support.google.com/a/answer/81126?hl=en-na), [Set up DMARC](https://support.google.com/a/answer/2466580?hl=en-AU), [Microsoft DMARC setup](https://learn.microsoft.com/en-us/microsoft-365/security/office-365-security/email-authentication-dmarc-configure?view=o365-worldwide).
- Microsoft’s current SmartScreen website-block guidance says URL reputation, page content, file behavior, TLS security, user feedback, and dynamic behavior affect unsafe-site decisions. Source: [Unexpected SmartScreen block or warning on website](https://learn.microsoft.com/en-us/troubleshoot/microsoft-edge/development/unexpected-block-warning).
- Chrome’s current extension guidance still favors only permissions needed for the extension’s main functionality and warns that user-visible permission changes can disable an extension until accepted. Source: [Permission warning guidelines](https://developer.chrome.com/docs/extensions/develop/concepts/permission-warnings).
- OWASP Secure Headers guidance still treats HSTS, CSP, framing protection, referrer policy, permissions policy, and content-type hardening as active controls. Current production now meets that baseline and should keep it preserved in deploy checks. Source: [OWASP Secure Headers Project](https://owasp.org/www-project-secure-headers/index.html).

## Access Requests And Evidence Gaps

- Need Google Search Console access for `trustedbums.com` to review Security Issues, Manual Actions, indexing warnings, and sitemap processing in-session.
- Need Bing Webmaster Tools, SmartScreen or Defender feedback visibility, and any active reputation-scanner dashboards the business already uses.
- Need DNS, registrar, hosting, and any CDN or WAF dashboard access to verify anti-abuse controls, monitor the new `_dmarc.trustedbums.com` quarantine policy, and confirm the TLS chain or intended ownership for `rcdl.tplinkdns.com`.
- Need current Microsoft 365 or other sender-auth and postmaster dashboards to cross-check sender inventory, DKIM/SPF alignment, and policy drift before advancing DMARC enforcement.
- Need one safe seeded tracked-email delivery id to prove the `email-track` allowed-host redirect plus click recording path in a live smoke check.

## Agent Inputs

- Date of run: 2026-06-09.
- Files and docs reviewed: [docs/agents/automation-prompts/trusted-bums-daily-trust-reputation-consultant.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/automation-prompts/trusted-bums-daily-trust-reputation-consultant.toml), [docs/agents/consultant-team-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/consultant-team-rules.md), [docs/agents/company-wide-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/company-wide-rules.md), [docs/agents/consultant-access-needs.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/consultant-access-needs.md), [docs/agents/business-access-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/agents/business-access-rules.md), [docs/shared-mailbox-operations.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/shared-mailbox-operations.md), [docs/dmarc-report-review.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/dmarc-report-review.md), prior [docs/trust-reputation-backlog.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/trust-reputation-backlog.md), [docs/codex-edit-log.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/codex-edit-log.md), [index.html](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/index.html), [public/.htaccess](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/.htaccess), [public/sitemap.xml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/sitemap.xml), [public/downloads/trustedbums-extension.zip](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/public/downloads/trustedbums-extension.zip), [src/App.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/App.tsx), [src/components/RouteMetadata.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/RouteMetadata.tsx), [src/pages/PrivacyPolicy.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/PrivacyPolicy.tsx), [src/pages/LegalDocumentPage.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/LegalDocumentPage.tsx), [chrome-extension/trustedbums/manifest.json](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/chrome-extension/trustedbums/manifest.json), [supabase/config.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/config.toml), [supabase/functions/submit-contact/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/submit-contact/index.ts), [supabase/functions/send-website-email/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/send-website-email/index.ts), [supabase/functions/email-track/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/email-track/index.ts), and [supabase/functions/dmarc-reports/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/dmarc-reports/index.ts).
- Commands and checks reviewed: `git status --short`, `git log --oneline -n 12`, `git branch --show-current`, targeted `rg`, targeted `sed`, `curl -I -L --max-time 20 https://trustedbums.com`, `curl -I -L --max-time 20 https://trustedbums.com/privacy-policy`, `curl -I -L --max-time 20 https://trustedbums.com/downloads/trustedbums-extension.zip`, `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`, `curl -sL --max-time 20 https://trustedbums.com`, `curl -sL --max-time 20 https://trustedbums.com/privacy-policy`, authenticated live `dmarc-reports` invocation after Supabase version 3 deployment, DreamHost DNS panel verification for `_dmarc`, repeated `dig @ns1.dreamhost.com`, `dig @ns2.dreamhost.com`, `dig @ns3.dreamhost.com`, default-resolver TXT checks for `_dmarc.trustedbums.com`, `corepack pnpm exec vitest run src/test/scrumQueueRegression.test.ts`, `corepack pnpm run lint`, `corepack pnpm run test`, `corepack pnpm run build`, `corepack pnpm exec vite preview --host 127.0.0.1 --port 8080`, local preview `curl -sL` metadata checks for `/`, `/privacy-policy/`, `/legal/terms-of-service/`, `/legal/security/`, `/bums/`, and `/login/`, GitHub QA run `27225576267`, DreamHost deploy run `27225575337`, production `curl -sL` metadata checks for `/`, `/privacy-policy`, `/legal/terms-of-service`, `/legal/security`, `/bums`, and `/login`, `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 12 --json databaseId,workflowName,displayTitle,headSha,status,conclusion,createdAt,updatedAt`, `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --workflow "Visual UI Audit" --limit 6 --json databaseId,headSha,status,conclusion,createdAt,updatedAt,displayTitle`, and `/Users/macdaddy/bin/gh-trustedbums run view 27178512695 --log-failed`.
- External guidance reviewed: [Security issues report](https://support.google.com/webmasters/answer/9044101), [Email sender guidelines FAQ](https://support.google.com/a/answer/14229414), [Email sender guidelines](https://support.google.com/a/answer/81126?hl=en-na), [Set up DMARC](https://support.google.com/a/answer/2466580?hl=en-AU), [Set up BIMI](https://support.google.com/a/answer/10911320?hl=en), [Microsoft DMARC setup](https://learn.microsoft.com/en-us/microsoft-365/security/office-365-security/email-authentication-dmarc-configure?view=o365-worldwide), [Unexpected SmartScreen block or warning on website](https://learn.microsoft.com/en-us/troubleshoot/microsoft-edge/development/unexpected-block-warning), [Permission warning guidelines](https://developer.chrome.com/docs/extensions/develop/concepts/permission-warnings), and [OWASP Secure Headers Project](https://owasp.org/www-project-secure-headers/index.html).
- Checks that could not run and why: No live Search Console, Safe Browsing owner dashboard, SmartScreen or Defender portal, registrar, CDN or WAF dashboard, or third-party scanner dashboard was available in-session. No seeded valid-delivery tracked-link smoke could run because this session had no safe non-production delivery id.
