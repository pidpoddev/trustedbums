# Trusted Bums Product Ops Workflow Backlog

_Last updated: 2026-06-19 by Codex daily product ops workflow analyst automation._

## Executive Read

Closeout update on 2026-06-19: the founder-decision Product Ops batch that this backlog previously held open is now implemented and live-proven. `admin-shared-mailbox` is deployed at version `3`; `client-team`, `admin-access-requests`, and `invite-bum` were redeployed with the new identity-review and referral-trust contracts; production schema now has Inner Circle company support capped at `3`, Inner Circle contact columns and limits, reverse-opportunity handoff fields, and mailbox owner indexes.

The `100` live shared-mailbox rows were triaged into explicit admin work queues with `0` unassigned, `0` uncategorized, and `0` still `OPEN`: `39` `dmarc`, `29` `question`, `26` `support`, `3` `legal`, and `3` `client_criteria`, all left `IN_PROGRESS` for human review. `TB-0102`, `TB-0113`, `TB-0103`, `TB-0051`, and `TB-0097` should now be treated as closed in the live tracker once the closeout commit is pushed. The remaining Product Ops queue in this file is `TB-0052` finance exception readiness, plus any fresh evidence a specialist finds in a later run.

The CoreWeave claim-review path is no longer the main Product Ops blocker. Live SQL now shows `1` approved claim and `1` claim contact, and current source routes `/client/opportunities?claimId=...` directly into the claim details panel with visible decision controls in [`ClientOpportunityNew.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientOpportunityNew.tsx). The operational lesson remains: email replies are valid workflow input only after the reply-sync path records the decision in product state.

## Active Recommendations

Founder-decision items `TB-0102`, `TB-0113`, `TB-0103`, `TB-0051`, and `TB-0097` are no longer active Product Ops recommendations after the 2026-06-19 closeout. Keep future runs focused on new evidence, finance exception readiness, and any fresh production failure rather than re-opening the stale pre-closeout wording.

### P2 - [TB-0052] Narrow finance follow-up to exception lanes, not basic finance-safe reads
- Evidence: Live SQL on 2026-06-19 still shows `0` `customer_payment_reports`, `0` `claim_invoices`, `0` `bum_payouts`, and `0` `managing_bum_commission_allocations`. Current source in [`portalApi.ts`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/lib/portalApi.ts) now partially ships the safer data shape by projecting `CLIENT_FINANCE` reads through finance-safe `select` lists for payment reports and claim invoices, but current admin finance routes in [`AdminPayments.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminPayments.tsx) and [`AdminPayouts.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminPayouts.tsx) still focus on lifecycle flow rather than explicit exception queues.
- Why it matters: Finance-safe reads are necessary, but they do not replace operational exception handling. The first disputed, voided, held, sent-not-paid, paid-not-allocated, or manager-allocation case will otherwise arrive without an operator-first lane.
- Recommendation: Keep `TB-0052` open, but narrow it to the exception model that is still missing. Define the first-class exception states, owner workflow, and audit or reason requirements before the first live paid records arrive.
- Acceptance criteria: Admin finance surfaces can isolate disputed, voided, held, sent-not-paid, paid-not-allocated, and manager-allocation exception states directly; transitions record owner plus reason; and the first live payout or allocation does not require operators to infer rescue work from generic lifecycle status alone.

## Business Access Rule Recommendations

- Keep the shared-mailbox rules in [`docs/business-access-rules.md`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/business-access-rules.md) and [`docs/shared-mailbox-operations.md`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/shared-mailbox-operations.md) aligned with the live truth that category, owner, and close guards are operational requirements now, but current production still needs the matching deployed function before those rules are fully enforceable.
- Add explicit Inner Circle business rules to represented contacts and Inner Circle companies. Bum ownership, the 20-contact cap, 3-company cap, Admin troubleshooting visibility, and client-visible claim-context-only exposure should now be treated as durable access rules, not only supply-language or onboarding copy.
- Decide whether reverse opportunities belong inside `/admin/handoffs` or a separate owner or due-state queue, then document that choice so `TB-0051` closes on a complete workflow model instead of a partial UI pass. `updated_at` last-touch is approved as the fallback stale date when no explicit follow-up deadline exists.
- Finalize the `/client/profile` field and role matrix before any live `CLIENT_IT` or `CLIENT_LEGAL` launch. Ordinary profile edits may be broad for active same-company client users, but legal name and domain changes must require Trusted Bums Admin review after they are set.

## Workflow Map

- Public contact intake: a public visitor submits interest -> `contact_submissions` stores triage metadata -> Admin claims, qualifies, invites, escalates, or archives. Current live proof still shows one unowned `NEW` row with no next action or deadline.
- Shared mailbox intake: `admin-shared-mailbox` syncs `bums@trustedbums.com` into `admin_shared_mailbox_messages` -> Admin triages by category, work queue, owner, and status -> live function version `3` now supports claim/category plus uncategorized close guards, and the current 100-message backlog was triaged into explicit work queues with no unassigned or uncategorized rows.
- Claim review and reply flow: a client reviews a Bum claim in the portal or replies by email -> `sync-claim-decision-replies` records `APPROVED` or `DECLINED` when the token or claim context is valid -> the Bum receives the next-step notification. Current live proof now shows one approved claim and one claim contact.
- Bum relationship graph: a Bum maintains represented contacts, named `Inner Circle` contacts, and up to `3` Inner Circle companies where they have credible context -> claim surfaces may carry that trust signal into route review -> live schema still lacks the required columns, so this remains staged rather than production-operational.
- Client profile and beta setup: `/client/profile` currently mixes broad company-profile edits with narrower beta-setup affordances -> production still lacks the beta config column and live Legal or IT roles -> Product Ops still needs the final ownership matrix before launch.
- Finance workflow: customer payment report -> invoice -> payout -> future manager allocation, with exception lanes still absent before first live volume.

## Watchlist

- Live SQL on 2026-06-19 still shows one unowned `NEW` contact submission with no next action or follow-up deadline. That remains the clearest proof that `TB-0051` is not yet closed operationally.
- Shared mailbox category spread is still narrow in live production: `support`, `question`, `legal`, `dmarc`, and `uncategorized` exist; `privacy`, `abuse`, `complaint`, and `client_criteria` do not yet. The first message in one of those higher-risk categories should trigger a fresh Product Ops plus Legal/Compliance review.
- Inner Circle is now visible in source and docs, but live production still lacks the required columns. Keep it treated as staged launch work until schema and access proof land together.
- Live SQL still shows `1` approved claim, `1` claim contact, and `2` `bum_contacts` rows split between `EXTENSION_CAPTURE` and `OPPORTUNITY_CLAIM`. The next live claim should verify whether the current client portal path is enough on its own or still needs mailbox-reply fallback.
- `client_company_access_requests` still shows only `1` `BUM_SIGNUP:approved` row. Same-domain, related-domain, public-email, and stale-admin override queues still lack live operational traffic.
- This run still could not independently verify Ryan's roughly `80` unhandled BlackCurrant opportunities because no CRM export or live owner and due-state opportunity queue was available in the inspected data path.

## Current Standards And Time-Sensitive Notes

- Zendesk's current SLA guidance still frames SLAs as explicit response and resolution policies rather than passive labels. That supports making Trusted Bums owner, next-action, and due-state fields operational for both contact and mailbox queues. Source: [Defining SLA policies](https://support.zendesk.com/hc/en-us/articles/4408829459866-Defining-SLA-policies).
- Microsoft Learn still treats shared-mailbox activity as an auditable operations surface and documents investigating sent-as activity, folder access, rules, forwarding, and missing-message questions through mailbox auditing. That supports keeping claim/category/status changes and mailbox replies explicitly auditable before broader reviewer access is introduced. Sources: [Manage mailbox auditing](https://learn.microsoft.com/en-us/purview/audit-mailboxes) and [Investigate shared mailbox activities using audit logs](https://learn.microsoft.com/en-us/purview/audit-log-identify-mailboxes).
- Stripe still treats payout reconciliation as a dedicated report and workflow, not something operators should infer from generic payment state alone. That supports first-class finance exception lanes before the first payout or manager-allocation volume arrives. Source: [Payout reconciliation report](https://docs.stripe.com/reports/payout-reconciliation).
- Supabase's 2026 changelog still matters here because new `public` tables and columns now require deliberate grant and deployment review. It is not the main blocker in this run, but it reinforces why new workflow objects should not be treated as live from repo source alone. Source: [Supabase changelog](https://supabase.com/changelog).

## Access Requests And Evidence Gaps

- Product Ops still lacks CRM pipeline exports, opportunity ownership exports, support queue exports with SLA buckets, finance exception samples, admin rescue logs, operations SOPs, and narrated role walkthroughs for Admin, Client Admin, Client Finance, Client Legal, Client IT, Client Member, Bum, and Managing Bum operators.
- This run had live Supabase SQL for queue counts, mailbox category and status counts, claim and represented-contact counts, client-role counts, access-request counts, schema inspection, tracker reads, and live function-source reads. It did not have CRM data, live mailbox body review, or narrated role walkthrough evidence.
- This run did not read live mailbox bodies, reply threads, or attachment payloads. Shared-mailbox evidence in this pass is aggregate production-row-backed plus live function-source-backed rather than a message-content audit.
- This run did not rerun browser or GitHub workflows locally because current hosted QA, deploy, and E2E evidence already existed for `a17a856`, and the Product Ops delta in this pass came from live schema/function truth rather than a fresh UI repro.

## Agent Inputs

- Date of run: 2026-06-19 (`America/New_York`).
- Files and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-product-ops-analyst.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/codex-edit-log.md`, `docs/product-ops-workflow-backlog.md`, `docs/shared-mailbox-operations.md`, `docs/business-access-rules.md`, `docs/release-verification-backlog.md`, [`src/pages/admin/AdminInbox.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminInbox.tsx), [`src/pages/admin/AdminHandoffs.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminHandoffs.tsx), [`src/pages/client/ClientProfile.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientProfile.tsx), [`src/components/FirstLoginWalkthrough.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/FirstLoginWalkthrough.tsx), [`src/pages/bum/BumContacts.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx), [`src/pages/client/ClientClaims.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientClaims.tsx), [`src/pages/bum/BumClaims.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumClaims.tsx), [`src/pages/client/ClientOpportunityNew.tsx`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientOpportunityNew.tsx), [`src/lib/portalApi.ts`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/lib/portalApi.ts), [`supabase/functions/admin-shared-mailbox/index.ts`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/admin-shared-mailbox/index.ts), [`supabase/functions/sync-claim-decision-replies/index.ts`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/sync-claim-decision-replies/index.ts), and [`supabase/migrations/20260618100000_add_inner_circle_contacts.sql`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260618100000_add_inner_circle_contacts.sql).
- Repo and hosted checks reviewed: `git status --short --branch`; `git rev-parse HEAD`; `git log --oneline --decorate -n 12`; `git diff --name-only 57231bf..HEAD -- src supabase docs .github`; `git diff --stat 57231bf..HEAD -- ...`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 20 --json databaseId,headSha,workflowName,status,conclusion,createdAt,updatedAt,displayTitle`; and current [docs/release-verification-backlog.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/release-verification-backlog.md).
- Live Supabase evidence reviewed for project `vaoqvtxqvbptyxddpoju`: project metadata and URL confirmation; live aggregate SQL for `contact_submissions`, `admin_shared_mailbox_messages`, `customer_target_responses`, `client_bum_intro_requests`, `reverse_opportunities`, `customer_payment_reports`, `claim_invoices`, `bum_payouts`, `managing_bum_commission_allocations`, `opportunity_claims`, `opportunity_claim_contacts`, `bum_contacts`, `profiles`, and `client_company_access_requests`; live schema inspection for `companies.deal_registration_config`, `bum_contacts.is_inner_circle`, and `opportunity_claim_contacts.is_inner_circle`; live tracker reads for `TB-0051`, `TB-0052`, `TB-0097`, `TB-0102`, and `TB-0027`; and deployed function-source reads for `admin-shared-mailbox`.
- Current external guidance reviewed: official Zendesk SLA guidance, Microsoft Learn mailbox-auditing guidance, Stripe payout-reconciliation guidance, and the Supabase changelog linked above.
- Checks that could not fully run and why: no CRM export, support queue export, finance exception sample dataset, admin rescue log sample, or narrated operator walkthroughs were available; no live mailbox-content audit was performed because this pass focused on queue shape, schema truth, and deployed function drift rather than raw message review; no fresh browser or Playwright interaction run was required because current hosted QA and E2E evidence already existed for `a17a856`.
