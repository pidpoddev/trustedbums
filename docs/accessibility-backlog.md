# Trusted Bums Accessibility Backlog

_Last updated: 2026-06-10 by Codex accessibility cleanup pass._

## Executive Read

The accessibility queue is narrower after commit `8a9e2d7`. The public contact-form recovery item remains resolved, the public sign-up intent dialog now has programmatic field and group error wiring, and the admin scrum tracker now has programmatic labels plus help text for search, filters, and create-form controls. The active backlog is now the real remaining set: the mobile portal sidebar still hides its only visible dismiss control and lacks a dialog name, and the collapsed site-wide `Privacy choices` launcher is still undersized for WCAG 2.2 target-size guidance.

Current evidence is mixed across public and authenticated surfaces. Current `main` head `ff59d2c` has green hosted `QA` run `27244531408`, deploy run `27244531370`, and `E2E Smoke` run `27244546687`. The remaining evidence gaps are specific: current-head `Visual UI Audit` run `27247209520` failed waiting for the public `Accessibility settings` button after the signup dialog step, `/bums` and `/admin/scrum` are still missing from retained visual coverage, the required external DNS target `https://rcdl.tplinkdns.com` remains infrastructure context only until TLS is fixed, and the repo still has no axe dependency.

## Active Recommendations

### P1 - [TB-0029] Give the mobile portal sidebar an accessible name and a visible dismiss control
- Evidence: `src/components/ui/sidebar.tsx` still renders the mobile portal navigation inside `SheetContent` and hides the default close button with `[&>button]:hidden` at [line 159](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:159). The sidebar wrapper still adds no `SheetTitle`, `aria-label`, or equivalent dialog name before rendering children at [lines 155-168](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:155).
- Why it matters: On mobile, the portal navigation is a modal dialog. Current WAI-ARIA APG guidance still expects the dialog to have an accessible name and strongly recommends a visible close button in the tab sequence.
- Recommendation: Keep the in-sheet close button visible and add a stable dialog name such as `Portal navigation` through `SheetTitle` or `aria-label` for the Admin, Client, and Bum mobile sidebars.
- Acceptance criteria: On mobile widths, opening the sidebar exposes a visible dismiss control, the dialog is announced with a clear name, `Escape` and the dismiss control both close it, and focus returns to the `Toggle Sidebar` trigger.

### P2 - [TB-0032] Enlarge the collapsed `Privacy choices` launcher to meet WCAG 2.2 target-size expectations
- Evidence: After the banner closes, `src/components/ConsentManager.tsx` still renders the persistent launcher as a fixed `Button` with `h-5`, `px-2`, and `text-[10px]` at [lines 141-156](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141). Local anonymous Playwright checks now confirm the privacy manager itself is operable on `127.0.0.1:8080`, but this run still had no fresh mobile screenshot evidence that would justify a target-size exception for the collapsed launcher.
- Why it matters: The privacy launcher is a site-wide legal and consent control. Small fixed targets are harder to activate for touch and motor-impaired users, especially on mobile.
- Recommendation: Increase the launcher hit area to at least 24 by 24 CSS pixels, or replace it with an equivalently reachable same-page control that already meets the target-size expectation.
- Acceptance criteria: The collapsed `Privacy choices` control exposes at least a 24 by 24 CSS pixel hit area on public and portal pages, remains visually discoverable, and does not rely on a separate page to provide the only adequately sized control.

## Watchlist

- `TB-0030` is closed from an implementation standpoint. [SignupIntentDialog.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/SignupIntentDialog.tsx) now connects the signup intent group and field errors with programmatic labels, descriptions, and invalid state after `8a9e2d7`.
- `TB-0031` is closed from an implementation standpoint. [AdminScrumTracker.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminScrumTracker.tsx) now labels search, filters, and create-form controls, and [adminScrumTracker.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/adminScrumTracker.test.ts) locks the pattern.
- Localhost auth routing still needs recheck before it becomes a product defect. In the current Playwright run against `http://127.0.0.1:8080`, 5 of 6 targeted public checks passed, but anonymous navigation to `/admin` stayed on `/admin` instead of reaching `/login` within five seconds. Keep localhost authenticated-route findings as provisional until a deployed-target check or current hosted auth artifact confirms the same behavior.
- Current-head `Visual UI Audit` run `27247209520` failed waiting for the public `Accessibility settings` button after the signup dialog step, and retained route coverage still needs `/bums` and `/admin/scrum`.

## Current Standards And Time-Sensitive Notes

- WCAG 2.2 remains the current W3C baseline for this backlog. The current W3C WCAG overview still points teams to the latest WCAG 2.2 materials, and the current WCAG 2.2 errata remain editorial rather than priority-changing for these findings. Sources: https://www.w3.org/WAI/standards-guidelines/wcag/ and https://www.w3.org/WAI/WCAG22/errata/
- Current WAI-ARIA APG modal dialog guidance still says dialogs need either `aria-labelledby` or `aria-label`, and it strongly recommends a visible close button in the tab sequence. That remains directly applicable to the mobile sidebar sheet. Source: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
- Current W3C form-label guidance still says form controls should be explicitly or implicitly associated with labels, and current WAI accessible-name guidance still treats placeholder and title content as fallback naming sources rather than the preferred control-label pattern. That directly applies to the new admin scrum tracker controls. Sources: https://www.w3.org/WAI/tutorials/forms/labels/ and https://www.w3.org/WAI/ARIA/apg/practices/names-and-descriptions/
- Current WCAG 2.2 target-size guidance still says pointer targets should be at least 24 by 24 CSS pixels unless a defined exception applies. That still applies to the collapsed site-wide `Privacy choices` launcher. Source: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html

## Access Requests And Evidence Gaps

- Authenticated browser-routable QA sessions, fresh desktop and mobile screenshots, keyboard walkthrough notes, screen-reader notes, and axe-backed route coverage are still missing for current Admin, Client Admin, Client Finance, Client Member, and Bum flows.
- Consultant shells still need a deterministic `.env.qa` export step. Current preflight passed only after `set -a; source .env.qa; set +a`, and accessibility runs still need that same bootstrap before local smoke or hosted preflight.
- Local public-route browser evidence is current on `127.0.0.1:8080`, but authenticated or protected-route validation is still incomplete because anonymous `/admin` stayed on `/admin` instead of redirecting to `/login` within the current Playwright timeout.
- The latest hosted smoke evidence is current (`27244546687` on `ff59d2c`), but current-head visual audit run `27247209520` failed and `/admin/scrum` still needs retained visual coverage.
- The required external DNS target still could not provide page-level evidence in this session because `curl -I -L --max-time 20 https://rcdl.tplinkdns.com` failed with `curl: (60) SSL certificate problem: unable to get local issuer certificate`.
- The repo still lacks `@axe-core/playwright` or `axe-core`, so this run could not add automated accessibility-scan findings.

Material missing access, credentials, browser or AT validation, screenshots, axe output, or other evidence needed for a stronger accessibility review. Mirror durable requests in `docs/consultant-access-needs.md`.

## Agent Inputs

- Date of run: 2026-06-08
- Files, tests, and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-accessibility-specialist.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/accessibility-backlog.md`, `docs/consultant-access-needs.md`, `docs/codex-edit-log.md`, `package.json`, `scripts/verify-qa-env.mjs`, `scripts/qa-target-preflight.mjs`, `src/App.tsx`, `src/layouts/AdminLayout.tsx`, `src/components/ui/sidebar.tsx`, `src/components/SignupIntentDialog.tsx`, `src/components/ConsentManager.tsx`, `src/pages/Index.tsx`, `src/pages/admin/AdminScrumTracker.tsx`, `src/lib/portalApi.ts`, `src/test/adminScrumTracker.test.ts`, `src/test/e2eSmokeRegression.test.ts`, `src/test/routeGuards.test.tsx`, `tests/e2e/staging-smoke.spec.ts`, and `tests/e2e/portal-interaction-audit.spec.ts`.
- Checks and commands reviewed: `git status --short`; `git log --since='2026-06-07 00:00' --name-only --pretty=format:'COMMIT %h %cs %s' -- src docs tests`; targeted `rg`, `sed`, and `nl`; `git rev-parse --abbrev-ref HEAD`; `git rev-parse --short HEAD`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 12 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --workflow 'Visual UI Audit' --limit 8 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27178512695 --log-failed`; `set -a; source .env.qa; set +a; QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run qa:env`; `set -a; source .env.qa; set +a; QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run qa:target-preflight`; `/Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm exec vitest run src/test/adminScrumTracker.test.ts src/test/e2eSmokeRegression.test.ts src/test/routeGuards.test.tsx`; `/Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run lint`; `set -a; source .env.qa; set +a; /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run build`; `curl -I --max-time 10 http://127.0.0.1:8080`; `QA_BASE_URL=http://127.0.0.1:8080 QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm exec playwright test tests/e2e/staging-smoke.spec.ts --project=chromium --grep 'loads public pages|lets anonymous visitors manage privacy choices|validates the signup intent dialog before Clerk handoff|validates the separate Bum signup path before Clerk handoff|shows inline contact form recovery without clearing entered details|redirects protected routes away from anonymous users'`; and `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`.
- Internet sources reviewed: https://www.w3.org/WAI/standards-guidelines/wcag/, https://www.w3.org/WAI/WCAG22/errata/, https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/, https://www.w3.org/WAI/tutorials/forms/labels/, https://www.w3.org/WAI/ARIA/apg/practices/names-and-descriptions/, and https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html
- Checks that could not run and why: no authenticated route browser validation against the required external DNS target because `https://rcdl.tplinkdns.com` still fails TLS verification from this runner; no reliable localhost authenticated portal validation because anonymous `/admin` stayed on `/admin` instead of redirecting to `/login` within the current Playwright timeout; no current authenticated browser or visual evidence for the new `/admin/scrum` route because the route exists only in the local worktree and no QA account session was exercised against it in this run; no axe scans because the repo has no axe dependency or configured accessibility spec; and no screen-reader validation because no AT notes or live assistive-technology session access were available.
