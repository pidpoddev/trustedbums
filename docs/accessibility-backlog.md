# Trusted Bums Accessibility Backlog

_Last updated: 2026-06-08 by Codex daily accessibility specialist automation._

## Executive Read

Current-state postscript: current head `441fd92` passed hosted QA, deploy, E2E Smoke, Deep QA, and Visual UI Audit. The accessibility queue remains source-backed and local/hosted-evidence-backed, but older extension-preflight or local-port notes should not be read as active release blockers.

Current source review, current W3C guidance, and the checks that actually ran on 2026-06-08 still support the same four implementation-ready accessibility issues: the mobile portal sidebar still hides its only visible close control and has no accessible dialog name, the public contact form still reports submit errors only through toasts, the collapsed site-wide `Privacy choices` launcher is undersized for WCAG 2.2 target-size expectations, and the public sign-up intent dialog still shows visible validation text without programmatic error wiring. Evidence quality is materially better than the prior run: `corepack pnpm run lint` and `corepack pnpm run build` passed, local `vite preview` on `127.0.0.1:8080` worked after rebuilding with exported QA env vars, and local anonymous Playwright checks passed for the landing page, privacy choices flow, and sign-up intent validation. The remaining gaps are narrower: default `qa:env` still fails on missing `QA_EXTENSION_API_TOKEN` unless extension coverage is intentionally skipped, local localhost auth routing still stalled on `Loading session...` for `/admin`, the required external DNS target `https://rcdl.tplinkdns.com` still fails TLS verification from this runner, and the repo still has no axe dependency.

## Active Recommendations

### P1 - Give the mobile portal sidebar an accessible name and a visible dismiss control
- Evidence: `src/components/ui/sidebar.tsx` still renders the mobile portal navigation inside `SheetContent` and hides the default close button with `[&>button]:hidden` at [line 159](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:159). The sidebar wrapper still adds no `SheetTitle`, `aria-label`, or equivalent dialog name before rendering children at [lines 155-168](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:155).
- Why it matters: On mobile, the portal navigation is a modal dialog. Current WAI-ARIA APG guidance still expects the dialog to have an accessible name and strongly recommends a visible close button in the tab sequence.
- Recommendation: Keep the in-sheet close button visible and add a stable dialog name such as `Portal navigation` through `SheetTitle` or `aria-label` for the Admin, Client, and Bum mobile sidebars.
- Acceptance criteria: On mobile widths, opening the sidebar exposes a visible dismiss control, the dialog is announced with a clear name, `Escape` and the dismiss control both close it, and focus returns to the `Toggle Sidebar` trigger.

### P1 - Add field-level errors and verification messaging to the public contact form
- Evidence: `src/pages/Index.tsx` still rejects invalid name, email, and message values only through a destructive toast at [lines 151-157](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/Index.tsx:151) and still reports missing Turnstile completion only through a second toast at [lines 160-166](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/Index.tsx:160). The rendered contact fields at [lines 549-649](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/Index.tsx:549) still have no inline error text, `aria-invalid`, or error-message `aria-describedby` wiring. Current local Playwright coverage only proves the public landing page loads; it does not yet cover invalid contact-form recovery.
- Why it matters: Toast-only validation forces users to infer which field failed and can disappear before screen-reader, keyboard, or magnification users recover. This remains the main public error-identification and recovery gap.
- Recommendation: Add persistent inline error text for each invalid field and the Turnstile block, mark invalid fields with `aria-invalid`, and move focus to the first invalid field or a concise error summary after submit.
- Acceptance criteria: Invalid submission keeps the user on the form, exposes visible error text next to every failing field, wires each message with `aria-describedby`, marks failing fields with `aria-invalid`, and does not rely on toast timing as the only error signal.

### P2 - Enlarge the collapsed `Privacy choices` launcher to meet WCAG 2.2 target-size expectations
- Evidence: After the banner closes, `src/components/ConsentManager.tsx` still renders the persistent launcher as a fixed `Button` with `h-5`, `px-2`, and `text-[10px]` at [lines 141-156](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141). Local anonymous Playwright checks now confirm the privacy manager itself is operable on `127.0.0.1:8080`, but this run still had no fresh mobile screenshot evidence that would justify a target-size exception for the collapsed launcher.
- Why it matters: The privacy launcher is a site-wide legal and consent control. Small fixed targets are harder to activate for touch and motor-impaired users, especially on mobile.
- Recommendation: Increase the launcher hit area to at least 24 by 24 CSS pixels, or replace it with an equivalently reachable same-page control that already meets the target-size expectation.
- Acceptance criteria: The collapsed `Privacy choices` control exposes at least a 24 by 24 CSS pixel hit area on public and portal pages, remains visually discoverable, and does not rely on a separate page to provide the only adequately sized control.

### P2 - Add programmatic error state to the public sign-up intent dialog
- Evidence: `src/components/SignupIntentDialog.tsx` still renders visible inline validation messages for missing account type, invalid email, and missing company name at [lines 137, 149, and 167](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/SignupIntentDialog.tsx:137), but the radio group and inputs at [lines 105-167](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/SignupIntentDialog.tsx:105) still do not add `aria-invalid` or `aria-describedby`. Current local Playwright coverage against `http://127.0.0.1:8080` passed `tests/e2e/staging-smoke.spec.ts` for the invalid sign-up flow, confirming the visible error text exists but that the test still treats visible text as the complete validation proof.
- Why it matters: Visible error text is better than toast-only validation, but screen-reader users still do not get a reliable programmatic link between the failing control and its error state during a public account-creation flow.
- Recommendation: Keep the inline messages, then add stable IDs plus `aria-describedby` and `aria-invalid` to the role, email, and company controls when validation fails. If the role group remains custom, add one group-level error target that assistive technology can announce reliably.
- Acceptance criteria: After an invalid submit, each failing sign-up control exposes `aria-invalid="true"`, its visible error message is linked with `aria-describedby`, and keyboard or screen-reader users can determine which field failed without relying on surrounding visual context.

## Watchlist

- Local localhost auth routing needs recheck before it becomes a product defect. In the current Playwright run against `http://127.0.0.1:8080`, anonymous navigation to `/admin` stayed on `Loading session...` instead of reaching `/login` within five seconds, so localhost is still not reliable evidence for authenticated portal accessibility without a deployed-target confirmation.

## Current Standards And Time-Sensitive Notes

- WCAG 2.2 remains the live baseline. W3C's WCAG overview page was updated on 2026-05-26 and explicitly says W3C encourages teams to use the latest version of WCAG 2.2. The current WCAG 2.2 errata list still shows editorial-only changes for 2025-10-28 and 2025-06-27, so nothing in the errata changes these backlog priorities. Sources: https://www.w3.org/WAI/standards-guidelines/wcag/ and https://www.w3.org/WAI/WCAG22/errata/
- Current WAI-ARIA APG modal dialog guidance still says dialogs need either `aria-labelledby` or `aria-label`, and it strongly recommends a visible close button in the tab sequence. That remains directly applicable to the mobile sidebar sheet. Source: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
- Current WCAG 2.2 error-identification guidance still says that when an input error is automatically detected, the item in error must be identified and the error described in text. That still makes toast-only recovery inadequate for the public contact form and still supports adding programmatic error wiring to the sign-up intent dialog. Source: https://www.w3.org/WAI/WCAG22/Understanding/error-identification
- Current WCAG 2.2 target-size guidance still says pointer targets should be at least 24 by 24 CSS pixels unless a defined exception applies. That still applies to the collapsed site-wide `Privacy choices` launcher. Source: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html

## Access Requests And Evidence Gaps

- Authenticated browser-routable QA sessions, fresh desktop and mobile screenshots, keyboard walkthrough notes, screen-reader notes, and axe coverage are still missing for current Admin, Client Admin, Client Finance, Client Member, and Bum flows.
- `.env.qa` is present again in this checkout, but consultant shells still need explicit export semantics. `corepack pnpm run qa:env` only passed after `set -a; source .env.qa; set +a` and `QA_EXTENSION_API_EXPECTATION=skip`; the default command still failed on missing `QA_EXTENSION_API_TOKEN`.
- Local public-route browser evidence is now available on `127.0.0.1:8080` after rebuilding with exported QA env vars, but local authenticated or protected-route validation is still incomplete because anonymous `/admin` stayed on `Loading session...` instead of reaching `/login` within the current Playwright timeout.
- The required external DNS target still could not provide page-level evidence in this session because `curl -I -L --max-time 20 https://rcdl.tplinkdns.com` failed with `curl: (60) SSL certificate problem: unable to get local issuer certificate`.
- The repo still lacks `@axe-core/playwright` or `axe-core`, so this run could not add automated axe findings.

Material missing access, credentials, browser or AT validation, screenshots, axe output, or other evidence needed for a stronger accessibility review. Mirror durable requests in `docs/consultant-access-needs.md`.

## Agent Inputs

- Date of run: 2026-06-08
- Files, tests, and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-accessibility-specialist.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/accessibility-backlog.md`, `docs/consultant-access-needs.md`, `docs/codex-edit-log.md`, `package.json`, `scripts/verify-qa-env.mjs`, `scripts/qa-target-preflight.mjs`, `src/App.tsx`, `src/main.tsx`, `src/components/ui/sidebar.tsx`, `src/components/ui/sheet.tsx`, `src/components/ui/dialog.tsx`, `src/components/SignupIntentDialog.tsx`, `src/components/ConsentManager.tsx`, `src/components/AccessibilityMenu.tsx`, `src/components/ProtectedRoute.tsx`, `src/contexts/AuthContext.tsx`, `src/pages/Index.tsx`, `src/pages/PrivacyPolicy.tsx`, `tests/e2e/staging-smoke.spec.ts`, `tests/e2e/portal-interaction-audit.spec.ts`, and `tests/e2e/visual-ui-audit.spec.ts`.
- Checks and commands reviewed: `git status --short`; `git log --since='2026-06-06 00:00' --name-only --pretty=format:'COMMIT %h %cs %s' -- docs/accessibility-backlog.md docs/consultant-access-needs.md src/components/ui/sidebar.tsx src/components/ui/sheet.tsx src/components/ui/dialog.tsx src/layouts/AdminLayout.tsx src/layouts/ClientLayout.tsx src/layouts/BumLayout.tsx src/pages/Index.tsx src/components/SignupIntentDialog.tsx src/components/ConsentManager.tsx src/components/AccessibilityMenu.tsx src/contexts/AccessibilityContext.tsx tests/e2e/visual-ui-audit.spec.ts tests/e2e/portal-interaction-audit.spec.ts tests/e2e/staging-smoke.spec.ts`; targeted `rg`, `sed`, and `nl`; `set -a; source .env.qa; set +a; QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run qa:env`; `set -a; source .env.qa; set +a; QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run qa:target-preflight`; `/Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run lint`; `set -a; source .env.qa; set +a; /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run build`; `/Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm exec vite preview --host 127.0.0.1 --port 8080`; `curl -I --max-time 20 http://127.0.0.1:8080`; `QA_BASE_URL=http://127.0.0.1:8080 QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm exec playwright test tests/e2e/staging-smoke.spec.ts --project=chromium --grep "loads public pages|lets anonymous visitors manage privacy choices|validates the signup intent dialog before Clerk handoff|redirects protected routes away from anonymous users"`; and `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`.
- Internet sources reviewed: https://www.w3.org/WAI/standards-guidelines/wcag/, https://www.w3.org/WAI/WCAG22/errata/, https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/, https://www.w3.org/WAI/WCAG22/Understanding/error-identification, and https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html
- Checks that could not run and why: no authenticated route browser validation against the required external DNS target because `https://rcdl.tplinkdns.com` still fails TLS verification from this runner; no reliable localhost authenticated portal validation because anonymous `/admin` stayed on `Loading session...` instead of redirecting to `/login` within the current Playwright timeout; no axe scans because the repo has no axe dependency or configured accessibility spec; and no screen-reader validation because no AT notes or live assistive-technology session access were available.
