# Trusted Bums Accessibility Backlog

_Last updated: 2026-06-19 by Codex daily accessibility specialist automation._

## Executive Read

Current `main` head [`a17a85639a1b24dfda36da87d763eb4ecd3457af`](https://github.com/pidpoddev/trustedbums/commit/a17a85639a1b24dfda36da87d763eb4ecd3457af) does not justify a new open accessibility defect from the evidence available in this run. Exact-head hosted functional proof is clean on 2026-06-19 UTC: GitHub `QA` run `27798687806`, DreamHost deploy `27798687708`, and `E2E Smoke` `27798711531` all completed `success`, and a fresh runner check still returns `HTTP/2 200` from `https://trustedbums.com`.

The two prior accessibility tracker items still remain closed on current head. `TB-0029` stays closed because [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:153) still exposes a visible `Portal navigation` title, keeps a visible dismiss control, and still drives an explicit `aria-expanded` state from the shared hamburger trigger, while [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts:1) passed `3/3` again in this run. `TB-0032` also stays closed because [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141) still renders the collapsed `Privacy choices` launcher with `min-h-9`, `px-3`, and `py-2`; that source still implies a minimum 36 CSS pixel height, so the earlier sub-24px target-size defect is not present in current code. Both live tracker rows were refreshed to exact head `a17a856` during this run.

Targeted source review of the post-`57231bf` UI churn, including [ClientOpportunityNew.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientOpportunityNew.tsx:1), [BumContacts.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx:1), [BumOpportunityDetail.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunityDetail.tsx:1), [AdminInbox.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminInbox.tsx:1), [FirstLoginWalkthrough.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/FirstLoginWalkthrough.tsx:1), and [BumLanding.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/BumLanding.tsx:1), did not reveal a new unlabeled control, missing heading, hidden close action, or icon-only regression that should be promoted today. Fresh local mobile checks against `http://127.0.0.1:8080/`, `http://127.0.0.1:8080/bums`, and `http://127.0.0.1:8080/privacy-policy` also found no horizontal overflow and kept the visible public header controls in the `36` to `40` CSS pixel range.

The remaining accessibility risk is still evidence depth, not a confirmed code defect. Exact-head `Visual UI Audit` run `27810878263` is now in progress, but there is still no completed current-head hosted visual artifact in hand, no current in-session screen-reader walkthrough, no authenticated mobile route proof for current Admin, Client Admin, Client Finance, Client Member, and Bum flows, and no `axe-core` wiring in the repo.

## Active Recommendations

No open P0, P1, or P2 accessibility recommendation is justified on current head `a17a856` from today's code, hosted workflow, local route evidence, and live tracker refresh.

## Watchlist

- `TB-0029` stays `CLOSED` and remains valid on current head. [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:153) still keeps the shared mobile sidebar named and dismissible, [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts:1) passed `3/3`, and the live tracker row now points at exact head `a17a856` plus runs `27798687806`, `27798687708`, and `27798711531`.
- `TB-0032` stays `CLOSED` and remains valid on current head. [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141) still keeps the enlarged collapsed launcher in source, and the live tracker row now points at exact head `a17a856` plus the same green exact-head functional runs.
- No new route-level issue needs tracker promotion from the recent UI churn in claim review, Bum contact management, mailbox triage, Bum onboarding walkthrough copy, or the responsive Bum landing header.
- Exact-head hosted visual evidence is not complete yet. GitHub `Visual UI Audit` run `27810878263` started on 2026-06-19 UTC for head `a17a856` and should be reviewed once it finishes.

## Current Standards And Time-Sensitive Notes

- WCAG 2.2 remains the current W3C baseline for this backlog. The W3C WCAG overview still points teams to the latest WCAG version and shows its page updated on 2026-05-26. Source: https://www.w3.org/WAI/standards-guidelines/wcag/
- Current WAI-ARIA APG dialog guidance still expects focus to move into a dialog when it opens, return to the invoking control when it closes, and include a visible close button in the tab sequence. That remains the relevant standard for the shared mobile sidebar sheet and the portal dialogs touched by recent UI work. Source: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
- WCAG 2.2 target-size minimum guidance still centers on a `24 by 24 CSS pixel` minimum unless an exception applies. The current `ConsentManager.tsx` source keeps the collapsed `Privacy choices` launcher above that height floor through `min-h-9`, so this standard does not support reopening `TB-0032` on current head. Source: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html

## Access Requests And Evidence Gaps

- Fresh screen-reader notes, authenticated mobile route captures, and route-level assistive-technology validation are still missing for current Admin, Client Admin, Client Finance, Client Member, and Bum flows.
- Exact-head hosted visual evidence is still incomplete while `Visual UI Audit` run `27810878263` remains in progress, so current-head visual and mobile artifact proof is weaker than the exact-head functional `QA` and `E2E Smoke` lanes.
- The repo still lacks `@axe-core/playwright` or `axe-core`, so this run could not add automated accessibility-scan findings even though route-level Playwright was callable.
- Consultant shells still need a deterministic `.env.qa` bootstrap pattern before broader authenticated QA commands. This run used `set -a; source .env.qa; set +a` successfully for focused local validation, but that flow is still not captured as a durable specialist-friendly bootstrap path.
- When runner-side external DNS context is needed, `https://rcdl.tplinkdns.com` still fails certificate validation from this runner and `http://rcdl.tplinkdns.com` still returns `403`. Keep that separate from primary hosted accessibility evidence on `https://trustedbums.com`.
- Live tracker reads and writes were callable again in this session on project `vaoqvtxqvbptyxddpoju`, so existing accessibility `TB-` rows stayed aligned to live tracker state. The broader tracker-write path is still historically session-variable and remains worth watching in [docs/consultant-access-needs.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/consultant-access-needs.md).

Material missing access, credentials, browser or AT validation, screenshots, axe output, or other evidence needed for a stronger accessibility review. Mirror durable requests in `docs/consultant-access-needs.md`.

## Agent Inputs

- Date of run: 2026-06-19
- Files, tests, and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-accessibility-specialist.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/accessibility-backlog.md`, `docs/consultant-access-needs.md`, `docs/codex-edit-log.md`, `package.json`, [App.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/App.tsx:1), [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:1), [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:1), [ClientOpportunityNew.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientOpportunityNew.tsx:1), [BumContacts.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx:1), [BumOpportunityDetail.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunityDetail.tsx:1), [AdminInbox.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminInbox.tsx:1), [FirstLoginWalkthrough.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/FirstLoginWalkthrough.tsx:1), [BumLanding.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/BumLanding.tsx:1), [PrivacyPolicy.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/PrivacyPolicy.tsx:1), [accessibilityReadAloud.test.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/accessibilityReadAloud.test.tsx:1), [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts:1), [firstLoginWalkthrough.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/firstLoginWalkthrough.test.ts:1), [claimDeclineWorkflow.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/claimDeclineWorkflow.test.ts:1), [bumContactsMutationContract.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/bumContactsMutationContract.test.ts:1), [uiVisualCleanup.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/uiVisualCleanup.test.ts:1), and [visual-ui-audit.spec.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/tests/e2e/visual-ui-audit.spec.ts:1).
- Checks and commands reviewed: `git rev-parse --short HEAD`; `git status --short`; `git log --oneline --decorate -8`; `git diff --stat 57231bf..a17a856 -- src docs tests supabase package.json`; targeted `git diff`; targeted `rg`, `sed`, and `nl`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 20 --json databaseId,workflowName,status,conclusion,headSha,headBranch,createdAt,updatedAt,url`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --workflow 'Visual UI Audit' --limit 8 --json databaseId,workflowName,status,conclusion,headSha,headBranch,createdAt,updatedAt,url`; `corepack pnpm exec vitest run src/test/mobileSidebarAccessibility.test.ts src/test/accessibilityReadAloud.test.tsx src/test/firstLoginWalkthrough.test.ts src/test/claimDeclineWorkflow.test.ts src/test/bumContactsMutationContract.test.ts src/test/uiVisualCleanup.test.ts`; `corepack pnpm run build:dev`; `corepack pnpm preview -- --host 127.0.0.1 --port 8080`; `node --input-type=module` Playwright checks against `http://127.0.0.1:8080/`, `http://127.0.0.1:8080/bums`, and `http://127.0.0.1:8080/privacy-policy`; `curl -I -L --max-time 20 https://trustedbums.com`; `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`; `curl -k -I -L --max-time 20 https://rcdl.tplinkdns.com`; `curl -I -L --max-time 20 http://rcdl.tplinkdns.com`; `curl -fsSL https://supabase.com/changelog.md`; Supabase project listing and project URL confirmation for `vaoqvtxqvbptyxddpoju`; live SQL schema read for `public.admin_scrum_items`; live SQL tracker reads for `TB-0029`, `TB-0032`, and `TB-0092`; and live SQL tracker refresh for `TB-0029` plus `TB-0032`.
- Internet sources reviewed: https://www.w3.org/WAI/standards-guidelines/wcag/, https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/, https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html, and https://supabase.com/changelog.md
- Checks that could not run and why: no fresh multi-route authenticated mobile or screen-reader validation because no assistive-technology session notes or live AT access were available; no completed exact-head hosted `Visual UI Audit` artifact existed yet because run `27810878263` was still in progress during this refresh; no axe scans could run because the repo has no `@axe-core/playwright` or `axe-core`; and no current Client Member route proof was available in-session.
