# Trusted Bums Accessibility Backlog

_Last updated: 2026-06-18 by Codex daily accessibility specialist automation._

## Executive Read

Current `main` head [`57231bf75e9900c11aea964ec9999517a831d1ca`](https://github.com/pidpoddev/trustedbums/commit/57231bf75e9900c11aea964ec9999517a831d1ca) does not warrant a new open accessibility defect from the evidence available in this run. Exact-head hosted functional proof is clean on 2026-06-17 UTC: GitHub `QA` run `27710960865`, DreamHost deploy `27710961582`, and `E2E Smoke` `27711014094` all completed `success`, and a fresh runner check on 2026-06-18 still returns `HTTP/2 200` from `https://trustedbums.com`.

The earlier `TB-0032` backlog item is no longer active. [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141) now renders the collapsed `Privacy choices` launcher with `min-h-9`, `px-3`, and `py-2`, and a fresh local Playwright recheck against `http://127.0.0.1:8080/` on a Pixel 7-sized viewport measured the dismissed launcher at approximately `116.75 by 36 CSS pixels`, which clears the WCAG 2.2 target-size minimum that previously failed.

The mobile sidebar fix also still holds on current head. [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:153) still renders a visible `Portal navigation` title plus a visible close control, and [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts:1) passed again in this run. Source review of newer changed surfaces, including [PortalGlobalSearch.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/PortalGlobalSearch.tsx:1), [ClientDashboard.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientDashboard.tsx:1), [LegalDocumentPage.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/LegalDocumentPage.tsx:1), [BumContacts.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx:1), [BumOpportunityDetail.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunityDetail.tsx:1), and [ClientClaims.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientClaims.tsx:1), did not reveal a new unlabeled control, missing heading, hidden close action, or icon-only regression that should be promoted today.

The remaining accessibility risk is evidence depth, not a confirmed code defect: there is still no exact-head `Visual UI Audit` run for `57231bf`, no current in-session screen-reader or authenticated mobile walkthrough, no current Client Member accessibility proof, no axe wiring in the repo, and the fallback DNS target `https://rcdl.tplinkdns.com` still fails TLS verification from this runner.

## Active Recommendations

No open P0, P1, or P2 accessibility recommendation is justified on current head `57231bf` from today's code, hosted workflow, and local route evidence.

## Watchlist

- `TB-0032` stays `CLOSED` and remains valid on current head. The current launcher sizing in [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141) measured `116.75 by 36 CSS pixels` on 2026-06-18 local Playwright, and current-head hosted `QA` `27710960865`, deploy `27710961582`, and `E2E Smoke` `27711014094` are all green.
- `TB-0029` stays `CLOSED` and remains valid on current head. [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:153) still provides the visible title and close control, and [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts:1) passed `3/3` again in this run. The remaining gap is broader AT and mobile artifact depth, not a reopened source regression.
- No new exact-head route-level issue needs tracker promotion from the recent UI churn in search, legal navigation, client dashboard recovery copy, Bum contact management, or claim preview surfaces.
- The latest hosted `Visual UI Audit` evidence is still older than current head. The most recent successful visual run is `27671724557` on `af944fe27b0ed851ce2b85dae99304a5b0c3a0bd`, so current-head hosted visual and mobile artifact proof is still a watch item.

## Current Standards And Time-Sensitive Notes

- WCAG 2.2 remains the current W3C baseline for this backlog. The W3C WCAG overview still points teams to the latest WCAG version and shows its page updated on 2026-05-26. Source: https://www.w3.org/WAI/standards-guidelines/wcag/
- Current WAI-ARIA APG dialog guidance still expects a dialog to expose an accessible name, keep a close control in the tab sequence, contain tab focus while open, and return focus to the invoking control when it closes. That remains the relevant standard for the shared mobile sidebar sheet. Source: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
- WCAG 2.2 target-size minimum guidance still centers on a `24 by 24 CSS pixel` minimum unless an exception applies. The current `Privacy choices` launcher measurement exceeds that threshold, so this standard no longer supports reopening `TB-0032` on current head. Source: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html

## Access Requests And Evidence Gaps

- Fresh screen-reader notes, authenticated mobile route captures, and route-level assistive-technology validation are still missing for current Admin, Client Admin, Client Finance, Client Member, and Bum flows.
- There is still no exact-head hosted `Visual UI Audit` run for `57231bf`, so current-head hosted visual and mobile artifact proof remains weaker than the current functional `QA` and `E2E Smoke` lanes.
- Consultant shells still need the deterministic `.env.qa` bootstrap pattern before broader authenticated QA commands. This run used `set -a; source .env.qa; set +a` successfully for focused local validation, but that flow is still not captured as a durable specialist-friendly bootstrap path.
- The required external DNS target still could not provide page-level evidence in this session because `curl -I -L --max-time 20 https://rcdl.tplinkdns.com` failed with `curl: (60) SSL certificate problem: unable to get local issuer certificate`.
- The repo still lacks `@axe-core/playwright` or `axe-core`, so this run could not add axe-backed automated accessibility-scan findings even though route-level Playwright was callable.
- Live tracker reads and writes were callable again in this session on project `vaoqvtxqvbptyxddpoju`, so existing accessibility `TB-` rows can stay aligned to live tracker state. The broader tracker-write path is still historically session-variable and remains worth watching in `docs/consultant-access-needs.md`.

Material missing access, credentials, browser or AT validation, screenshots, axe output, or other evidence needed for a stronger accessibility review. Mirror durable requests in `docs/consultant-access-needs.md`.

## Agent Inputs

- Date of run: 2026-06-18
- Files, tests, and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-accessibility-specialist.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/accessibility-backlog.md`, `docs/consultant-access-needs.md`, `docs/codex-edit-log.md`, `package.json`, [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:1), [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:1), [PortalGlobalSearch.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/PortalGlobalSearch.tsx:1), [ClientDashboard.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientDashboard.tsx:1), [LegalDocumentPage.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/LegalDocumentPage.tsx:1), [BumContacts.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx:1), [BumOpportunityDetail.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunityDetail.tsx:1), [ClientClaims.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientClaims.tsx:1), [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts:1), [adminScrumTracker.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/adminScrumTracker.test.ts:1), [e2eSmokeRegression.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/e2eSmokeRegression.test.ts:1), [uiVisualCleanup.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/uiVisualCleanup.test.ts:1), and [clientCommissionPlans.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/clientCommissionPlans.test.ts:1).
- Checks and commands reviewed: `git rev-parse HEAD`; `git status --short`; `git log --oneline --decorate -8`; `git diff --stat HEAD~5..HEAD -- src docs tests`; targeted `git diff`; targeted `rg`, `sed`, and `nl`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 20 --json databaseId,workflowName,status,conclusion,headSha,headBranch,createdAt,updatedAt,url`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --workflow 'Visual UI Audit' --limit 8 --json databaseId,workflowName,status,conclusion,headSha,headBranch,createdAt,updatedAt,url`; `set -a; source .env.qa; set +a; corepack pnpm exec vitest run src/test/mobileSidebarAccessibility.test.ts src/test/adminScrumTracker.test.ts src/test/e2eSmokeRegression.test.ts src/test/uiVisualCleanup.test.ts src/test/clientCommissionPlans.test.ts`; `set -a; source .env.qa; set +a; corepack pnpm run build:dev`; `lsof -nP -iTCP:8080 -sTCP:LISTEN`; `ps -p 73527 -o pid=,command=`; `node --input-type=module` Playwright recheck against `http://127.0.0.1:8080/` and `http://127.0.0.1:8080/legal/privacy-policy`; `curl -I -L --max-time 20 https://trustedbums.com`; `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`; Supabase project listing; live SQL schema read for `public.admin_scrum_items`; and live SQL tracker reads for `TB-0029`, `TB-0032`, and `TB-0092`.
- Internet sources reviewed: https://www.w3.org/WAI/standards-guidelines/wcag/, https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/, and https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html
- Checks that could not run and why: no fresh multi-route authenticated mobile or screen-reader validation because no assistive-technology session notes or live AT access were available; no current-head hosted `Visual UI Audit` artifact existed for `57231bf`; no axe scans could run because the repo has no `@axe-core/playwright` or `axe-core`; and no current Client Member route proof was available in-session.
