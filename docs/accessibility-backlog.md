# Trusted Bums Accessibility Backlog

_Last updated: 2026-06-17 by Codex daily accessibility specialist automation._

## Executive Read

The accessibility queue stays narrow on current `main` head `af944fe`. Exact-head hosted evidence is mixed but still useful: GitHub `QA` run `27653495600` passed on 2026-06-16 UTC, the paired DreamHost workflow `27653495695` reached the live SSH deploy and `https://trustedbums.com` now returns `HTTP/2 200` with `Last-Modified: Tue, 16 Jun 2026 22:54:45 GMT`, but that workflow finished `failure` only after deployment because Bing Webmaster URL submission hit the daily quota (`HTTP 400`, quota `100`). Exact-head `E2E Smoke` run `27653527364` was then skipped, and the next exact-head `Visual UI Audit` run `27671724557` later completed successfully on 2026-06-17 UTC. That improves current hosted visual confidence, but it does not replace the still-missing hosted mobile, assistive-technology, and Client Member closure proof.

One implementation-backed issue still remains active. `TB-0032` is still real in current source and current local route behavior on `af944fe`: [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141) still fixes the collapsed site-wide `Privacy choices` launcher height at `20px` through Tailwind `h-5`, and a fresh local Playwright recheck on `http://127.0.0.1:8080/` measured the dismissed launcher at approximately `53.58 by 20 CSS pixels`, confirming the control still misses the WCAG 2.2 target-size minimum on height. `TB-0029` remains source-fixed and current source still shows the visible `Portal navigation` title, visible close control, and explicit hamburger state for the shared mobile portal drawer, but hosted mobile and assistive-technology proof are still missing before closure.

Current source additions since the last accessibility sweep did not create a new backlog item. The recent Bum/contact/claim wave added a labeled `I can sponsor a call with this customer` select to [BumOpportunities.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunities.tsx:1295) and [BumOpportunityDetail.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunityDetail.tsx:773), a visible delete action for removable rows in [BumContacts.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx:307), and a readable client-message preview block in [ClientClaims.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientClaims.tsx:241). Focused current-head regression coverage stayed green, and this source review did not reveal a new unlabeled control, titleless dialog, or inaccessible icon-only action in those newer surfaces.

The durable evidence gaps are still open: no fresh screen-reader notes, no axe dependency, no fresh Client Member portal proof, no fresh multi-role mobile route capture, and no usable external-DNS route proof while `https://rcdl.tplinkdns.com` still fails TLS verification from this runner. Public-route local Playwright remains callable on port `8080`, but authenticated and assistive-technology depth is still missing.

## Active Recommendations

### P2 - [TB-0032] Enlarge the collapsed `Privacy choices` launcher to meet WCAG 2.2 target-size expectations
- Evidence: After the banner closes, [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141) still renders the persistent launcher as a fixed `Button` with `h-5`, `px-2`, and `text-[10px]`. `h-5` fixes the control height at `20px`, so the target cannot contain the `24 by 24 CSS pixel` square WCAG 2.2 uses for the minimum-size requirement. A fresh local Playwright measurement on `http://127.0.0.1:8080/` after dismissing the banner returned an approximate bounding box of `53.58 by 20 CSS pixels`, which confirms the active failure is the launcher height rather than a stale source-only inference. The collapsed launcher is a site-wide legal control, not inline text and not a user-agent-sized control.
- Why it matters: The privacy launcher is the durable entry point for changing consent choices. Small fixed targets are harder to activate for touch and motor-impaired users, especially on mobile public routes.
- Recommendation: Increase the collapsed launcher hit area to at least `24 by 24 CSS pixels`, or replace it with an equally discoverable same-page control that already meets the minimum target-size expectation.
- Acceptance criteria: The collapsed `Privacy choices` control exposes at least a `24 by 24 CSS pixel` hit area on public and portal pages, remains visually discoverable after the banner closes, and does not require a separate page or reset flow to reach a compliant control.

## Watchlist

- `TB-0029` remains source-fixed in current `main`. [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:1) still imports `SheetHeader` and `SheetTitle`, renders the visible `Portal navigation` title in the mobile drawer, keeps the close button visible and touchable, and uses a hamburger `Menu` trigger with `aria-expanded` plus `Toggle portal navigation` labeling. [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts:1) still covers the shared component and the focused 2026-06-17 Vitest rerun stayed green, confirming Admin, Client, and Bum layouts still inherit `SidebarTrigger`. Remaining proof needed before closure: hosted mobile route capture and screen-reader or equivalent assistive-technology validation that the dialog title announces, the close control is reachable, `Escape` closes, and focus returns to the hamburger trigger. Exact-head `Visual UI Audit` `27671724557` is now green, but this run did not inspect retained mobile drawer artifacts or add narrated AT proof, so closure evidence is still incomplete.
- `TB-0030` remains closed in source. [SignupIntentDialog.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/SignupIntentDialog.tsx:1) still has programmatic labels, descriptions, and invalid state for the signup intent group and field errors, and the current exact-head smoke lane did not produce a usable rerun because `E2E Smoke` `27653527364` was skipped after the post-deploy Bing quota failure.
- `TB-0031` remains closed in source. [AdminScrumTracker.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminScrumTracker.tsx:1) still labels search, filters, and create-form controls, and the focused 2026-06-17 Vitest rerun for [adminScrumTracker.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/adminScrumTracker.test.ts:1) stayed green. Exact-head `Visual UI Audit` `27671724557` is now green, so there is no fresh hosted signal pushing this item back open.
- The prior `TB-0092` harness issue remains closed and no longer blocks accessibility screenshot evidence on current head. The current blocker on fresh exact-head hosted accessibility screenshots is workflow completeness, not the older regex false positive.
- No new current-head route-level accessibility defect needs promotion today. Current source review of [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:1), [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:1), [BumContacts.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx:1), [BumOpportunities.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunities.tsx:1), [BumOpportunityDetail.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunityDetail.tsx:1), and [ClientClaims.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientClaims.tsx:1) did not reveal a new unlabeled-control, titleless-dialog, or hidden-close-control regression on `af944fe`.

## Current Standards And Time-Sensitive Notes

- WCAG 2.2 remains the current W3C baseline for this backlog. The current W3C WCAG overview says WCAG 2.2 was published on 2023-10-05, updated on 2024-12-12, and W3C still encourages teams to use the latest WCAG version. The overview page itself was updated on 2026-05-26. Source: https://www.w3.org/WAI/standards-guidelines/wcag/
- Current WAI-ARIA APG modal dialog guidance still says dialogs need either `aria-labelledby` that points to a visible dialog title or an `aria-label`, and it strongly recommends a visible dialog-close button in the tab sequence plus focus return to the invoking control when the dialog closes. That remains directly applicable to the mobile sidebar sheet. Source: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
- Current WCAG 2.2 target-size guidance still says targets should be at least `24 by 24 CSS pixels` unless a defined exception applies, and it recommends meeting the minimum size even when spacing might otherwise excuse an undersized control. That still applies to the collapsed site-wide `Privacy choices` launcher. Source: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html

## Access Requests And Evidence Gaps

- Fresh screen-reader notes, mobile route captures, and route-level assistive-technology validation are still missing for current Admin, Client Admin, Client Finance, Client Member, and Bum flows. This run did get current exact-head hosted `QA` proof on `af944fe`, plus a fresh local public-route Playwright measurement on `127.0.0.1:8080`, but not fresh AT proof.
- Consultant shells still need the same deterministic `.env.qa` bootstrap step before broader authenticated QA commands. This run revalidated public local Playwright and focused local Vitest/build checks without rerunning the broader authenticated suite, and the current exact-head hosted `E2E Smoke` lane was skipped before it could add role coverage.
- There is still no in-session assistive-technology proof for protected routes beyond current source review and prior historical desktop interaction evidence.
- The required external DNS target still could not provide page-level evidence in this session because `curl -I -L --max-time 20 https://rcdl.tplinkdns.com` failed with `curl: (60) SSL certificate problem: unable to get local issuer certificate`.
- The repo still lacks `@axe-core/playwright` or `axe-core`, so this run could not add axe-backed automated accessibility-scan findings even though route-level Playwright was callable.
- Live tracker refresh succeeded again for `TB-0029` and `TB-0032` in this session after callable Supabase SQL access was revalidated on project `vaoqvtxqvbptyxddpoju`. `TB-0092` was rechecked as already `CLOSED` and did not need another rewrite. The broader tracker-write path still remains session-variable across specialist runs.

Material missing access, credentials, browser or AT validation, screenshots, axe output, or other evidence needed for a stronger accessibility review. Mirror durable requests in `docs/consultant-access-needs.md`.

## Agent Inputs

- Date of run: 2026-06-17
- Files, tests, and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-accessibility-specialist.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/accessibility-backlog.md`, `docs/consultant-access-needs.md`, `docs/agents/consultant-access-needs.md`, `docs/codex-edit-log.md`, `package.json`, [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:1), [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:1), [BumContacts.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx:1), [BumOpportunities.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunities.tsx:1), [BumOpportunityDetail.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumOpportunityDetail.tsx:1), [ClientClaims.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientClaims.tsx:1), [AdminOpportunities.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminOpportunities.tsx:1), [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts:1), [adminScrumTracker.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/adminScrumTracker.test.ts:1), [e2eSmokeRegression.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/e2eSmokeRegression.test.ts:1), and [opportunityClaimStakeholders.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/opportunityClaimStakeholders.test.ts:1).
- Checks and commands reviewed: `git rev-parse --short HEAD`; `git status --short`; `git log --oneline --decorate -5`; `git diff --stat c89db03..HEAD -- src tests docs`; targeted `git diff c89db03..HEAD -- ...`; targeted `rg`, `sed`, and `nl`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 30 --json databaseId,workflowName,status,conclusion,headSha,headBranch,createdAt,updatedAt,url`; `/Users/macdaddy/bin/gh-trustedbums run view 27653495600 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27653495695 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27653495695 --log-failed`; `/Users/macdaddy/bin/gh-trustedbums run view 27653527364 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27671724557 --json ...`; `corepack pnpm exec vitest run src/test/mobileSidebarAccessibility.test.ts src/test/adminScrumTracker.test.ts src/test/e2eSmokeRegression.test.ts src/test/opportunityClaimStakeholders.test.ts`; `corepack pnpm run build:dev`; attempted `corepack pnpm run preview --host 127.0.0.1 --port 8080`; `lsof -nP -iTCP:8080 -sTCP:LISTEN`; `ps -p 37519 -o pid=,command=`; `curl -I -L --max-time 20 http://127.0.0.1:8080/`; `node --input-type=module` Playwright measurement against `http://127.0.0.1:8080/`; `curl -I -L --max-time 20 https://trustedbums.com`; `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`; live Supabase `_get_project`; live Supabase `_execute_sql` schema read for `public.admin_scrum_items`; live Supabase `_execute_sql` tracker reads for `TB-0029`, `TB-0032`, and `TB-0092`; and live Supabase `_execute_sql` tracker refreshes for `TB-0029` and `TB-0032`.
- Internet sources reviewed: https://www.w3.org/WAI/standards-guidelines/wcag/, https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/, and https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html
- Checks that could not run and why: no fresh multi-route authenticated mobile or screen-reader validation because no assistive-technology session notes or live AT access were available; no axe scans because the repo has no axe dependency or configured accessibility spec; no exact-head hosted artifact inspection or screenshot review in-session even though `Visual UI Audit` `27671724557` later completed successfully; and no exact-head hosted interaction proof from `E2E Smoke` because run `27653527364` was skipped after the post-deploy Bing quota failure.
