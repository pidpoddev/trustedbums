# Trusted Bums Accessibility Backlog

_Last updated: 2026-06-03 by Codex daily accessibility specialist automation._

## Executive Read

This run revalidated the current source, current W3C guidance, and the current QA/browser access state. Three implementation-ready issues remain grounded in current code and fresh local browser evidence: the mobile portal sidebar still opens as an unlabeled dialog and hides its built-in close button, the public contact form still reports validation only through toasts, and the public sign-up intent dialog still renders visible validation text without programmatic error wiring. Live deployed validation is still blocked because `https://trustedbums.com/` aborted at first navigation in Playwright, and the public smoke suite also hung on the local page `load` event even though sourced `.env.qa`, local Vite, `curl`, and manual Playwright review with `waitUntil: "domcontentloaded"` did render the public shell.

## Active Recommendations

### P1 - Give the mobile sidebar sheet an accessible name and visible dismiss control
- Evidence: [`src/components/ui/sidebar.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/ui/sidebar.tsx:155) renders portal mobile navigation inside Radix `SheetContent`, but hides the built-in close button with `[&>button]:hidden` at line 159. [`src/components/ui/sheet.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/ui/sheet.tsx:60) shows that the underlying sheet does provide a focusable `Close` control, and the mobile sidebar wrapper does not add a `SheetTitle`, `aria-label`, or equivalent accessible name.
- Why it matters: Mobile navigation is behaving like a modal dialog. Current APG guidance expects modal dialogs to have an accessible name and strongly recommends a visible close control in the tab order.
- Recommendation: Keep the in-sheet close button visible and add a stable dialog name such as `Portal navigation`, either through `SheetTitle` or `aria-label`, for the client, admin, and Bum mobile sidebars.
- Acceptance criteria: On mobile widths, opening the sidebar exposes a visible dismiss button, the dialog is announced with a clear name, `Escape` and the dismiss button both close it, and focus returns to the `Toggle Sidebar` trigger.

### P1 - Add field-level errors and verification messaging to the public contact form
- Evidence: [`src/pages/Index.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/pages/Index.tsx:151) rejects invalid name, email, and message values, and line 160 rejects missing Turnstile completion, but both states are surfaced only through destructive toasts. The form controls do not receive `aria-invalid`, inline error copy, or programmatic `aria-describedby` associations.
- Why it matters: Toast-only validation makes users infer which control failed and can disappear before screen-reader, keyboard, or magnification users recover. That is a direct error-identification and recovery risk on the primary public conversion form.
- Recommendation: Add persistent inline error text for each invalid field and the Turnstile block, mark invalid controls with `aria-invalid`, and move focus to the first invalid control or a concise error summary after submit.
- Acceptance criteria: Invalid submission leaves the user on the form, exposes visible error text next to every failing control, wires each message with `aria-describedby`, marks the failing fields with `aria-invalid`, and does not rely on toast timing as the only error signal.

### P2 - Add programmatic error state to the public sign-up intent dialog
- Evidence: [`src/components/SignupIntentDialog.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/SignupIntentDialog.tsx:137) now renders visible inline messages for missing role, invalid email, and missing company name, but the `RadioGroupItem` and `Input` controls still do not receive `aria-invalid` or `aria-describedby`. A fresh local Playwright pass against `http://127.0.0.1:4173/` on 2026-06-03 confirmed that after clicking `Continue`, the dialog shows `Select Client or Bum.` and `Enter a valid email address.` while `#signup-client` and `#signup-email` still expose neither `aria-invalid` nor `aria-describedby`.
- Why it matters: This dialog is part of the public account-creation flow. Visible error text alone is better than toast-only messaging, but screen-reader users still do not get a programmatic link between the failing field and its error state.
- Recommendation: Keep the inline messages, then add stable IDs plus `aria-describedby` and `aria-invalid` to the role, email, and company controls when validation fails. If the role group remains custom, also expose one concise group-level error target that assistive tech can announce reliably.
- Acceptance criteria: After an invalid submit, each failing sign-up control exposes `aria-invalid="true"`, its visible error message is linked with `aria-describedby`, and keyboard or screen-reader users can determine which field failed without relying on surrounding visual context.

## Watchlist

- [`src/components/ConsentManager.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/ConsentManager.tsx:141) still renders a fixed bottom-left `Privacy` launcher after the banner closes. The freshest authenticated overlap evidence remains the 2026-05-31 artifact [`test-results/visual-ui-audit-authentica-7475d-ctive-states-render-cleanly-chromium/chromium-client_admin-client-target-add-form-open.png`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/test-results/visual-ui-audit-authentica-7475d-ctive-states-render-cleanly-chromium/chromium-client_admin-client-target-add-form-open.png). Keep this on watch until fresh portal screenshots can confirm whether the launcher still obscures lower navigation or focus targets.
- [`tests/e2e/portal-interaction-audit.spec.ts`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/tests/e2e/portal-interaction-audit.spec.ts:162) still contains the anchor check that previously treated valid same-page skip links as broken. Do not use that specific assertion as standalone accessibility evidence until it is corrected.

## Current Standards And Time-Sensitive Notes

- WCAG 2.2 remains the current W3C Recommendation. For this repo, `2.4.11 Focus Not Obscured (Minimum)` and `2.4.13 Focus Appearance` remain the most relevant newer AA guidance for persistent overlays and keyboard focus visibility. Sources: [WCAG 2.2 overview](https://www.w3.org/WAI/standards-guidelines/wcag/new-in-22/), [Understanding 2.4.11](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html), [Understanding 2.4.13](https://www.w3.org/WAI/WCAG22/Understanding/focus-appearance.html).
- Current WAI-ARIA APG guidance for modal dialogs still expects an accessible name, focus containment, and strongly recommends a visible close button in the tab sequence. That applies directly to the mobile sidebar sheet implementation. Source: [Dialog (Modal) Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/).
- Current WCAG 2.2 error-identification guidance still expects input errors to be identified in text and works well with `aria-invalid` plus `aria-describedby` for failed fields. Source: [Understanding SC 3.3.1 Error Identification](https://www.w3.org/WAI/WCAG22/Understanding/error-identification.html), [W3C aria-invalid working example](https://www.w3.org/WAI/WCAG22/working-examples/aria-invalid-data-format/).
- W3C’s WCAG 2.2 errata page was last modified on 2026-05-07 and only adds editorial corrections since publication, so no newer normative change alters the recommendations above. Source: [WCAG 2.2 Errata](https://www.w3.org/WAI/WCAG22/errata/).

## Access Requests And Evidence Gaps

- The repo still lacks `@axe-core/playwright` or `axe-core`, so this run could not add automated axe findings even after QA env export was restored.
- Shell state and sourced state still differ. `set -a; source .env.qa; set +a; pnpm run qa:env` restored the expected contract, but that export pattern is still manual and should be scripted or documented so consultants do not mistake an env-bootstrap miss for missing credentials.
- After sourcing `.env.qa`, `pnpm exec playwright test tests/e2e/staging-smoke.spec.ts --project=chromium --grep 'loads public pages|lets anonymous visitors manage privacy choices|validates the signup intent dialog'` still failed on first navigation to `https://trustedbums.com/` with `net::ERR_ABORTED`, so this run could not produce fresh deployed public screenshots, authenticated route captures, keyboard walkthroughs, or focus-order proof.
- Local public review is only partially automated. Sourced `.env.qa` now lets `pnpm dev --host 127.0.0.1 --port 4173` render the public shell, and manual Playwright review with `waitUntil: "domcontentloaded"` produced fresh screenshots, but the existing smoke spec still hangs on the local page `load` event for `http://127.0.0.1:4173/`. That means the current harness still needs either load-blocking request diagnosis or a safer browser wait strategy before it can serve as reliable accessibility evidence.
- No screen-reader notes, narrated keyboard walkthroughs, or AT/browser matrix evidence were available for admin, client, finance, or Bum flows. Keep this access request open until at least high-traffic routes have real AT validation.

Material missing access, credentials, browser/AT validation, screenshots, axe output, or other evidence needed for a stronger accessibility review. Mirror durable requests in [`docs/consultant-access-needs.md`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/docs/consultant-access-needs.md).

## Agent Inputs

- Date of run: 2026-06-03
- Files and routes reviewed: [`docs/consultant-team-rules.md`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/docs/consultant-team-rules.md), [`docs/consultant-access-needs.md`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/docs/consultant-access-needs.md), [`docs/accessibility-backlog.md`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/docs/accessibility-backlog.md), [`src/pages/Index.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/pages/Index.tsx), [`src/components/ConsentManager.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/ConsentManager.tsx), [`src/components/AccessibilityMenu.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/AccessibilityMenu.tsx), [`src/components/SignupIntentDialog.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/SignupIntentDialog.tsx), [`src/components/PortalGlobalSearch.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/PortalGlobalSearch.tsx), [`src/components/ui/dropdown-menu.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/ui/dropdown-menu.tsx), [`src/components/ui/dialog.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/ui/dialog.tsx), [`src/components/ui/sheet.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/ui/sheet.tsx), [`src/components/ui/sidebar.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/components/ui/sidebar.tsx), [`src/layouts/AdminLayout.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/layouts/AdminLayout.tsx), [`src/layouts/ClientLayout.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/layouts/ClientLayout.tsx), [`src/layouts/BumLayout.tsx`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/src/layouts/BumLayout.tsx), [`tests/e2e/staging-smoke.spec.ts`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/tests/e2e/staging-smoke.spec.ts), [`tests/e2e/visual-ui-audit.spec.ts`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/tests/e2e/visual-ui-audit.spec.ts), [`tests/e2e/portal-interaction-audit.spec.ts`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/tests/e2e/portal-interaction-audit.spec.ts), and local screenshot artifacts under [`test-results/`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/test-results) plus fresh local screenshots [`tmp-accessibility-home-mobile.png`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/tmp-accessibility-home-mobile.png), [`tmp-privacy-launcher-mobile.png`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/tmp-privacy-launcher-mobile.png), and [`tmp-privacy-launcher-desktop.png`](/Users/ryan.peterson/Documents/Trusted%20Bums%20LOCAL/trustedbums/tmp-privacy-launcher-desktop.png).
- Checks and commands reviewed: `git status --short`, `git log --oneline -n 15`, targeted `rg`, `sed`, and `nl`, `set -a; source .env.qa; set +a; pnpm run qa:env`, `pnpm run lint`, `pnpm run test`, `set -a; source .env.qa; set +a; pnpm exec playwright test tests/e2e/staging-smoke.spec.ts --project=chromium --grep 'loads public pages|lets anonymous visitors manage privacy choices|validates the signup intent dialog'`, `set -a; source .env.qa; set +a; pnpm dev --host 127.0.0.1 --port 4173`, local `curl` against `http://127.0.0.1:4173/`, and manual Playwright review against `http://127.0.0.1:4173/` with `waitUntil: "domcontentloaded"`.
- Internet sources reviewed: W3C WCAG 2.2 overview and errata, WAI-ARIA APG modal dialog guidance, and W3C understanding guidance for error identification and focus not obscured.
- Checks that could not run and why: no axe scans because the repo has no axe dependency or configured accessibility spec; no authenticated route browser validation because deployed navigation still failed at `https://trustedbums.com/` before route capture; no screen-reader validation because no AT notes or live screen-reader session access were available; existing public Playwright smoke still could not serve as reliable evidence because its `waitUntil: "load"` navigation timed out against both the deployed target and the local Vite server.
