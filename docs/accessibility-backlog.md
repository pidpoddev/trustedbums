# Trusted Bums Accessibility Backlog

_Last updated: 2026-06-12 by Codex daily accessibility specialist automation._

## Executive Read

The accessibility queue still stays narrow on current `main` head `d360570`. Exact-head hosted release proof is current and healthy: GitHub `QA` run `27371736190`, DreamHost deploy run `27371736211`, and hosted `E2E Smoke` run `27371773276` all passed on 2026-06-11 UTC against `https://trustedbums.com`.

The broad protected-route interaction audit also passed on current head. A sourced hosted `portal-interaction-audit` covered Admin, Client Admin, Client Finance, and Bum routes on `https://trustedbums.com` with `4/4` passing on 2026-06-12, including the changed client `Inbox`, `Claims`, and `Opportunities` routes that landed between `349bbe0` and `d360570`.

One implementation-backed issue remains active. `TB-0029` is source-fixed in the current working tree: the shared mobile portal sidebar now renders a visible `Portal navigation` `SheetTitle`, keeps the Radix sheet close button visible, uses a hamburger `Menu` trigger with explicit expanded state, and has focused source coverage in `src/test/mobileSidebarAccessibility.test.ts`. Hosted mobile and assistive-technology proof are still pending before closure. `TB-0032` is still real in current source on `d360570`: the collapsed site-wide `Privacy choices` launcher still fixes its height at `20px` through Tailwind `h-5`, which stays below WCAG 2.2 target-size minimum guidance for a fixed legal control.

The durable evidence gaps are still open: no screen-reader notes, no axe dependency, no multi-role mobile accessibility coverage, no Client Member portal proof in this run, and no usable external-DNS route proof while `https://rcdl.tplinkdns.com` still fails TLS verification from this runner.

## Active Recommendations

### P2 - [TB-0032] Enlarge the collapsed `Privacy choices` launcher to meet WCAG 2.2 target-size expectations
- Evidence: After the banner closes, `src/components/ConsentManager.tsx` still renders the persistent launcher as a fixed `Button` with `h-5`, `px-2`, and `text-[10px]` at [lines 141-156](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:141). `h-5` fixes the control height at `20px`, so the target cannot contain the `24 by 24 CSS pixel` square WCAG 2.2 uses for the minimum-size requirement. The collapsed launcher is a site-wide legal control, not inline text and not a user-agent-sized control.
- Why it matters: The privacy launcher is the durable entry point for changing consent choices. Small fixed targets are harder to activate for touch and motor-impaired users, especially on mobile public routes.
- Recommendation: Increase the collapsed launcher hit area to at least `24 by 24 CSS pixels`, or replace it with an equally discoverable same-page control that already meets the minimum target-size expectation.
- Acceptance criteria: The collapsed `Privacy choices` control exposes at least a `24 by 24 CSS pixel` hit area on public and portal pages, remains visually discoverable after the banner closes, and does not require a separate page or reset flow to reach a compliant control.

## Watchlist

- `TB-0029` is source-fixed in the current working tree. [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx) now imports `SheetHeader`/`SheetTitle`, renders the visible `Portal navigation` title in the mobile drawer, no longer hides the sheet close button, styles the close control as visible/touchable, and uses a hamburger `Menu` trigger with `aria-expanded` and `Toggle portal navigation` labeling. [mobileSidebarAccessibility.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/mobileSidebarAccessibility.test.ts) covers the shared component and confirms Admin, Client, and Bum layouts all inherit `SidebarTrigger`. Remaining proof needed before closure: hosted mobile route capture and screen-reader or equivalent assistive-technology validation that the dialog title announces, the close control is reachable, `Escape` closes, and focus returns to the hamburger trigger.
- `TB-0030` remains closed in source. [SignupIntentDialog.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/SignupIntentDialog.tsx) still has programmatic labels, descriptions, and invalid state for the signup intent group and field errors, and current exact-head hosted `E2E Smoke` run `27371773276` stayed green.
- `TB-0031` remains closed in source. [AdminScrumTracker.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminScrumTracker.tsx) still labels search, filters, and create-form controls, [adminScrumTracker.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/adminScrumTracker.test.ts) still covers the route, and current hosted portal interaction coverage did not surface a new admin form-label regression.
- `TB-0092` is still a QA harness issue, not a newly promoted accessibility defect. Exact-head standard `Visual UI Audit` run `27395701277` failed on `/admin/scrum`, but the failed log shows the route rendered and the harness tripped only because `tests/e2e/visual-ui-audit.spec.ts` still treats literal body text containing `404` as an error page.
- No new current-head route-level accessibility defect needs promotion today. Hosted desktop `portal-interaction-audit` passed Admin, Client Admin, Client Finance, and Bum route coverage on `d360570`, and current source review of [ClientLayout.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/layouts/ClientLayout.tsx), [ClientDashboard.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientDashboard.tsx), [ClientClaims.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientClaims.tsx), and [ClientOpportunityNew.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/client/ClientOpportunityNew.tsx) did not reveal a new unlabeled-control or titleless-dialog regression on the changed client workflows.

## Current Standards And Time-Sensitive Notes

- WCAG 2.2 remains the current W3C baseline for this backlog. The current W3C WCAG overview, updated 2026-05-26, still points teams to WCAG 2.2 resources and says W3C encourages teams to use the latest WCAG version. Source: https://www.w3.org/WAI/standards-guidelines/wcag/
- Current WAI-ARIA APG modal dialog guidance still says dialogs need either `aria-labelledby` that points to a visible dialog title or an `aria-label`, and it still strongly recommends a visible dialog-close button in the tab sequence. That remains directly applicable to the mobile sidebar sheet. Source: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
- Current WCAG 2.2 target-size guidance still says pointer targets should be at least `24 by 24 CSS pixels` unless a defined exception applies. That still applies to the collapsed site-wide `Privacy choices` launcher. Source: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html

## Access Requests And Evidence Gaps

- Fresh screen-reader notes, mobile route captures, and route-level assistive-technology validation are still missing for current Admin, Client Admin, Client Finance, Client Member, and Bum flows. This run did get current hosted desktop route coverage on exact head `d360570`, but not the full AT proof the backlog still needs.
- Consultant shells still need the same deterministic `.env.qa` bootstrap step. Raw `QA_EXTENSION_API_EXPECTATION=skip corepack pnpm run qa:env` still failed in this run until `.env.qa` was sourced, while sourced `qa:env` exited cleanly and sourced `qa:target-preflight` passed against `https://trustedbums.com`.
- The current exact-head hosted evidence is strong on `d360570`, but there is still no in-session assistive-technology proof for protected routes beyond the desktop interaction audit.
- The required external DNS target still could not provide page-level evidence in this session because `curl -I -L --max-time 20 https://rcdl.tplinkdns.com` failed with `curl: (60) SSL certificate problem: unable to get local issuer certificate`.
- The repo still lacks `@axe-core/playwright` or `axe-core`, so this run could not add automated accessibility-scan findings.
- `TB-0029` has source-fixed evidence and live tracker status updated to `FIXED` from this implementation run. `TB-0032` remains open and mapped by its existing tracker ID.

Material missing access, credentials, browser or AT validation, screenshots, axe output, or other evidence needed for a stronger accessibility review. Mirror durable requests in `docs/consultant-access-needs.md`.

## Agent Inputs

- Date of run: 2026-06-12
- Files, tests, and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-accessibility-specialist.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/accessibility-backlog.md`, `docs/consultant-access-needs.md`, `docs/codex-edit-log.md`, `package.json`, `src/components/ui/sidebar.tsx`, `src/components/ConsentManager.tsx`, `src/layouts/ClientLayout.tsx`, `src/components/PortalGlobalSearch.tsx`, `src/components/FirstLoginWalkthrough.tsx`, `src/pages/client/ClientDashboard.tsx`, `src/pages/client/ClientClaims.tsx`, `src/pages/client/ClientOpportunityNew.tsx`, `src/pages/admin/AdminScrumTracker.tsx`, `tests/e2e/portal-interaction-audit.spec.ts`, and `tests/e2e/visual-ui-audit.spec.ts`.
- Checks and commands reviewed: `git status --short --branch`; `git rev-parse HEAD`; `git log --oneline --decorate -n 12`; `git diff --stat 349bbe0..d360570 -- src tests docs`; targeted `git diff 349bbe0..d360570 -- ...`; targeted `rg`, `sed`, and `nl`; `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 20 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27395701277 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27395701277 --log-failed`; raw `QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run qa:env`; sourced `QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run qa:env`; sourced `QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run qa:target-preflight`; sourced `QA_EXTENSION_API_EXPECTATION=skip /Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm exec playwright test tests/e2e/portal-interaction-audit.spec.ts --project=chromium`; and `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`.
- Internet sources reviewed: https://www.w3.org/WAI/standards-guidelines/wcag/, https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/, and https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html
- Checks that could not run and why: no fresh multi-route mobile or screen-reader validation because no assistive-technology session notes or live AT access were available; no axe scans because the repo has no axe dependency or configured accessibility spec; no Client Member browser audit because this run did not have a matching protected-route interaction pass for that role; no route-level external-DNS validation because `https://rcdl.tplinkdns.com` still fails TLS verification from this runner.
