# Trusted Bums Consultant Access Needs

_Last updated: 2026-06-20 by Codex._

## Executive Read

This file tracks access, credentials, dashboards, logs, fixtures, and third-party systems that would materially improve consultant output. Missing access belongs here, not buried inside product recommendations.

Current-state postscript: exact head `e231cc0` now has clean hosted functional proof on the primary host. GitHub `QA` run `27857690007`, DreamHost workflow `27857689995`, exact-head `Visual UI Audit` run `27857691601`, and hosted `E2E Smoke` run `27857708006` all completed `success` on 2026-06-20 UTC. The current artifact narrows the remaining UI issue rather than closing it: source diff from `a17a856` to `e231cc0` shows client and Bum shells now hide the floating chat launcher and admin now reserves more bottom padding, but the collapsed `Privacy choices` chip still overlaps live authenticated-mobile content on admin dashboard, admin scrum, admin handoffs, client opportunity registration, Bum dashboard, and Bum contacts, so `TB-0060` had to be reopened to current exact-head truth. When runner-side external DNS context is needed, use `https://rcdl.tplinkdns.com`; it still fails TLS verification from this runner, and `http://rcdl.tplinkdns.com` still returns `403`, so keep that separate from primary-host release, UX, or accessibility evidence. The active consultant access gaps are dashboard, fixture, mailbox, control-plane depth, exact-head hosted visual artifact coverage, and local owner-report env access rather than a fresh public outage.

All agents need write/read access to the Admin Tools Scrum Tracker through `/admin/scrum`, Supabase MCP, or an approved equivalent path. If an agent cannot create or update `public.admin_scrum_items`, cannot set `added_by_agent`, or cannot get the generated `TB-` number back for its handoff, the agent must record that as an access blocker here and keep the affected recommendation explicitly untracked until access is restored.

Current tracker-write blocker note: tracker-write access is still session-variable. The 2026-06-10 Performance Engineer run could read current code, workflow evidence, and partial Supabase live state, but it did not have a callable tracker-write surface for `public.admin_scrum_items`. Some 2026-06-11 specialist shells still saw the same thin shape: callable project metadata and live edge-function inventory, but no SQL or `/admin/scrum` tracker-write path. The 2026-06-11 Performance Engineer shell regained direct SQL and used it to refresh `TB-0047` through `TB-0050`, and the 2026-06-12 Security Engineer shell also regained direct SQL plus tracker-write capability and used it to refresh the current security queue. The 2026-06-13 Marketing Graphics shell could again read project `vaoqvtxqvbptyxddpoju`, inspect `public.admin_scrum_items`, and verify live row `TB-0080`, but two update attempts still returned connector `RATE_LIMITED`. The 2026-06-13 Accessibility Specialist shell hit the same `RATE_LIMITED` condition on repeated `_execute_sql` calls while trying to refresh `TB-0029`, `TB-0032`, and `TB-0092`. The 2026-06-14 Accessibility Specialist shell regained callable SQL, confirmed the project URL at `https://vaoqvtxqvbptyxddpoju.supabase.co`, rechecked `TB-0092` as already `CLOSED`, and refreshed live rows `TB-0029` and `TB-0032` to exact head `7ee97c1`. This 2026-06-14 Trust & Reputation shell also restored read and write on the same project by inspecting the live tracker schema, reading `TB-0024`, and refreshing that row to exact head `7ee97c1`. This 2026-06-14 UX Consultant shell likewise regained direct SQL plus tracker-write capability and refreshed `TB-0064`, `TB-0065`, `TB-0082`, and `TB-0096` to exact head `7ee97c1`. This 2026-06-14 Security Engineer shell then completed advisors, direct SQL, function-source review, edge-function logs, and current-row tracker refreshes for `TB-0023`, `TB-0081`, `TB-0085`, `TB-0087`, and `TB-0091` without the prior same-session rate-limit burst. The 2026-06-15 Security Engineer shell repeated that fuller tool shape, rechecked the same five security rows, and refreshed them again without connector throttling. The 2026-06-15 Accessibility Specialist shell also regained callable SQL, revalidated project `vaoqvtxqvbptyxddpoju`, and refreshed live rows `TB-0029` and `TB-0032` after exact-head hosted, source, and local Playwright evidence review on `7ee97c1`. The 2026-06-17 Accessibility Specialist shell again had direct SQL, revalidated project `vaoqvtxqvbptyxddpoju`, read the current `TB-0029`, `TB-0032`, and `TB-0092` rows, and refreshed `TB-0029` plus `TB-0032` to exact head `af944fe` after current hosted QA, current workflow-log review, and a fresh local `127.0.0.1:8080` target-size measurement. The 2026-06-17 Security Engineer shell also had direct SQL plus tracker-read access, verified the current `TB-0023`, `TB-0081`, `TB-0085`, `TB-0087`, `TB-0089`, `TB-0091`, and `TB-0108` rows, and refreshed the active security queue to exact head `af944fe` without a same-session rate-limit burst. This 2026-06-18 Trust & Reputation shell also had direct SQL plus tracker-read and write access, revalidated project `vaoqvtxqvbptyxddpoju`, read `TB-0024`, and refreshed that row to exact head `57231bf` after current hosted workflow, curl, DNS, and metadata checks. This 2026-06-19 Trust & Reputation shell again had direct SQL plus tracker-read access, revalidated project `vaoqvtxqvbptyxddpoju`, re-read live row `TB-0024`, and confirmed the exact-head `a17a856` external-DNS evidence before mirroring it into the trust backlog and access docs. This 2026-06-20 Content Copyeditor shell also had direct SQL plus tracker-read access, re-read current row `TB-0040`, confirmed the exact-head `e231cc0` wording seam, and then successfully reopened the row on retry. This 2026-06-20 Security Engineer shell reached the same live project, listed current Edge Functions, pulled deployed source for `admin-access-requests` v5, `client-team` v3, `invite-bum` v4, and `send-admin-email` v10, and completed initial SQL checks proving `claim_client_notification_previews` still uses `security_invoker=true`, `bum_inner_circle_companies` keeps RLS enabled, and no exposed public tables with RLS disabled were returned; after that, `_execute_sql` calls returned `RATE_LIMITED`, so same-session tracker refreshes and migration-ledger follow-up could not complete. This 2026-06-20 Performance Engineer shell also regained direct SQL, advisor, and tracker-read access long enough to confirm `TB-0047 CLOSED`, `TB-0048 CLOSED`, and `TB-0049 OPEN` on exact head `e231cc0`, but narrower route re-queries hit connector `RATE_LIMITED` `429` before a new tracker write or deeper comparison could be attempted. Existing backlog items can therefore stay mapped by current `TB-` IDs when a specialist shell can prove the live row, but dependable tracker-write access still remains an active need because that capability is not consistent across runs.

2026-06-20 trust update: exact head `e231cc07ee6959bc8eac9d04ed3b68b80d76f6c4` now has current hosted proof on the primary host. GitHub `QA` `27857690007`, DreamHost deploy `27857689995`, exact-head `Visual UI Audit` `27857691601`, and `E2E Smoke` `27857708006` all completed `success` on 2026-06-20 UTC. The primary public host is healthy, but the currently named external DNS target is not: `curl -I -L https://rcdl.tplinkdns.com` still fails local TLS verification, and forced HTTPS plus plain HTTP both return `HTTP/1.1 403`. Linked CLI SQL confirmed the live `TB-0024` row is already current on this exact head, so this run only needed to mirror the same trust state into docs. Keep `TB-0024` open until the host is repaired or the cross-agent contract stops naming it.

2026-06-20 UX update: exact head `e231cc07ee6959bc8eac9d04ed3b68b80d76f6c4` now has clean hosted proof on the primary host. Hosted `QA` `27857690007`, deploy `27857689995`, exact-head `Visual UI Audit` `27857691601`, and hosted `E2E Smoke` `27857708006` are all green on that head. `TB-0065` is now closed in the live tracker: the current `ConsentManager.tsx` source keeps the compact first-layer treatment, and a fresh hosted Pixel 7 capture of `https://trustedbums.com/?consent=reset` shows `Reject all`, `Accept all`, and `Customize` visible while the hero headline and first `Create Client account` CTA remain in view. `TB-0082` stays closed, and no active UX backlog item remains on current head. `GA4_PROPERTY_ID` and `BING_WEBMASTER_API_KEY` still remain missing after sourcing `.env.qa`, and the runner-side external target `https://rcdl.tplinkdns.com` still fails TLS verification when checked directly.

2026-06-18 tracker/access update: this Security Engineer shell again had project-scoped SQL plus tracker-write access, refreshed `TB-0023` and `TB-0089` to exact head `57231bf`, and created `TB-0111` from the new live `function_search_path_mutable` advisor result on `public.sync_admin_scrum_item_owner_fields`. Tracker writes are therefore currently possible, but the session-to-session reliability problem still remains open.

## Active Access Requests

### P0 - Provide automated cleanup authority for mutating Playwright QA
- Why it matters: QA/Test, Release Verification, Security, and Lead Developer need mutating browser tests to create and remove synthetic records in one automated run. Without reliable cleanup authority, write-path QA can leave `qa-deep-*` or `qa-go-live-*` companies, target accounts, opportunities, and related domain rows behind.
- Needed access: A Supabase `service_role` JWT accepted by the Playwright REST cleanup helper for project `vaoqvtxqvbptyxddpoju`, or an approved scoped cleanup RPC/endpoint that can delete only synthetic `qa-deep-*` and `qa-go-live-*` mutation-QA rows plus related child records.
- Current status: Added by mutation QA on 2026-06-09. The configured `QA_SUPABASE_SERVICE_ROLE_KEY` is an `sb-secret` shape rather than a Supabase `service_role` JWT accepted by the current REST cleanup path. Initial hosted mutation QA created synthetic rows and failed cleanup for a target-company row; Supabase MCP cleanup then deleted 2 `company_domains`, 2 `customer_targets`, 1 `opportunity_registrations`, and 2 `companies`, and verification returned zero remaining synthetic rows. The Playwright harness now skips mutating go-live/deep checks unless cleanup authority is present, so mutation QA is guarded but not fully green.

### P0 - Provide CRM, analytics, approved GTM evidence, and the missing brand strategy source
- Why it matters: B2B Growth Marketer, Content, Marketing Graphics, Trust & Reputation, Product Ops, Data/Analytics, UX/UI, and Lead Developer need real funnel evidence plus the authoritative brand-positioning source before scaling acquisition or hardening public messaging. Without those inputs, growth recommendations stay source-backed, selective, and intentionally conservative, with Client-demand capture prioritized over any broad Bum or paid expansion.
- Needed access: `docs/brand-strategy.md` or its replacement source of truth covering positioning, audience priorities, voice, proof hierarchy, and trust constraints; CRM or pipeline views for Client prospects and Bum candidates including source, owner, stage, qualification status, disqualification reason, and conversion timestamps; website analytics and source tracking; LinkedIn organic analytics; LinkedIn paid account access or exports; email/campaign performance; referral-source tracking; approved sales collateral; founder scripts; outreach examples; objection notes; approved case-study permissions; legal-approved claims boundaries for logos, outcomes, commissions, payouts, and referral disclosures; customer or Bum interview notes; and budget constraints by channel.
- Current status: This remains the core GTM evidence blocker on 2026-06-20. Exact head `e231cc07ee6959bc8eac9d04ed3b68b80d76f6c4` has green hosted proof on the primary host: GitHub `QA` `27857690007`, DreamHost deploy `27857689995`, `Visual UI Audit` `27857691601`, and hosted `E2E Smoke` `27857708006` all completed successfully, and fresh runner checks still show `https://trustedbums.com` returning `HTTP/2 200` with `Last-Modified: Sat, 20 Jun 2026 02:39:03 GMT`. The public acquisition path is not missing fields or obvious product surface area: `src/pages/Index.tsx` stays Client-first, `src/pages/BumLanding.tsx` owns Bum recruiting, the Client form still captures buyer role, target-account count, target-account context, blocker, urgency, referral source, and commercial reason, and admin review still exposes qualification status, owner claim, priority, next action, and follow-up deadline before target creation. The live gap is conversion truth and GTM operating assets. This run re-pulled current GA aggregates through the documented impersonation path and confirmed rising consented activity, including `1,804` public home route views and `661` `form_start` events in the last seven days, but GA `outcomes` still shows only one live business-event row and no public lead-submission event rows. Supabase aggregate reads in the same session show only one `CLIENT` contact submission in the last 30 days, still `NEW` and `NEEDS_REVIEW`, against a marketplace base of `8` client companies, `9` approved client profiles, and `9` approved Bum profiles. The remaining gap is therefore not intake shape. It is GTM evidence and follow-through: this session still had no `docs/brand-strategy.md`, CRM or pipeline view, LinkedIn organic or paid analytics, email-platform evidence, interview archive, approved sales collateral, founder scripts beyond `docs/claim-safe-proof-spine.md`, objection notes, or claims-approval source. The recurring shell can re-pull GA aggregates through manual env exports, but `.env.qa` still omits the GA exports and `BING_WEBMASTER_API_KEY` is still missing after sourcing `.env.qa`, so Bing report access remains the live recurring gap. This shell had initial tracker and aggregate SQL access, but follow-up GTM SQL attempts also hit connector `RATE_LIMITED`, so dependable tracker or SQL capacity is still not guaranteed across specialist runs. Until GTM evidence and the brand source are available, the backlog should continue to prioritize founder-led, referral-led, proof-led, and sales-assisted motions over scaled paid or lifecycle automation.

### P0 - Provide domain reputation, DNS, and webmaster access
- Why it matters: Trust & Reputation, Security, Content, UX, and Lead Developer need to know whether `trustedbums.com` is blocked, distrusted, misconfigured, spoofable, poorly indexed, or weakly credible to web-security systems and buyers.
- Needed access: DreamHost hosting or DNS dashboard (or whichever provider is authoritative now), registrar/DNS records, Google Search Console security/manual-action reports, Bing Webmaster Tools, Google Safe Browsing status where available, Microsoft Defender/SmartScreen reputation feedback, postmaster tools, Microsoft 365 or other email-provider domain-authentication dashboards, post-quarantine DMARC monitoring and rollback authority, Supabase Edge Function deployment provenance for `dmarc-reports`, and any security/reputation scanner dashboards already in use.
- Current status: This remains a dashboard-and-control-plane evidence gap, not a blanket public-routability failure. Exact head `e231cc0` has fresh hosted proof on 2026-06-20 UTC: GitHub `QA` run `27857690007`, DreamHost deploy run `27857689995`, exact-head `Visual UI Audit` run `27857691601`, and hosted `E2E Smoke` run `27857708006` all passed against `https://trustedbums.com`. Fresh 2026-06-20 runner checks still show `HTTP/2 200`, the Bing verification tag in HTML, the Google site-verification TXT record in DNS, route-specific metadata, the old public extension ZIP returning `HTTP/2 403`, and the expected HSTS, CSP, frame, content-type, referrer, and permissions headers. The deployed CSP still includes the consent-gated Google Analytics, Google Tag Manager, and Microsoft Clarity origins, and current exact-head source review shows the Clerk domain is now pinned to `clerk.trustedbums.com`. The DMARC mailbox evidence remains source-backed from the 2026-06-09 trust run rather than freshly re-invoked in this session. DreamHost and authoritative DNS still show `_dmarc.trustedbums.com` at `v=DMARC1; p=quarantine; rua=mailto:bums@trustedbums.com; pct=100;`; the prior rollback record is `v=DMARC1; p=none; rua=mailto:bums@trustedbums.com; pct=100;`. The recurring trust shell still lacks local `BING_WEBMASTER_API_KEY`, `GA4_PROPERTY_ID`, and `GOOGLE_APPLICATION_CREDENTIALS` after sourcing `.env.qa`, and the Supabase MCP path was `RATE_LIMITED` for project URL, inventory, log, and SQL reads, so current Bing, GA, or function-runtime proof remains blocked even though the public host is healthy. The unresolved trust-side access gap is now the external DNS contract itself: `TB-0024` stays open because `rcdl.tplinkdns.com` is still named by current rules but still fails local TLS verification and returns `403` when forced.

### P0 - Stabilize Supabase consultant tooling for project `vaoqvtxqvbptyxddpoju`
- Why it matters: Security, Data/Analytics, Performance, Product Ops, and Lead Developer need consistent live validation for RLS, policies, grants, views, functions, advisors, and safe aggregates.
- Needed access: Supabase MCP or connector capability for read-only SQL, catalog inspection, security advisors, performance advisors, storage inspection, safe aggregate queries, function inventory, logs, and tracker reads or writes in the actual consultant session.
- Current status: This remains session-variable on 2026-06-20, so keep the request open. Recent specialist history still includes full read and write shells, but this Trust & Reputation run hit `RATE_LIMITED` immediately on `_get_project_url`, `_list_edge_functions`, `_get_logs`, and `_execute_sql`. The linked CLI fallback was enough to confirm the live `TB-0024` row on exact head `e231cc0`, but not enough to replace current project-scoped inventory, function-log, or catalog proof. The remaining need is not base project reachability anymore; it is stable, project-scoped live access that does not vary by specialist run: function inventory, logs, read-only SQL, advisors, Auth config visibility by setting or variable name, vault-secret visibility by variable name, query plans, deployment provenance, live allow or deny proof for service-role paths, and dependable tracker read or write capacity.

- 2026-06-18 update: this shell confirmed the live project URL, current security advisors, live Edge Function inventory, deployed `extension-api-v1` source, deployed `admin-access-requests` source, direct SQL for `claim_client_notification_previews` reloptions and grants, direct SQL for helper execute grants, and live tracker writes. The current live security queue is now `TB-0023`, `TB-0089`, and new `TB-0111`, not `TB-0108`. The remaining gap is narrower and more specific than basic project reachability: Auth-setting visibility, vault-secret-name visibility, repeatable live allow/deny proof for the remaining issuer-pinning paths, and dependable tracker read/write capacity across sessions.

- 2026-06-19 Security update: this shell again had strong project-scoped access for project `vaoqvtxqvbptyxddpoju`: project inventory, read-only SQL, live tracker reads and writes, `list_edge_functions`, and deployed function source reads through `get_edge_function`. The remaining gap is no longer basic function visibility or sampled live issuer drift. This closeout redeployed and verified the sampled issuer-pinned functions, including `extension-api-v1` v7, `portal-contacts` v5, `profile-bootstrap` v5, `admin-access-requests` v5, and `bum-extension-download` v3. Auth-setting visibility for leaked-password protection and a seeded post-redeploy allow-or-deny matrix are still missing.

- 2026-06-20 Security update: this shell again had partial but strong project-scoped access for project `vaoqvtxqvbptyxddpoju`: live function inventory, deployed source reads for `admin-access-requests` v5, `client-team` v3, `invite-bum` v4, and `send-admin-email` v10, plus initial SQL checks proving `claim_client_notification_previews` still uses `security_invoker=true`, `bum_inner_circle_companies` still has RLS enabled, and no exposed public tables with RLS disabled were returned. The remaining gap narrows to Auth-setting visibility for leaked-password protection, stable tracker refresh capacity without `RATE_LIMITED`, seeded live allow-or-deny proof for the remaining issuer-pinning paths, and a durable migration or deploy ledger.

### P0 - Define authoritative profile bootstrap and self-editable identity fields
- Why it matters: Security, Product Ops, QA/Test, and Lead Developer need an explicit business rule for who may assign portal role, client company, client access role, and Bum identity before RLS and auth bootstrap can be safely tightened.
- Needed access: Product/security decision or documented business rule covering sign-up intent, company matching, admin-approved role assignment, client workspace creation, and which `profiles` fields are user-editable versus admin-controlled.
- Current status: This gap remains important on 2026-06-08, but the broad hosted bootstrap outage now has fix evidence. `docs/business-access-rules.md` already includes a release-gate rule for profile bootstrap and self-editable identity fields, and later hosted runs `27110216996` and `27110329150` failed across multiple roles with `Unable to bootstrap this profile.` After the `8fa0796` helper fix, local hosted role smoke passed all five roles and GitHub `27110757594` passed smoke plus all three Deep QA shards. Product and security still need to confirm the exact self-editable field list and whether first-time client signup may create a workspace automatically or must wait for Admin approval; consultants need live logs only if future current-head hosted runs repeat the bootstrap failure.

### P1 - Provide accessibility validation evidence for authenticated and public routes
- Why it matters: Accessibility review can confirm label wiring and some semantics from source, but it still cannot validate keyboard order, focus visibility, disclosure behavior, table announcements, dialog trapping, or screen-reader output on real portal pages without live evidence.
- Needed access: Authenticated browser-routable QA sessions for admin, client admin, client finance, client member, and Bum roles; fresh desktop and mobile screenshots for key routes; `@axe-core/playwright` or equivalent automated scan wiring; lightweight screen-reader or keyboard walkthrough notes for high-traffic routes; and a scripted QA env bootstrap that exports `.env.qa` reliably for consultant shells.
- Current status: This gap remains open on 2026-06-20, but the current exact-head story narrowed to one shared mobile overlap instead of a broader accessibility regression. Current `main` head `e231cc07ee6959bc8eac9d04ed3b68b80d76f6c4` has green hosted `QA` `27857690007`, DreamHost deploy `27857689995`, `Visual UI Audit` `27857691601`, and `E2E Smoke` `27857708006`, and `https://trustedbums.com` still returns `HTTP/2 200` from the runner. Exact-head screenshot review confirms that the prior `TB-0029` mobile sidebar naming work and `TB-0032` collapsed `Privacy choices` target-size work still hold, but the authenticated mobile shell still needs coordinated support-zone placement: the lower-right `Privacy choices` control still overlaps live admin content and should continue as cross-ticket `TB-0060` rather than reopening a duplicate accessibility-only bug. Local proof stayed limited but clean where it ran: targeted Vitest coverage passed `29/29`, current source still keeps the shared mobile sidebar named and dismissible plus the larger collapsed privacy launcher in [sidebar.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ui/sidebar.tsx:1) and [ConsentManager.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/ConsentManager.tsx:1), `http://127.0.0.1:8080` already served the current app with `HTTP/1.1 200 OK`, and `corepack pnpm run build:dev` passed. The remaining blockers are still evidence-depth blockers beyond that one active mobile overlap: there is still no fresh screen-reader or assistive-technology walkthrough, no reviewed current-head Client Member route audit, no clean reproducible authenticated local keyboard lane from this shell, no automation-backed accessibility scan in-session, and the repo still has no `@axe-core/playwright` or `axe-core`. Keep the access request open until consultant shells can export QA env vars deterministically, capture authenticated route evidence on demand, add current-head AT proof, and retire `TB-0060` with verified mobile support-zone placement.

### P0 - Provide deterministic Clerk QA auth support plus failure logs
- Why it matters: QA/Test, Accessibility, Security, and Lead Developer currently depend on deployed authenticated browser coverage, but the 2026-05-31 run showed that role smoke no longer provides stable evidence because Clerk-backed sign-in itself is timing out or closing before route assertions can settle.
- Needed access: Confirmed-good QA accounts for Admin, Client Admin, Client Finance, Client Member, and Bum; the approved sign-in method for automation; Clerk dashboard or request-log visibility for failed QA sign-ins; and any relevant edge-function or auth-proxy logs tied to `clerk.trustedbums.com` requests.
- Current status: This gap reopened on 2026-06-08 after looking briefly closed, then narrowed again after the helper fix. Hosted authenticated smoke passed in `E2E Smoke` run `27109958355`, and local target preflight still reaches `https://trustedbums.com` after sourcing `.env.qa`. Later hosted runs `27110216996` and `27110329150` failed authenticated coverage across multiple roles after setup, redirecting users back to `/login` with `Authorization required` and `Unable to bootstrap this profile.` After commit `8fa0796`, local hosted role smoke passed all five roles and GitHub `27110757594` passed smoke plus `Deep QA (admin|client|bum)`. Clerk logs, auth-proxy logs, and profile-bootstrap traces should be treated as conditional blockers only if a future current-head rerun repeats the failure.

### P0 - Provide seeded extension API allow and deny QA coverage
- Why it matters: QA/Test and Security need extension-specific authorization proof instead of stopping at config and preflight success.
- Needed access: `QA_EXTENSION_API_BASE_URL`, `QA_EXTENSION_API_TOKEN` or an equivalent deterministic auth mechanism, plus seeded data for one allowed and one denied authorization case. Also needed: an approved test path that ties extension destinations back to the business rule and current role fixtures.
- Current status: The env-contract portion is in much better shape on 2026-06-08. After sourcing `.env.qa`, local `corepack pnpm run qa:env` and `corepack pnpm run qa:target-preflight` both pass, and hosted `E2E Smoke` run `27178530411` on `9f42bf4` also passed with extension preflight intact. What remains open is the actual security proof: a seeded allow case plus a seeded denied case for extension API destinations and capture flows. Do not reopen the older `27112837432` release blocker unless a future required-extension run fails the same base-env contract again.

### P0 - Apply seeded multi-company authorization fixtures for QA and RLS validation
- Why it matters: QA/Test, Security, Product Ops, and Lead Developer need deterministic allow/deny proof for customer targets, accepted opportunities, extension destinations, Bum represented contacts, and other company-scoped workflows.
- Needed access: Seeded records and credentials spanning at least two client companies, one admin, one client admin, one client finance, one client member, two Bums, one allowed accepted opportunity, one denied accepted opportunity, one own-company customer target, one foreign-company customer target, represented contacts from each source type, profile-bootstrap/self-edit denial fixtures, seeded telemetry rows, same-domain and cross-company client-team approval fixtures, related-domain pending/approval fixtures, public-email manual-review fixtures, disabled-user fixtures, audit-event expectations, and replay-safe extension capture inputs.
- Current status: The repo now includes an opt-in deterministic fixture seed at [`supabase/qa_authorization_seed.sql`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/qa_authorization_seed.sql:1), a usage handoff at [`docs/qa-authorization-fixtures.md`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/qa-authorization-fixtures.md:1), and source-level fixture contract coverage in [`src/test/qaAuthorizationFixtures.test.ts`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/qaAuthorizationFixtures.test.ts:1). Keep this request open until the fixture is applied in a protected QA database, real Clerk QA ids are mapped where browser-authenticated checks are needed, and direct allow/deny tests prove same-company allow, cross-company deny, public-email manual review, related-domain pending behavior, disabled-user denial, and audit event creation.

### P1 - Provide approved product language and legal terminology sources
- Why it matters: Content, UX, Product Ops, Support, and Legal decisions now depend on distinguishing `customer leads`, `Bum intro requests`, `Bum responses`, role names, and legal workspace language with more confidence than code alone can provide.
- Needed access: Sales collateral, onboarding docs or emails, support macros, legal-approved terminology, brand voice guidance, customer-language research, CRM stage naming, recruiting or partner-acquisition copy for future Bums, and any customer-facing SOPs or training content that define these workflows.
- Current status: This remains open on 2026-06-20 for owner-approved language sources, and the content queue is narrower than the older backlog text implied but not fully closed. [Bum dashboard](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumDashboard.tsx), [Bum reports](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumReports.tsx), reverse-opportunity/admin opportunity badges, Client target helper copy, and related validation copy now use `Prospective Client`, but exact head `e231cc0` still leaves residual visible `Prospect` wording in [Bum contacts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumContacts.tsx), [Bum prospects](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/bum/BumProspects.tsx), [Portal search](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/PortalGlobalSearch.tsx), and [Admin Reports](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminReports.tsx). There is still no legal-approved terminology matrix, onboarding source, support-macro source, finance-owner wording source, or product-approved future post-beta naming source, and live tracker row `TB-0040` is now reopened on exact head `e231cc0`.

### P1 - Provide deployment provenance for Supabase functions and schema changes
- Why it matters: Live Supabase inventory still shows deployment drift risk, which weakens trust-boundary review, release auditability, and incident response.
- Needed access: CI/CD deploy history for Supabase functions and database migrations, release notes or change tickets for live-only functions, Chrome extension release/version provenance where extension auth depends on those deploys, environment or CI secrets inventory by variable name, confirmation of which branch or environment should match project `vaoqvtxqvbptyxddpoju`, and a deploy-capable Supabase token or workflow for Edge Function updates.
- Current status: This gap remains concrete on 2026-06-19 as an audit-history and CI-secret proof gap, not as current sampled function drift. The active `email-track` drift is resolved live, `dmarc-reports` is now live at version `4`, and this closeout added a DreamHost deploy provenance gate. What is still missing is the durable audit trail explaining when earlier live/local divergence happened, which branch or workflow last deployed each function, and hosted proof that GitHub has the `SUPABASE_ACCESS_TOKEN` secret for the new provenance step.
- 2026-06-19 closeout update: the live drift described above has been resolved for the current workflow set. Project `vaoqvtxqvbptyxddpoju` now serves `admin-shared-mailbox` version `3`, `invite-bum` version `4`, `client-team` version `3`, `extension-api-v1` version `7`, `portal-contacts` version `5`, `profile-bootstrap` version `5`, `admin-access-requests` version `5`, and `bum-extension-download` version `3`; live SQL shows latest migration `20260619120328 add_identity_review_inner_circle_companies_reverse_handoffs`. The remaining access need is CI-level confirmation that GitHub has `SUPABASE_ACCESS_TOKEN` available for the new deploy provenance step, plus durable deployment history/audit visibility beyond connector reads.

- 2026-06-20 follow-up: direct migration-ledger SQL returned no rows for versions `20260619120000` or `20260620012000` even though live objects from the former migration already exist and exact head `e231cc0` is green on hosted `QA` `27857690007`, DreamHost deploy `27857689995`, `Visual UI Audit` `27857691601`, and `E2E Smoke` `27857708006`. The remaining access need is explicit live migration and deploy ledger proof plus CI confirmation that GitHub still exposes `SUPABASE_ACCESS_TOKEN` for the provenance gate.

### P1 - Provide public-form abuse protection and mail-reputation operations access
- Why it matters: Trust & Reputation, Security, QA/Test, and Lead Developer need to verify that public contact/signup flows cannot be used to flood inboxes, damage sender reputation, or trigger browser/security distrust.
- Needed access: Cloudflare Turnstile or equivalent captcha configuration, edge or CDN rate-limit settings, any WAF or bot-management rules protecting public forms, Microsoft 365 or mail-provider sending dashboards for the notification mailbox, and any alerting on contact-form spikes or delivery failures.
- Current status: This gap remains open on 2026-06-07, but the live app-sec posture is narrower than before. Fresh deployed source shows `submit-contact` still verifies Turnstile, checks origin, rate-limits recent submissions, and writes `contact_submissions` before notifying operations; live `send-website-email` requires trusted internal caller proof instead of acting as a direct anonymous mail sender; and follow-up `email-track` version `2` now rejects off-domain destinations and unknown deliveries before redirecting. The consultant session still needs Turnstile/dashboard evidence, edge or CDN rate-limit/WAF settings, sender-reputation operations visibility, Microsoft Graph/mail tracking policy, alerting evidence, and one safe seeded valid-delivery tracked-click proof to prove the public mail and tracked-link path cannot be abused in production. Ryan relayed on 2026-06-18 that Akshay found the CoreWeave test claim email in junk before replying `approved`; follow-up live reads verified open/click/reply processing and the Bum notification, but not the junk-placement reason. The next trust/mail review needs message trace, delivery classification, or safe recipient-header evidence before classifying this as isolated recipient filtering versus a broader sender-reputation problem.

### P1 - Provide payment provider and finance exception security evidence
- Why it matters: Security, Data/Analytics, Product Ops, QA/Test, and Lead Developer need to verify payment, invoice, payout, commission, refund, credit, and admin exception boundaries against real provider configuration instead of source-only assumptions.
- Needed access: Payment provider dashboard or configuration export, webhook endpoint inventory, webhook signing-secret variable names, payout and refund role settings, finance reconciliation samples, chargeback/dispute examples, sandbox credentials or seeded payment fixtures, and approved admin exception SOPs for credits, payouts, and commission overrides.
- Current status: Added by Security on 2026-06-04. The repo has payment, payout, commission, and client finance surfaces, but this session had no payment provider configuration, webhook logs, seeded payment fixtures, or finance exception evidence. Security review therefore could not validate whether Client Finance, Client Admin, Bum, and Admin payment boundaries match provider-side reality.

### P1 - Provide production or staging observability and dependency alert context
- Why it matters: Security, Performance, QA/Test, and Lead Developer need CI failures, deploy history, runtime errors, Web Vitals, and security alert visibility to prioritize risk instead of relying only on source review.
- Needed access: CI run history, deployment logs, frontend error monitoring, production or staging Web Vitals, Lighthouse or bundle-analyzer artifacts, flaky-test history, repository security alert or Dependabot visibility if available, and a dependable tracker-write path when specialist runs need to refresh existing `TB-` items with new evidence.
- Current status: This gap narrowed again on 2026-06-20, but the missing pieces are still important. Repo-local GitHub access still works through `/Users/macdaddy/bin/gh-trustedbums`, and exact head `e231cc0` now has current hosted `QA` `27857690007`, DreamHost deploy `27857689995`, `Visual UI Audit` `27857691601`, and hosted `E2E Smoke` `27857708006` all green on `https://trustedbums.com`. Local `corepack pnpm run build` passed on 2026-06-20 with Vite `5.4.21`, current chunk output, and one non-blocking `react-hooks/exhaustive-deps` warning in `ClientOpportunityNew.tsx`; `corepack pnpm exec vitest run src/test/highTrafficRouteHydration.test.ts src/test/clientDashboardLayout.test.ts src/test/clientExportsAccess.test.ts src/test/clientClaimsWorkflow.test.ts` passed `9/9`; and `pnpm audit --prod --json` still returned `0` runtime advisories across `225` production dependencies. This Performance Engineer shell also retained live Supabase SQL, performance-advisor, and tracker-read access long enough to confirm `241,884` `performance_metric_events` rows across `73` routes since 2026-05-29, fresh samples through `2026-06-20 02:48:28+00`, and current 7-day field p75 of `LCP 2056 ms`, `INP 40 ms`, `TTFB 90.8 ms`, and `CLS 0.1`, while proving that live tracker rows now show `TB-0047 CLOSED`, `TB-0048 CLOSED`, and `TB-0049 OPEN`. What is still missing is the broader observability layer: authenticated browser traces, query plans or `pg_stat_statements`, frontend error monitoring, a durable Web Vitals dashboard, Lighthouse or bundle-analyzer artifacts, repository security-alert visibility beyond local `pnpm audit`, deployment provenance beyond workflow summaries, flaky-test history beyond recent GitHub runs, and dependable same-session SQL throughput once repeated Supabase reads start returning `RATE_LIMITED`.

### P1 - Provide dependable GitHub Visual QA workflow and artifact access in consultant sessions
- Why it matters: UI, UX, Accessibility, QA/Test, and Lead Developer are expected to use GitHub-hosted `Visual UI Audit` evidence instead of local visual runs. Without direct workflow and artifact access, visual review degrades into stale or manually copied evidence.
- Needed access: Reliable `gh` or equivalent GitHub access that can list `Visual UI Audit` runs, inspect logs, download the `visual-ui-audit` artifact, and trigger `workflow_dispatch` for `.github/workflows/visual-ui-audit.yml` against approved targets when a fresh UI audit is needed.
- Current status: This gap narrowed again on 2026-06-20. Repo-local GitHub access through `/Users/macdaddy/bin/gh-trustedbums` now works for read, download, and dispatch paths. This session listed current runs, inspected exact-head `Visual UI Audit` `27857691601`, and downloaded its retained artifact for route review without needing a fresh dispatch because the current head already had hosted proof. The remaining weakness is no longer workflow control. It is artifact durability and populated-state coverage: GitHub artifact retention is still too short to serve as the only durable visual baseline, and the densest claim/contact routes still need stronger populated-state captures.

### P1 - Provide seeded telemetry samples and role access proof for admin performance monitoring
- Why it matters: QA/Test, Performance, Security, and Product Ops need to verify the new `/admin/performance` route with realistic data and prove that telemetry remains admin-scoped.
- Needed access: Production-safe or seeded `performance_metric_events` aggregate access covering multiple metrics and routes, one admin account, one non-admin account for deny checks, and a route-level aggregate/query-plan path that does not expose raw private telemetry.
- Current status: This gap remains open on 2026-06-20, but the missing layers are narrower now. The current Performance Engineer shell re-ran live SQL aggregates, advisor output, tracker reads, and current table-schema introspection: `performance_metric_events` now has `241,884` stored rows across `73` routes since 2026-05-29, the live schema still stores route data in `page_path`, values in `metric_value`, and timestamps in `created_at`, and current tracker rows now show `TB-0047 CLOSED`, `TB-0048 CLOSED`, and `TB-0049 OPEN`. The same telemetry still identifies the repeated hotspots clearly across both client and Bum routes: `/client/terms 2661 ms`, `/bum/terms 2569 ms`, `/client/dashboard 2420 ms`, `/bum/dashboard 2399 ms`, `/bum/profile 2318 ms`, `/client/opportunities/new 2221 ms`, and `/bum/live-conversations 2220 ms` on 30-day p75 LCP, while 7-day overall field p75 remains within current Core Web Vitals thresholds at `LCP 2056 ms`, `INP 40 ms`, `TTFB 90.8 ms`, and `CLS 0.1`. What is still missing is the role-proof layer rather than raw telemetry access: a fresh authenticated admin walkthrough of `/admin/performance`, a non-admin deny check at the real route or API surface, confirmation that the admin-only summary RPCs stay inaccessible to non-admin shells for the right reason, query-plan or statement-level evidence if the telemetry table keeps growing, and a dependable browser trace or waterfall for the shared client, Bum, and agreement routes that the telemetry identifies as the live hotspots. This shell also hit connector `RATE_LIMITED` `429` responses on narrower follow-up SQL after the initial successful reads, so post-deploy route-slice comparisons still need a steadier SQL lane.

### P2 - Confirm telemetry retention and deployment-provider values
- Why it matters: The app now treats performance telemetry as necessary operational monitoring, stores Web Vitals in Supabase, and gives admins a review tool.
- Needed access: Deployment-provider confirmation for `VITE_PERFORMANCE_BEACON_URL` and `VITE_CLOUDFLARE_WEB_ANALYTICS_TOKEN`, plus retention expectations for `performance_metric_events`.
- Current status: Product classification is decided as necessary operational monitoring, consent/legal copy is aligned, repo env templates include the variable names, Cloudflare Web Analytics setup was completed, the Supabase beacon endpoint is deployed, `/admin/performance` provides admin review, and live Supabase data now confirms ongoing stored beacon rows through `2026-06-20 02:48:28+00`. The remaining questions are narrower now: confirm production deployment-provider values, retention period, and whether any telemetry origins beyond the currently observed `https://trustedbums.com` and small local-dev samples are expected.

### P2 - Verify Google Analytics live collection and agent access path
- Why it matters: B2B Growth, Data/Analytics, UX, UI, Product Ops, Trust & Reputation, Performance, Release Verification, and Lead Developer can now use GA as the primary website analytics source, but only after deployment and consented traffic prove collection is live.
- Needed access: Google Analytics property access for the `Trusted Bums` account/property, the `Trusted Bums Web` stream for `https://trustedbums.com`, date-range-filtered aggregate reports, source/medium and campaign reports, landing-page/event aggregates, and an agreed agent-safe way to cite aggregate GA evidence without exporting raw visitor-level data.
- Current status: GA4 setup remains API-verified under `TB-0066`: web stream `Trusted Bums Web`, stream URL `https://trustedbums.com`, stream ID `15031871944`, measurement ID `G-P6B5EYQMVN`, and property id `540873763`. Current source keeps GA consent-gated, sends manual SPA page views for public and authenticated routes, strips portal route IDs and sensitive query strings before sending `page_path`, and emits a `trustedbums_route_view` event with aggregate-safe parameters: `portal_area`, `route_group`, `auth_gate`, and `is_portal_route`. The repo still includes the browserless GA API path in `docs/google-analytics-api.md` and `scripts/google-analytics-api.mjs`. This 2026-06-20 growth run again successfully re-pulled GA aggregates through the documented keyless impersonation path by exporting `GA4_PROPERTY_ID=540873763`, `GOOGLE_CLOUD_PROJECT=project-d45d35fc-184a-43c2-889`, and `GA4_IMPERSONATE_SERVICE_ACCOUNT=trusted-bums-ga4-agent@project-d45d35fc-184a-43c2-889.iam.gserviceaccount.com`, with `gcloud auth list` confirming the active account as `bums@trustedbums.com`. Agents therefore do have a working aggregate GA access path from this checkout without browser UI automation or service-account keys. The new limitation is clarity, not reachability: seven-day GA now shows strong consented route activity (`1,804` public home route views, `6,636` login route views, and `661` `form_start` events), but the `outcomes` preset still has only one live business-event row, so growth recommendations should stay narrow until the team reconciles `contact_submissions` against consent-gated lead events and documents why public lead completions are absent or undercounted in GA.

### P2 - Verify Bing Webmaster Tools reporting and API-key access path
- Why it matters: Trust & Reputation, B2B Growth, Data/Analytics, Content, Marketing Graphics, UX, UI, Performance, Release Verification, and Lead Developer can use Bing Webmaster Tools for Microsoft search visibility, crawl, sitemap, indexing, SEO/GEO, backlink, keyword, and reputation evidence.
- Needed access: Bing Webmaster Tools access for the `https://trustedbums.com/` site entry, verification status, sitemap submission/status, crawl/indexing reports, SEO/GEO reports, keyword/backlink reports, Microsoft-side security or reputation messages when available, a Bing Webmaster API key stored as `BING_WEBMASTER_API_KEY`, and an agreed agent-safe way to cite aggregate Bing evidence without exporting private data.
- Current status: Bing setup is verified under closed item `TB-0071`: the site entry exists, production serves `<meta name="msvalidate.01" content="F262EC5D731F39A7B9C8912DF741F807" />`, `public/robots.txt` allows Bingbot and points to `https://trustedbums.com/sitemap.xml`, `public/sitemap.xml` is live as XML, Bing Verify succeeded, and the IndexNow key file is live. The current production host is now serving exact head `e231cc0` with hosted `QA` `27857690007`, deploy `27857689995`, `Visual UI Audit` `27857691601`, and `E2E Smoke` `27857708006` all green on 2026-06-20 UTC. The recurring blocker in this growth shell is still local operator access, not the basic production path: on 2026-06-20, `corepack pnpm bing:webmaster traffic` still failed after sourcing `.env.qa` because `BING_WEBMASTER_API_KEY` was missing locally, even though the deploy workflow clearly has a configured Bing secret. Keep monitoring Bing Search Performance, Site Explorer, URL Inspection, and IndexNow Insights before treating low traffic as a crawler defect, but first restore recurring local report access or route those checks through the existing deploy-capable path.

### P1 - Provide product, design, and operations source-of-truth artifacts
- Why it matters: UX, UI, Content, Product Ops, and Data/Analytics need stronger non-code evidence to validate terminology, queue design, workflow ownership, access handoffs, visual intent, funnel friction, and customer trust objections.
- Needed access: Brand and design sources, approved logo usage rules, campaign visual guidelines, campaign calendar, ad-account/channel priorities, audience definitions by channel, ad-account performance exports, examples of winning creative, legal-approved marketing claims, approved screenshot baselines, durable access to retained `Visual UI Audit` artifacts or an equivalent long-lived screenshot library for public plus authenticated role shells, production-safe populated-state screenshots or seeded fixtures for dense admin/client/finance routes, ad-manager preview access or exportable crop proofs, OCR-capable image QA for spelling verification when generated text is unavoidable, funnel or product analytics, session recordings, support tickets or macros, support queue exports with current statuses and SLAs, customer-feedback exports, sales-objection notes, CRM or sales-pipeline exports for reverse-opportunity and target-response follow-through, onboarding materials, finance reconciliation and exception samples, admin audit/log examples, operations SOPs, and narrated role walkthroughs for Admin, Client Admin, Client Finance, Client Member, and Bum accounts.
- Current status: This gap remains open on 2026-06-20. `docs/brand-strategy.md` is still missing, so brand guidance remains inferred from shipped public-site copy, assets, typography, color usage, the claim-safe proof spine, and consultant rules rather than confirmed by an approved design source. Exact-head evidence is stronger again because current head `e231cc0` now has green hosted `QA` `27857690007`, DreamHost deploy `27857689995`, hosted `Visual UI Audit` `27857691601`, and hosted `E2E Smoke` `27857708006`. That exact-head artifact also sharpens the current UI story: client and Bum shells no longer float the chat launcher, but the remaining `Privacy choices` chip still overlaps live authenticated-mobile content strongly enough to reopen `TB-0060`, while `/client/claims`, `/bum/claims`, and parts of `/bum/contacts` still stay too sparse for a stronger density review. Exact-head source diff from `a17a856` leaves the main public marketing surfaces unchanged, so the active 2026-06-09 graphics pack still fits the current live story and still passes rendered inspection plus zero-visible-text source QA. That proof is still implementation-grade rather than design-approved, GitHub artifact retention is still too short to serve as the only durable baseline, and Product Ops still lacks support queue export with SLA buckets, CRM stage history, finance exception samples, admin rescue logs, access-request proof examples, narrated role walkthroughs for Admin, Client Admin, Client Finance, Client Member, and Bum roles, plus the operations SOPs that explain queue ownership and escalation policy. Marketing Graphics keeps `TB-0080` open to track the missing brand and creative-governance inputs specifically: approved logo usage rules, campaign visual guidelines, campaign calendar, channel audience definitions, ad-account performance, winning-creative references, legal-approved claims boundaries, ad-manager crop proof, and OCR-backed spelling QA when text-bearing creative is required.

### P1 - Provide GTM source-of-truth and growth performance evidence
- Why it matters: B2B Growth, Content, Marketing Graphics, UX, Trust & Reputation, Product Ops, Data/Analytics, and Lead Developer need approved positioning, proof boundaries, and channel evidence before scaling Client acquisition or Bum recruiting.
- Needed access: `docs/brand-strategy.md` or equivalent approved brand source, CRM pipeline or tracking sheet with source/owner/stage/disqualification fields, website analytics and source attribution, LinkedIn organic and paid analytics, email performance metrics, founder scripts, approved case-study permissions, proof or claims matrix, objection notes, referral-disclosure guardrails, and channel budget constraints.
- Current status: This gap remains open on 2026-06-14. The repo still has no `docs/brand-strategy.md`, and the current session still had no CRM or pipeline export, live GA aggregate evidence, LinkedIn account evidence, email-platform evidence, founder-script source beyond `docs/claim-safe-proof-spine.md`, approved case-study permissions, claims matrix, objection-note source, or channel budget evidence. Exact-head public proof is stronger on `7ee97c1`, but growth recommendations are still source-backed and operationally useful rather than validated against approved GTM proof and performance data. This shell regained direct tracker-write access and refreshed `TB-0036` through `TB-0039`, but dependable tracker-write access is still variable across specialist sessions.

### P0 - Provide The Org API access for BlackCurrant decision-maker research pilot
- Why it matters: The BlackCurrant supply sprint needs sharper decision-maker and reporting-line hypotheses for customer accounts before Supply can find credible Inner Circle or Second Circle routes. The Org API may provide official org-chart and manager data by company domain, which is directly useful for customer research.
- Needed access: The Org account, API key, approved plan or credit budget, and a decision on whether contact-enrichment fields such as work email or LinkedIn URL may be requested or stored. Default stance: do not store contact-enrichment fields; use name, title, company, section, manager/reporting context, source note, and verification status only.
- Current status: Evaluation requested on 2026-06-17. Recommended pilot is four BlackCurrant accounts first: Crusoe, Vantage Data Centers, QTS/Blackstone, and CoreWeave/Oracle. Use the official API only, respect credits and rate limits, and avoid third-party scrapers.

## Role Access Matrix

### UX Consultant
- Core access: Authenticated role accounts, browser screenshots, Google Analytics funnel and landing-page aggregates when live, session recordings, support or customer feedback.
- If missing: Keep recommendations source-backed and say which user-confidence questions remain open.

### UI Consultant
- Core access: Browser screenshots, design sources, component references, and brand guidance.
- If missing: Keep visual recommendations narrow and route-specific.

### Marketing Graphics Artist
- Core access: Brand guidelines, approved logo/source assets, channel specs, campaign calendar, target audiences, ad-account performance data, examples of winning creative, legal-approved claims, image generation or design tooling, and visual QA/OCR capability.
- If missing: Keep concepts clearly marked as source-backed creative drafts, avoid factual or legal-sensitive claims, and do not mark generated assets campaign-ready without visual and spelling QA. Use `TB-0080` for the missing brand and creative-governance blocker.

### B2B Growth Marketer
- Core access: CRM/pipeline data for Client Prospects and Bum candidates, Google Analytics website/source/funnel aggregates when live, campaign performance, LinkedIn/email/ad data, audience definitions, approved sales collateral, legal-approved claims, case-study permissions, customer/Bum interview notes, objection notes, and channel budget constraints.
- If missing: Keep growth plays source-backed, prioritize low-risk founder/referral motions, and avoid claiming performance confidence or scale readiness.

### Content Copyeditor
- Core access: Product copy, legal-approved terminology, sales collateral, onboarding and support language.
- If missing: Flag naming drift but avoid claiming final terminology decisions.

### Trust And Reputation Consultant
- Core access: DNS/Cloudflare or registrar records, Google Search Console, Bing Webmaster Tools, Safe Browsing or security-issue reports, Microsoft SmartScreen feedback, email provider auth dashboards, DMARC reports, postmaster tools, scanner dashboards, public site crawl, and brand/company proof sources.
- If missing: Run public-source checks, keep findings scanner/source-backed, and mark dashboard-only checks as access gaps.

### Accessibility Specialist
- Core access: Authenticated browser access, screenshots, keyboard and screen-reader notes, automated axe coverage.
- If missing: Keep findings code-backed and identify operability gaps that still need live validation.

### QA/Test Engineer
- Core access: Routable QA target, seeded accounts, role credentials, CI history, flaky-test history, and deterministic auth setup.
- If missing: Treat skipped or unreachable suites as coverage limitations, not passing coverage.

### Security Engineer
- Core access: Supabase read-only SQL and advisors, Clerk configuration visibility, deployed function inventory or logs, seeded role tokens, CI secrets inventory.
- If missing: Do not assume current live policy or grant state from code alone.

### Performance Engineer
- Core access: Production or staging traces, Web Vitals, bundle analysis, Supabase advisor access, query plans, authenticated route access.
- If missing: Distinguish measured production evidence from local build or source inference.

### Data And Analytics Engineer
- Core access: Supabase SQL, Google Analytics aggregate reporting when live, product analytics or event data, export samples, reconciliation examples, and metric owners.
- If missing: Treat metric definitions and data quality as inferred, not confirmed.

### Product Ops Workflow Analyst
- Core access: Support queues, CRM pipeline, admin logs, finance exception examples, operations SOPs, and role walkthroughs.
- If missing: Treat recommendations as product-model findings, not proven operating reality.

### Lead Developer
- Core access: All specialist backlogs, Supabase tooling, QA/browser access, CI and deployment history, dependency audit output, and current external guidance.
- If missing: Prioritize the access request alongside the implementation recommendation it blocks.
