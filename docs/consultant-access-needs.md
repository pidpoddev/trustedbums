# Trusted Bums Consultant Access Needs

_Last updated: 2026-06-12 by Codex._

## Executive Read

This file tracks access, credentials, dashboards, logs, fixtures, and third-party systems that would materially improve consultant output. Missing access belongs here, not buried inside product recommendations.

Current-state postscript: exact head `d360570` is still in a clean hosted proof state on the primary host. GitHub `QA` run `27371736190`, DreamHost deploy run `27371736211`, and hosted `E2E Smoke` run `27371773276` all passed on 2026-06-11 UTC, and fresh 2026-06-12 runner checks still show `https://trustedbums.com` returning `HTTP/2 200` with current route metadata, Bing verification, Google site-verification TXT, and the expected security headers. The exact-head standard `Visual UI Audit` run `27395701277` is currently red, but QA and Release Verification already classify that one as harness item `TB-0092` rather than a product or trust regression, and a fresh exact-head `Complete Visual UI Audit` run `27400338279` is in progress. The active consultant access gaps are therefore still dashboard, fixture, mailbox, and control-plane depth rather than a fresh release outage.

All agents need write/read access to the Admin Tools Scrum Tracker through `/admin/scrum`, Supabase MCP, or an approved equivalent path. If an agent cannot create or update `public.admin_scrum_items`, cannot set `added_by_agent`, or cannot get the generated `TB-` number back for its handoff, the agent must record that as an access blocker here and keep the affected recommendation explicitly untracked until access is restored.

Current tracker-write blocker note: tracker-write access is still session-variable. The 2026-06-10 Performance Engineer run could read current code, workflow evidence, and partial Supabase live state, but it did not have a callable tracker-write surface for `public.admin_scrum_items`. Some 2026-06-11 specialist shells still saw the same thin shape: callable project metadata and live edge-function inventory, but no SQL or `/admin/scrum` tracker-write path. The 2026-06-11 Performance Engineer shell regained direct SQL and used it to refresh `TB-0047` through `TB-0050`, and the 2026-06-12 Security Engineer shell also regained direct SQL plus tracker-write capability and used it to refresh the current security queue. The later 2026-06-12 Marketing Graphics shell then hit Supabase connector `RATE_LIMITED` errors on both `_get_project` and `_execute_sql`, so even a simple `TB-0080` refresh could not run. Existing backlog items can therefore stay mapped by current `TB-` IDs without a same-session live row refresh when the specialist shell lands on the thinner or rate-limited tool shape, but dependable tracker-write access remains an active need.

## Active Access Requests

### P0 - Provide automated cleanup authority for mutating Playwright QA
- Why it matters: QA/Test, Release Verification, Security, and Lead Developer need mutating browser tests to create and remove synthetic records in one automated run. Without reliable cleanup authority, write-path QA can leave `qa-deep-*` or `qa-go-live-*` companies, target accounts, opportunities, and related domain rows behind.
- Needed access: A Supabase `service_role` JWT accepted by the Playwright REST cleanup helper for project `vaoqvtxqvbptyxddpoju`, or an approved scoped cleanup RPC/endpoint that can delete only synthetic `qa-deep-*` and `qa-go-live-*` mutation-QA rows plus related child records.
- Current status: Added by mutation QA on 2026-06-09. The configured `QA_SUPABASE_SERVICE_ROLE_KEY` is an `sb-secret` shape rather than a Supabase `service_role` JWT accepted by the current REST cleanup path. Initial hosted mutation QA created synthetic rows and failed cleanup for a target-company row; Supabase MCP cleanup then deleted 2 `company_domains`, 2 `customer_targets`, 1 `opportunity_registrations`, and 2 `companies`, and verification returned zero remaining synthetic rows. The Playwright harness now skips mutating go-live/deep checks unless cleanup authority is present, so mutation QA is guarded but not fully green.

### P0 - Provide CRM, analytics, approved GTM evidence, and the missing brand strategy source
- Why it matters: B2B Growth Marketer, Content, Marketing Graphics, Trust & Reputation, Product Ops, Data/Analytics, UX/UI, and Lead Developer need real funnel evidence plus the authoritative brand-positioning source before scaling acquisition or hardening public messaging. Without those inputs, growth recommendations stay source-backed, selective, and intentionally conservative, with Client-demand capture prioritized over any broad Bum or paid expansion.
- Needed access: `docs/brand-strategy.md` or its replacement source of truth covering positioning, audience priorities, voice, proof hierarchy, and trust constraints; CRM or pipeline views for Client Prospects and Bum Prospects including source, owner, stage, qualification status, disqualification reason, and conversion timestamps; website analytics and source tracking; LinkedIn organic analytics; LinkedIn paid account access or exports; email/campaign performance; referral-source tracking; approved sales collateral; founder scripts; outreach examples; objection notes; approved case-study permissions; legal-approved claims boundaries for logos, outcomes, commissions, payouts, and referral disclosures; customer/Bum interview notes; and budget constraints by channel.
- Current status: This remains the core GTM evidence blocker on 2026-06-11. The public acquisition surface is no longer shallow in the old way: current head `349bbe0` deployed successfully via DreamHost run `27316712643`, hosted `E2E Smoke` run `27316733435` passed, hosted `Complete Visual UI Audit` run `27325075884` passed, `src/pages/Index.tsx` is Client-first, `src/pages/BumLanding.tsx` owns Bum recruiting, the Client path still captures buyer role, target-account count, target-account context, blocker, urgency, referral source, and commercial reason, and admin review continues to expose qualification status, owner claim, priority, next action, follow-up deadline, and qualification gating before target creation. The Bum side is also deeper now: `349bbe0` moved prospect recommendation into `Clients We Represent`, and `09aa045` added manual Bum contact creation. The remaining gap is no longer intake shape. It is GTM evidence and operating assets: the session still had no `docs/brand-strategy.md`, CRM or pipeline view, GA aggregate access path, LinkedIn organic or paid analytics, email-platform evidence, interview archive, approved sales collateral, founder scripts beyond `docs/claim-safe-proof-spine.md`, objection notes, or claims-approval source. This session also lacked a callable `/admin/scrum` or Supabase SQL tracker-write path, so the existing growth tracker rows could not be refreshed from this shell. The highest-value missing evidence therefore remains source quality and conversion truth: referred-company quality, founder-call conversion, objection patterns, first-target-account activation by source, Bum approved-to-activated quality by source, and the kit needed to turn the current product path into a repeatable referral, LinkedIn, and nurture motion. `https://trustedbums.com` is live with current security headers, but `curl -I -L --max-time 20 https://rcdl.tplinkdns.com` still fails TLS verification with `curl: (60) SSL certificate problem: unable to get local issuer certificate`. Until GTM evidence and the brand source are available, the backlog should continue to prioritize founder-led, referral-led, proof-led, and sales-assisted motions over scaled paid or lifecycle automation.

### P0 - Provide domain reputation, DNS, and webmaster access
- Why it matters: Trust & Reputation, Security, Content, UX, and Lead Developer need to know whether `trustedbums.com` is blocked, distrusted, misconfigured, spoofable, poorly indexed, or weakly credible to web-security systems and buyers.
- Needed access: DreamHost hosting or DNS dashboard (or whichever provider is authoritative now), registrar/DNS records, Google Search Console security/manual-action reports, Bing Webmaster Tools, Google Safe Browsing status where available, Microsoft Defender/SmartScreen reputation feedback, postmaster tools, Microsoft 365 or other email-provider domain-authentication dashboards, post-quarantine DMARC monitoring and rollback authority, Supabase Edge Function deployment provenance for `dmarc-reports`, and any security/reputation scanner dashboards already in use.
- Current status: This remains a dashboard-and-control-plane evidence gap, not a blanket public-routability failure. Exact head `d360570` has fresh hosted proof on 2026-06-11 UTC: GitHub `QA` run `27371736190`, DreamHost deploy run `27371736211`, and hosted `E2E Smoke` run `27371773276` all passed against `https://trustedbums.com`. Fresh 2026-06-12 runner checks still show `HTTP/2 200`, the Bing verification tag in HTML, the Google site-verification TXT record in DNS, route-specific metadata, the old public extension ZIP returning `HTTP/2 403`, and the expected HSTS, CSP, frame, content-type, referrer, and permissions headers. The deployed CSP now includes the consent-gated Google Analytics and Google Tag Manager origins, so the older unpublished-CSP note is no longer current. The DMARC mailbox evidence remains source-backed from the 2026-06-09 trust run rather than freshly re-invoked in this session: `dmarc-reports` version `3` on project `vaoqvtxqvbptyxddpoju` scanned `100` mailbox messages, found `17` likely DMARC reports, parsed all `17`, summarized `44` reported messages, and found `44` aligned passes with `0` full SPF/DKIM alignment failures. DreamHost and authoritative DNS still show `_dmarc.trustedbums.com` at `v=DMARC1; p=quarantine; rua=mailto:bums@trustedbums.com; pct=100;`; the prior rollback record is `v=DMARC1; p=none; rua=mailto:bums@trustedbums.com; pct=100;`. The unresolved external-DNS evidence blocker is still the required fallback target `https://rcdl.tplinkdns.com`: `curl -I -L --max-time 20` still fails TLS verification with `curl: (60) SSL certificate problem: unable to get local issuer certificate`, and open tracker `TB-0024` remains the active repair-or-retire path for that host.

### P0 - Stabilize Supabase consultant tooling for project `vaoqvtxqvbptyxddpoju`
- Why it matters: Security, Data/Analytics, Performance, Product Ops, and Lead Developer need consistent live validation for RLS, policies, grants, views, functions, advisors, and safe aggregates.
- Needed access: Supabase MCP or connector capability for read-only SQL, catalog inspection, security advisors, performance advisors, storage inspection, safe aggregate queries, function inventory, and logs in the actual consultant session.
- Current status: This remains session-variable on 2026-06-12, so keep the request open. The current Data Analytics run confirmed project `vaoqvtxqvbptyxddpoju` is `ACTIVE_HEALTHY`, returned the live project URL, exposed current edge-function inventory plus performance and security advisors, and restored direct SQL for safe aggregate reads and tracker inspection. The 2026-06-12 Security Engineer shell also regained a fuller live scope: project lookup, project URL confirmation, Edge Function inventory, deployed function source, direct SQL against `pg_proc`, `pg_policies`, and `information_schema.columns`, plus tracker reads and writes for `public.admin_scrum_items`. Earlier 2026-06-11 Performance Engineer work had the same fuller shape, while other specialist shells still saw only project metadata and edge-function inventory, and the later 2026-06-12 Marketing Graphics shell hit connector `RATE_LIMITED` errors on `_get_project` and `_execute_sql` before a simple tracker refresh could run. The remaining need is not basic project reachability anymore; it is stable, project-scoped live access that does not vary by specialist run: function inventory, logs, read-only SQL, advisors, Auth config visibility by setting or variable name, vault-secret visibility by variable name, query plans, deployment provenance, and dependable tracker read/write capacity.

### P0 - Define authoritative profile bootstrap and self-editable identity fields
- Why it matters: Security, Product Ops, QA/Test, and Lead Developer need an explicit business rule for who may assign portal role, client company, client access role, and Bum identity before RLS and auth bootstrap can be safely tightened.
- Needed access: Product/security decision or documented business rule covering sign-up intent, company matching, admin-approved role assignment, client workspace creation, and which `profiles` fields are user-editable versus admin-controlled.
- Current status: This gap remains important on 2026-06-08, but the broad hosted bootstrap outage now has fix evidence. `docs/business-access-rules.md` already includes a release-gate rule for profile bootstrap and self-editable identity fields, and later hosted runs `27110216996` and `27110329150` failed across multiple roles with `Unable to bootstrap this profile.` After the `8fa0796` helper fix, local hosted role smoke passed all five roles and GitHub `27110757594` passed smoke plus all three Deep QA shards. Product and security still need to confirm the exact self-editable field list and whether first-time client signup may create a workspace automatically or must wait for Admin approval; consultants need live logs only if future current-head hosted runs repeat the bootstrap failure.

### P1 - Provide accessibility validation evidence for authenticated and public routes
- Why it matters: Accessibility review can confirm label wiring and some semantics from source, but it still cannot validate keyboard order, focus visibility, disclosure behavior, table announcements, dialog trapping, or screen-reader output on real portal pages without live evidence.
- Needed access: Authenticated browser-routable QA sessions for admin, client admin, client finance, client member, and Bum roles; fresh desktop and mobile screenshots for key routes; `@axe-core/playwright` or equivalent automated scan wiring; lightweight screen-reader or keyboard walkthrough notes for high-traffic routes; and a scripted QA env bootstrap that exports `.env.qa` reliably for consultant shells.
- Current status: This gap remains open on 2026-06-12, but the exact-head hosted evidence is strong on current head `d360570`. Hosted `QA` run `27371736190`, DreamHost deploy run `27371736211`, and hosted `E2E Smoke` run `27371773276` all passed on `https://trustedbums.com`. A sourced hosted `portal-interaction-audit` also passed `4/4` on exact head across Admin, Client Admin, Client Finance, and Bum desktop routes, including the changed client `Inbox`, `Claims`, and `Opportunities` flows. Exact-head standard `Visual UI Audit` run `27395701277` is not a new accessibility regression signal: its failed log shows `/admin/scrum` rendered and the harness tripped only because `tests/e2e/visual-ui-audit.spec.ts` still treats body text containing `404` as an error page, tracked separately as `TB-0092`. The remaining blockers are still the durable accessibility evidence gaps: consultant shells need the deterministic `set -a; source .env.qa; set +a` bootstrap before QA commands, this run still had no Client Member audit, no fresh screen-reader or assistive-technology walkthroughs, no multi-role mobile route captures on `d360570`, `https://rcdl.tplinkdns.com` still fails TLS verification from this runner, and the repo still has no `@axe-core/playwright` or `axe-core`. Keep this request open until consultant shells can export QA env vars deterministically, capture fresh mobile and authenticated screenshots, and run at least one automation-backed accessibility scan in-session.

### P0 - Provide deterministic Clerk QA auth support plus failure logs
- Why it matters: QA/Test, Accessibility, Security, and Lead Developer currently depend on deployed authenticated browser coverage, but the 2026-05-31 run showed that role smoke no longer provides stable evidence because Clerk-backed sign-in itself is timing out or closing before route assertions can settle.
- Needed access: Confirmed-good QA accounts for Admin, Client Admin, Client Finance, Client Member, and Bum; the approved sign-in method for automation; Clerk dashboard or request-log visibility for failed QA sign-ins; and any relevant edge-function or auth-proxy logs tied to `clerk.trustedbums.com` requests.
- Current status: This gap reopened on 2026-06-08 after looking briefly closed, then narrowed again after the helper fix. Hosted authenticated smoke passed in `E2E Smoke` run `27109958355`, and local target preflight still reaches `https://trustedbums.com` after sourcing `.env.qa`. Later hosted runs `27110216996` and `27110329150` failed authenticated coverage across multiple roles after setup, redirecting users back to `/login` with `Authorization required` and `Unable to bootstrap this profile.` After commit `8fa0796`, local hosted role smoke passed all five roles and GitHub `27110757594` passed smoke plus `Deep QA (admin|client|bum)`. Clerk logs, auth-proxy logs, and profile-bootstrap traces should be treated as conditional blockers only if a future current-head rerun repeats the failure.

### P0 - Provide seeded extension API allow and deny QA coverage
- Why it matters: QA/Test and Security need extension-specific authorization proof instead of stopping at config and preflight success.
- Needed access: `QA_EXTENSION_API_BASE_URL`, `QA_EXTENSION_API_TOKEN` or an equivalent deterministic auth mechanism, plus seeded data for one allowed and one denied authorization case. Also needed: an approved test path that ties extension destinations back to the business rule and current role fixtures.
- Current status: The env-contract portion is in much better shape on 2026-06-08. After sourcing `.env.qa`, local `corepack pnpm run qa:env` and `corepack pnpm run qa:target-preflight` both pass, and hosted `E2E Smoke` run `27178530411` on `9f42bf4` also passed with extension preflight intact. What remains open is the actual security proof: a seeded allow case plus a seeded denied case for extension API destinations and capture flows. Do not reopen the older `27112837432` release blocker unless a future required-extension run fails the same base-env contract again.

### P0 - Apply seeded multi-company authorization fixtures for QA and RLS validation
- Why it matters: QA/Test, Security, Product Ops, and Lead Developer need deterministic allow/deny proof for customer targets, accepted opportunities, extension destinations, Bum represented contacts, and other company-scoped workflows.
- Needed access: Seeded records and credentials spanning at least two client companies, one admin, one client admin, one client finance, one client member, two Bums, one allowed accepted opportunity, one denied accepted opportunity, one own-company customer target, one foreign-company customer target, represented contacts from each source type, profile-bootstrap/self-edit denial fixtures, seeded telemetry rows, same-domain and cross-company client-team approval fixtures, related-domain pending/approval fixtures, public-email manual-review fixtures, disabled-user fixtures, audit-event expectations, and replay-safe extension capture inputs.
- Current status: The repo now includes an opt-in deterministic fixture seed at [`supabase/qa_authorization_seed.sql`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/qa_authorization_seed.sql:1), a usage handoff at [`docs/qa-authorization-fixtures.md`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/qa-authorization-fixtures.md:1), and source-level fixture contract coverage in [`src/test/qaAuthorizationFixtures.test.ts`](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/qaAuthorizationFixtures.test.ts:1). Keep this request open until the fixture is applied in a protected QA database, real Clerk QA ids are mapped where browser-authenticated checks are needed, and direct allow/deny tests prove same-company allow, cross-company deny, public-email manual review, related-domain pending behavior, disabled-user denial, and audit event creation.

### P1 - Provide approved product language and legal terminology sources
- Why it matters: Content, UX, Product Ops, Support, and Legal decisions now depend on distinguishing `customer leads`, `Bum intro requests`, `Bum responses`, role names, and legal workspace language with more confidence than code alone can provide.
- Needed access: Sales collateral, onboarding docs or emails, support macros, legal-approved terminology, brand voice guidance, customer-language research, CRM stage naming, recruiting or partner-acquisition copy for future Bums, and any customer-facing SOPs or training content that define these workflows.
- Current status: This remains open on 2026-06-12, and current head `d360570` widened the approved-language gap instead of narrowing it. The Bum-side company workflow still alternates between `Client Prospect`, `Prospects`, `Prospected Clients`, `Prospect client submissions`, `Clients We Represent`, and `Target Prospects` across nav, page, walkthrough, dashboard, reports, search, and route-test copy. The unlocked signup chooser, admin intake, and generated notes still use `Bum Prospect` while the public recruiting flow says `Apply as a Bum`. The legal flow still mixes current `Client Agreement` language with legacy `Partner Terms`, and the newer Client Legal and Client IT role flows now extend the singular `Client Agreement` workspace label across dashboard recovery copy, sidebar nav, walkthrough steps, and search even though `/client/agreements` contains the current agreement plus FAQ and agreement records. The new finance and integration surfaces also opened two more unresolved noun seams: the same commercial object now flips between `Commission Plans`, `commission structure`, and `commission schedule`, and the same integration setup flips between `Deal Registration Process`, `Deal Registration Automation`, and `Deal Registration Beta`. This session had live tracker access again and refreshed `TB-0040` through `TB-0043`, so the blocker is still the approved-language source itself, not `/admin/scrum` tooling. There is still no legal-approved terminology matrix, onboarding source, support-macro source, recruiting-approved noun decision, finance-owner terminology source, or product-approved wording source for agreement, recruiting, request-history, commission-plan, and deal-registration labels.

### P1 - Provide deployment provenance for Supabase functions and schema changes
- Why it matters: Live Supabase inventory still shows deployment drift risk, which weakens trust-boundary review, release auditability, and incident response.
- Needed access: CI/CD deploy history for Supabase functions and database migrations, release notes or change tickets for live-only functions, Chrome extension release/version provenance where extension auth depends on those deploys, environment or CI secrets inventory by variable name, confirmation of which branch or environment should match project `vaoqvtxqvbptyxddpoju`, and a deploy-capable Supabase token or workflow for Edge Function updates.
- Current status: This gap remains concrete on 2026-06-09, but `TB-0057` is no longer blocked on `dmarc-reports` deployment. The active `email-track` drift is resolved live, and Codex deployed `dmarc-reports` version 3 with the gzip attachment fix through the Supabase connector. What is still missing is the durable audit trail explaining when earlier live/local divergence happened, which branch or workflow last deployed each function, and a repeatable CI-backed release path for future Edge Function updates instead of relying on ad hoc connector deployment.

### P1 - Provide public-form abuse protection and mail-reputation operations access
- Why it matters: Trust & Reputation, Security, QA/Test, and Lead Developer need to verify that public contact/signup flows cannot be used to flood inboxes, damage sender reputation, or trigger browser/security distrust.
- Needed access: Cloudflare Turnstile or equivalent captcha configuration, edge or CDN rate-limit settings, any WAF or bot-management rules protecting public forms, Microsoft 365 or mail-provider sending dashboards for the notification mailbox, and any alerting on contact-form spikes or delivery failures.
- Current status: This gap remains open on 2026-06-07, but the live app-sec posture is narrower than before. Fresh deployed source shows `submit-contact` still verifies Turnstile, checks origin, rate-limits recent submissions, and writes `contact_submissions` before notifying operations; live `send-website-email` requires trusted internal caller proof instead of acting as a direct anonymous mail sender; and follow-up `email-track` version `2` now rejects off-domain destinations and unknown deliveries before redirecting. The consultant session still needs Turnstile/dashboard evidence, edge or CDN rate-limit/WAF settings, sender-reputation operations visibility, Microsoft Graph/mail tracking policy, alerting evidence, and one safe seeded valid-delivery tracked-click proof to prove the public mail and tracked-link path cannot be abused in production.

### P1 - Provide payment provider and finance exception security evidence
- Why it matters: Security, Data/Analytics, Product Ops, QA/Test, and Lead Developer need to verify payment, invoice, payout, commission, refund, credit, and admin exception boundaries against real provider configuration instead of source-only assumptions.
- Needed access: Payment provider dashboard or configuration export, webhook endpoint inventory, webhook signing-secret variable names, payout and refund role settings, finance reconciliation samples, chargeback/dispute examples, sandbox credentials or seeded payment fixtures, and approved admin exception SOPs for credits, payouts, and commission overrides.
- Current status: Added by Security on 2026-06-04. The repo has payment, payout, commission, and client finance surfaces, but this session had no payment provider configuration, webhook logs, seeded payment fixtures, or finance exception evidence. Security review therefore could not validate whether Client Finance, Client Admin, Bum, and Admin payment boundaries match provider-side reality.

### P1 - Provide production or staging observability and dependency alert context
- Why it matters: Security, Performance, QA/Test, and Lead Developer need CI failures, deploy history, runtime errors, Web Vitals, and security alert visibility to prioritize risk instead of relying only on source review.
- Needed access: CI run history, deployment logs, frontend error monitoring, production or staging Web Vitals, Lighthouse or bundle-analyzer artifacts, flaky-test history, repository security alert or Dependabot visibility if available, and a dependable tracker-write path when specialist runs need to refresh existing `TB-` items with new evidence.
- Current status: This gap narrowed again on 2026-06-12, but the missing pieces are still important. Repo-local GitHub access still works through `/Users/macdaddy/bin/gh-trustedbums`, and exact head `d360570` now has green hosted `QA` run `27371736190`, green DreamHost deploy `27371736211`, and green hosted `E2E Smoke` `27371773276` from 2026-06-11 UTC. The latest standard `Visual UI Audit` run `27395701277` failed on 2026-06-12 UTC, but the failure log shows the admin scrum page hit the audit's generic error-page regex on tracker text rather than exposing a broad host or auth outage. Local `corepack pnpm run build` passed on 2026-06-12 with current chunk output, and the targeted client-route and performance Vitest set passed `15/15`. The current Performance Engineer shell also regained live Supabase SQL and performance-advisor access, which was enough to refresh `performance_metric_events` aggregates to `170,403` total rows across `69` routes since 2026-05-29 and recheck the active performance tracker items. What is still missing is the broader observability layer: authenticated browser traces, query plans or `pg_stat_statements`, frontend error monitoring, a durable Web Vitals dashboard, Lighthouse or bundle-analyzer artifacts, repository security-alert visibility, deployment provenance beyond workflow summaries, flaky-test history beyond recent GitHub runs, and a dependable tracker-write path that does not disappear or rate-limit between specialist sessions.

### P1 - Provide dependable GitHub Visual QA workflow and artifact access in consultant sessions
- Why it matters: UI, UX, Accessibility, QA/Test, and Lead Developer are expected to use GitHub-hosted `Visual UI Audit` evidence instead of local visual runs. Without direct workflow and artifact access, visual review degrades into stale or manually copied evidence.
- Needed access: Reliable `gh` or equivalent GitHub access that can list `Visual UI Audit` runs, inspect logs, download the `visual-ui-audit` artifact, and trigger `workflow_dispatch` for `.github/workflows/visual-ui-audit.yml` against approved targets when a fresh UI audit is needed.
- Current status: This gap narrowed again on 2026-06-11. Repo-local GitHub access through `/Users/macdaddy/bin/gh-trustedbums` now works for both read and dispatch paths: this session listed current runs, downloaded exact-head `Complete Visual UI Audit` artifact `27325075884` on `349bbe0`, and successfully dispatched fresh standard `Visual UI Audit` run `27329844306` for `main` against `https://trustedbums.com`. The remaining weakness is no longer workflow control. It is artifact durability and baseline retention: the standard run was still in progress at handoff time, and GitHub artifact retention is still too short to serve as the only durable visual baseline. Keep `https://trustedbums.com` as the default hosted QA target, and use `https://rcdl.tplinkdns.com` only when DNS or TLS context itself is the thing under review.

### P1 - Provide seeded telemetry samples and role access proof for admin performance monitoring
- Why it matters: QA/Test, Performance, Security, and Product Ops need to verify the new `/admin/performance` route with realistic data and prove that telemetry remains admin-scoped.
- Needed access: Production-safe or seeded `performance_metric_events` aggregate access covering multiple metrics and routes, one admin account, one non-admin account for deny checks, a route-level aggregate/query-plan path that does not expose raw private telemetry, and a callable tracker-write or admin UI path so the performance specialist can refresh `TB-0047` through `TB-0050` after each evidence pass.
- Current status: This gap remains open on 2026-06-12, but the missing layers are narrower now. The current Performance Engineer shell re-ran live SQL aggregates and current advisor output: `performance_metric_events` now has `170,403` stored rows across `69` routes since 2026-05-29 and `152,579` rows across `68` routes in the last 7 days, `/admin/performance` has fresh 30-day route metrics (`LCP p75 1962 ms`, `INP p75 16 ms`, `TTFB p75 99.75 ms`), and the route-summary screen still reads the aggregate helpers in source while staying a `6.38 kB` route chunk in the latest local build. The same telemetry now also identifies the live client-route hotspots more clearly: `/client/dashboard 2440 ms`, `/client/opportunities 2301 ms`, `/client/reports 2184 ms`, `/client/payments 2168 ms`, `/client/targets 2156 ms`, and `/client/exports 2096 ms` on 30-day p75 LCP. What is still missing is the role-proof layer rather than raw telemetry access: a fresh authenticated admin walkthrough of `/admin/performance`, a non-admin deny check at the real route or API surface, query-plan or statement-level evidence if the telemetry table keeps growing, and a dependable browser trace or waterfall for the shared client routes that the telemetry now identifies as the live hotspots.

### P2 - Confirm telemetry retention and deployment-provider values
- Why it matters: The app now treats performance telemetry as necessary operational monitoring, stores Web Vitals in Supabase, and gives admins a review tool.
- Needed access: Deployment-provider confirmation for `VITE_PERFORMANCE_BEACON_URL` and `VITE_CLOUDFLARE_WEB_ANALYTICS_TOKEN`, plus retention expectations for `performance_metric_events`.
- Current status: Product classification is decided as necessary operational monitoring, consent/legal copy is aligned, repo env templates include the variable names, Cloudflare Web Analytics setup was completed, the Supabase beacon endpoint is deployed, `/admin/performance` provides admin review, and live Supabase data now confirms ongoing stored beacon rows. The remaining questions are narrower now: confirm production deployment-provider values, retention period, and whether any telemetry origins beyond the currently observed `https://trustedbums.com` and small local-dev samples are expected.

### P2 - Verify Google Analytics live collection and agent access path
- Why it matters: B2B Growth, Data/Analytics, UX, UI, Product Ops, Trust & Reputation, Performance, Release Verification, and Lead Developer can now use GA as the primary website analytics source, but only after deployment and consented traffic prove collection is live.
- Needed access: Google Analytics property access for the `Trusted Bums` account/property, the `Trusted Bums Web` stream for `https://trustedbums.com`, date-range-filtered aggregate reports, source/medium and campaign reports, landing-page/event aggregates, and an agreed agent-safe way to cite aggregate GA evidence without exporting raw visitor-level data.
- Current status: GA4 setup remains complete under `TB-0066`: web stream `Trusted Bums Web`, stream URL `https://trustedbums.com`, stream ID `15031871944`, and measurement ID `G-P6B5EYQMVN`. Exact head `d360570` now ships the consent-gated GA loader and targeted `src/test/googleAnalyticsConsent.test.tsx` coverage from commit `795ebd2`, and the current local GA consent test passes. The remaining gap is no longer the code path; it is live evidence. This session still lacked GA property access to verify `Realtime`, `DebugView`, `Tag Diagnostics`, or current `Data received`, so agents may cite GA as configured but should not claim live website analytics evidence until property access confirms production collection and `TB-0066` can move from `FIXED` to `CLOSED`.

### P2 - Verify Bing Webmaster Tools ownership, sitemap, and agent access path
- Why it matters: Trust & Reputation, B2B Growth, Data/Analytics, Content, Marketing Graphics, UX, UI, Performance, Release Verification, and Lead Developer can use Bing Webmaster Tools for Microsoft search visibility, crawl, sitemap, indexing, SEO/GEO, backlink, keyword, and reputation evidence.
- Needed access: Bing Webmaster Tools access for the `https://trustedbums.com/` site entry, verification status, sitemap submission/status, crawl/indexing reports, SEO/GEO reports, keyword/backlink reports, Microsoft-side security or reputation messages when available, and an agreed agent-safe way to cite aggregate Bing evidence without exporting private data.
- Current status: Bing setup is verified under closed item `TB-0071`: the site entry exists, production serves `<meta name="msvalidate.01" content="F262EC5D731F39A7B9C8912DF741F807" />`, `public/robots.txt` allows Bingbot, `public/sitemap.xml` is live, Bing Verify succeeded, and `https://trustedbums.com/sitemap.xml` was submitted on 2026-06-09 with status `Processing`. Agents may use Bing Webmaster Tools when report data is available; record processing or empty-report states as current Bing evidence rather than setup blockers.

### P1 - Provide product, design, and operations source-of-truth artifacts
- Why it matters: UX, UI, Content, Product Ops, and Data/Analytics need stronger non-code evidence to validate terminology, queue design, workflow ownership, access handoffs, visual intent, funnel friction, and customer trust objections.
- Needed access: Brand and design sources, approved logo usage rules, campaign visual guidelines, campaign calendar, ad-account/channel priorities, audience definitions by channel, ad-account performance exports, examples of winning creative, legal-approved marketing claims, approved screenshot baselines, durable access to retained `Visual UI Audit` artifacts or an equivalent long-lived screenshot library for public plus authenticated role shells, ad-manager preview access or exportable crop proofs, OCR-capable image QA for spelling verification when generated text is unavoidable, funnel or product analytics, session recordings, support tickets or macros, support queue exports with current statuses and SLAs, customer-feedback exports, sales-objection notes, CRM or sales-pipeline exports for reverse-opportunity and target-response follow-through, onboarding materials, finance reconciliation and exception samples, admin audit/log examples, operations SOPs, and narrated role walkthroughs for Admin, Client Admin, Client Finance, Client Member, and Bum accounts.
- Current status: This gap remains open on 2026-06-11. `docs/brand-strategy.md` is still missing, so brand guidance remains inferred from shipped public-site copy, assets, typography, color usage, the claim-safe proof spine, and consultant rules rather than confirmed by an approved design source. Exact-head public evidence is stronger now because GitHub `QA` run `27316712677`, `E2E Smoke` run `27316733435`, and `Complete Visual UI Audit` run `27325075884` all passed on `349bbe0` for `https://trustedbums.com`, but that proof is still implementation-grade rather than design-approved and the artifact-retention window is too short to function as the only durable baseline. Product Ops still lacks support queue export with SLA buckets, CRM stage history, finance exception samples, admin rescue logs, access-request proof examples, narrated role walkthroughs for Admin, Client Admin, Client Finance, Client Member, and Bum roles, and the operations SOPs that explain queue ownership and escalation policy. Marketing Graphics keeps `TB-0080` open to track the missing brand and creative-governance inputs specifically: approved logo usage rules, campaign visual guidelines, campaign calendar, channel audience definitions, ad-account performance, winning-creative references, legal-approved claims boundaries, ad-manager crop proof, and OCR-backed spelling QA when text-bearing creative is required.

### P1 - Provide GTM source-of-truth and growth performance evidence
- Why it matters: B2B Growth, Content, Marketing Graphics, UX, Trust & Reputation, Product Ops, Data/Analytics, and Lead Developer need approved positioning, proof boundaries, and channel evidence before scaling Client acquisition or Bum recruiting.
- Needed access: `docs/brand-strategy.md` or equivalent approved brand source, CRM pipeline or tracking sheet with source/owner/stage/disqualification fields, website analytics and source attribution, LinkedIn organic and paid analytics, email performance metrics, founder scripts, approved case-study permissions, proof or claims matrix, objection notes, referral-disclosure guardrails, and channel budget constraints.
- Current status: This gap remains open on 2026-06-11. The repo still has no `docs/brand-strategy.md`, and the current session still had no CRM or pipeline export, live GA aggregate evidence beyond pending setup, LinkedIn account evidence, email-platform evidence, founder-script source beyond `docs/claim-safe-proof-spine.md`, approved case-study permissions, claims matrix, objection-note source, or channel budget evidence. Exact-head public proof is stronger on `349bbe0`, but growth recommendations are still source-backed and operationally useful rather than validated against approved GTM proof and performance data. The session also lacked a callable tracker-write path for refreshing the existing growth `TB-` rows.

## Role Access Matrix

### UX Consultant
- Core access: Authenticated role accounts, browser screenshots, Google Analytics funnel and landing-page aggregates when live, session recordings, support or customer feedback.
- If missing: Keep recommendations source-backed and say which user-confidence questions remain open.

### UI Consultant
- Core access: Browser screenshots, design sources, component references, and brand guidance.
- If missing: Keep visual recommendations narrow and route-specific.

### Marketing Graphics Artist
- Core access: Brand guidelines, approved logo/source assets, channel specs, campaign calendar, target audiences, ad-account performance data, examples of winning creative, legal-approved claims, image generation or design tooling, and visual QA/OCR capability.
- If missing: Keep concepts clearly marked as source-backed creative drafts, avoid factual or legal-sensitive claims, and do not mark generated assets campaign-ready without visual and spelling QA. Use `TB-0080` for the missing brand and creative-governance blocker.

### B2B Growth Marketer
- Core access: CRM/pipeline data for Client Prospects and Bum candidates, Google Analytics website/source/funnel aggregates when live, campaign performance, LinkedIn/email/ad data, audience definitions, approved sales collateral, legal-approved claims, case-study permissions, customer/Bum interview notes, objection notes, and channel budget constraints.
- If missing: Keep growth plays source-backed, prioritize low-risk founder/referral motions, and avoid claiming performance confidence or scale readiness.

### Content Copyeditor
- Core access: Product copy, legal-approved terminology, sales collateral, onboarding and support language.
- If missing: Flag naming drift but avoid claiming final terminology decisions.

### Trust And Reputation Consultant
- Core access: DNS/Cloudflare or registrar records, Google Search Console, Bing Webmaster Tools, Safe Browsing or security-issue reports, Microsoft SmartScreen feedback, email provider auth dashboards, DMARC reports, postmaster tools, scanner dashboards, public site crawl, and brand/company proof sources.
- If missing: Run public-source checks, keep findings scanner/source-backed, and mark dashboard-only checks as access gaps.

### Accessibility Specialist
- Core access: Authenticated browser access, screenshots, keyboard and screen-reader notes, automated axe coverage.
- If missing: Keep findings code-backed and identify operability gaps that still need live validation.

### QA/Test Engineer
- Core access: Routable QA target, seeded accounts, role credentials, CI history, flaky-test history, and deterministic auth setup.
- If missing: Treat skipped or unreachable suites as coverage limitations, not passing coverage.

### Security Engineer
- Core access: Supabase read-only SQL and advisors, Clerk configuration visibility, deployed function inventory or logs, seeded role tokens, CI secrets inventory.
- If missing: Do not assume current live policy or grant state from code alone.

### Performance Engineer
- Core access: Production or staging traces, Web Vitals, bundle analysis, Supabase advisor access, query plans, authenticated route access.
- If missing: Distinguish measured production evidence from local build or source inference.

### Data And Analytics Engineer
- Core access: Supabase SQL, Google Analytics aggregate reporting when live, product analytics or event data, export samples, reconciliation examples, and metric owners.
- If missing: Treat metric definitions and data quality as inferred, not confirmed.

### Product Ops Workflow Analyst
- Core access: Support queues, CRM pipeline, admin logs, finance exception examples, operations SOPs, and role walkthroughs.
- If missing: Treat recommendations as product-model findings, not proven operating reality.

### Lead Developer
- Core access: All specialist backlogs, Supabase tooling, QA/browser access, CI and deployment history, dependency audit output, and current external guidance.
- If missing: Prioritize the access request alongside the implementation recommendation it blocks.
