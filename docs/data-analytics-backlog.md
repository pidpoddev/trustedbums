# Trusted Bums Data Analytics Backlog

_Last updated: 2026-06-20 by Codex TB-0046 implementation pass._

## Executive Read

Exact-head review of `a17a85639a1b24dfda36da87d763eb4ecd3457af`, current hosted proof (`QA` `27798687806`, DreamHost deploy `27798687708`, and `E2E Smoke` `27798711531`), live Supabase reads for project `vaoqvtxqvbptyxddpoju`, and fresh GA4 aggregates for property `540873763` narrow the active analytics queue to one item. `TB-0044` is no longer active: current source now routes `CLIENT_FINANCE` through finance-safe projections in `src/lib/portalApi.ts`, the focused finance/export tests pass on the current head, and the live tracker row is already closed on `a17a856`.

The remaining analytics gap has been partially implemented in local source: the Admin Emails headline KPI path now has an admin-only aggregate helper and the UI no longer treats opens as the primary engagement number. GA business outcomes remain too sparse for decision-grade funnel recommendations, and live finance volume remains zero, so both stay in the watchlist rather than reopening closed or speculative implementation work.

## Active Recommendations

### P2 - [TB-0046] Make admin email metrics aggregate-first and stop treating opens as decision-grade
- Evidence: Implemented locally on 2026-06-20. [send-admin-email](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/send-admin-email/index.ts) now exposes admin-only `get_metrics`, which returns aggregate delivery, campaign, event, open-proxy, and click counts without returning recipient-level rows. [portalApi.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/lib/portalApi.ts) now has `getAdminEmailMetrics()`, and [AdminEmails.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/pages/admin/AdminEmails.tsx) moves headline cards to the aggregate helper. Opens are now labeled as `open proxy signals`; clicks are the named engagement card.
- Why it matters: The admin email dashboard will become sample-based as activity grows past the hard cap, and its most visible engagement number still leans on the least trustworthy signal in the dataset. The same reporting path also keeps using recipient-level joins that will get more expensive as campaign and delivery volume grows.
- Recommendation: Push and deploy the source change, then verify the live function serves the new operation. The remaining follow-up is bounded navigation for the raw delivery/campaign tables plus either additive indexes or explicit waivers for the admin-email joins used by the final query shape.
- Acceptance criteria: Admin email summary cards read aggregate data instead of capped lists; raw delivery and campaign tables support bounded navigation; open metrics are labeled as proxy or partial signals; the shared KPI surface avoids recipient-level rows; and the next SQL-backed review confirms either new indexes or an explicit waiver for the flagged admin-email joins used by the final query shape.

## Business Access And Reporting Rules

- Client Finance payment and invoice reporting: Current-head source now matches the committed finance-safe rule in `docs/business-access-rules.md`. `src/lib/portalApi.ts` selects the finance-safe projection for `CLIENT_FINANCE`, current report/export tests verify the narrowed payload shape, and live tracker row `TB-0044` is already closed on `a17a856`. No new business-access rule text is needed in this pass; the requirement is to preserve the same finance-safe contract in future finance-reporting changes.
- Admin email reporting: Current-head `docs/business-access-rules.md` still contains the correct admin-only raw reporting rule plus the aggregate-only exception path. Roles that need raw data: Admin and approved internal automation only. Roles that should not see raw rows: Client Admins, Client Finance, Client Members, Bums, and Public Visitors. Sensitivity: recipient identity, message content, click behavior, and suppression state. The remaining work is implementation: move the KPI path to aggregate-first helpers without broadening raw recipient-level access.

## Watchlist

- GA business outcomes are still too sparse for funnel analysis: `pnpm ga4:report -- --preset=outcomes --start-date=7daysAgo --end-date=today --limit=30` returned one row on 2026-06-19 (`trustedbums_claim_requested` with `opportunity_origin=opportunity_detail`, `1` event, `1` active user). Keep `TB-0066` closed, but do not promote conversion-rate or drop-off recommendations from GA outcomes until more than one outcome family shows stable live volume.
- Live production finance volume is still zero: `0` `customer_payment_reports` rows and `0` `claim_invoices` rows were present on 2026-06-19. Recheck finance exception reporting once seeded disputed, void, late, or paid-history cases exist in live or QA data.

## Agent Inputs

- Date of run: 2026-06-19.
- Files and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-data-analytics-engineer.toml`, `docs/agents/consultant-team-rules.md`, `docs/agents/company-wide-rules.md`, `docs/agents/consultant-access-needs.md`, `docs/agents/business-access-rules.md`, `docs/data-analytics-backlog.md`, `docs/business-access-rules.md`, `docs/codex-edit-log.md`, `docs/google-analytics-api.md`, `docs/consultant-access-needs.md`, `src/lib/portalApi.ts`, `src/pages/admin/AdminEmails.tsx`, `src/pages/client/ClientDashboard.tsx`, `src/pages/client/ClientReports.tsx`, `src/pages/client/ClientPayments.tsx`, `src/pages/client/ClientExports.tsx`, `src/pages/client/clientReportsModel.ts`, `src/pages/client/clientExportsModel.ts`, `src/lib/analyticsEvents.ts`, `supabase/functions/send-admin-email/index.ts`, `src/test/clientExportsAccess.test.ts`, `src/test/financeReportsModel.test.ts`, `src/test/analyticsEvents.test.ts`, `src/test/googleAnalyticsConsent.test.tsx`, and `src/test/adminScrumTracker.test.ts`.
- Live Supabase inputs reviewed for project `vaoqvtxqvbptyxddpoju`: project list and URL confirmation, current performance and security advisors, aggregate row counts for `admin_email_deliveries`, `admin_email_campaigns`, `admin_email_events`, `customer_payment_reports`, and `claim_invoices`, aggregate `OPEN` and `CLICK` event counts, current `public.admin_scrum_items` rows for `TB-0044`, `TB-0046`, and `TB-0066`, `information_schema.columns` for `admin_email_engagement_summary`, and current missing foreign-key index checks for the admin-email reporting tables.
- Checks run:
- `git rev-parse HEAD`
- `git status --short`
- `git log --since='2026-06-18 00:00' --stat -- src/lib/portalApi.ts src/pages/client/ClientDashboard.tsx src/pages/client/ClientExports.tsx src/pages/client/ClientReports.tsx src/pages/client/ClientPayments.tsx src/pages/client/clientReportsModel.ts src/pages/client/clientExportsModel.ts src/pages/admin/AdminEmails.tsx supabase/functions/send-admin-email/index.ts docs/data-analytics-backlog.md docs/business-access-rules.md docs/codex-edit-log.md`
- `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 30 --json databaseId,workflowName,status,conclusion,headSha,displayTitle,createdAt,updatedAt,event`
- `corepack pnpm exec vitest run src/test/clientExportsAccess.test.ts src/test/financeReportsModel.test.ts src/test/analyticsEvents.test.ts src/test/googleAnalyticsConsent.test.tsx src/test/adminScrumTracker.test.ts`
- `export GA4_PROPERTY_ID=540873763 GOOGLE_CLOUD_PROJECT=project-d45d35fc-184a-43c2-889 GA4_IMPERSONATE_SERVICE_ACCOUNT=trusted-bums-ga4-agent@project-d45d35fc-184a-43c2-889.iam.gserviceaccount.com; gcloud auth list --filter=status:ACTIVE --format='value(account)'`
- `export GA4_PROPERTY_ID=540873763 GOOGLE_CLOUD_PROJECT=project-d45d35fc-184a-43c2-889 GA4_IMPERSONATE_SERVICE_ACCOUNT=trusted-bums-ga4-agent@project-d45d35fc-184a-43c2-889.iam.gserviceaccount.com; pnpm ga4:report -- --preset=portal --start-date=7daysAgo --end-date=today --limit=30`
- `export GA4_PROPERTY_ID=540873763 GOOGLE_CLOUD_PROJECT=project-d45d35fc-184a-43c2-889 GA4_IMPERSONATE_SERVICE_ACCOUNT=trusted-bums-ga4-agent@project-d45d35fc-184a-43c2-889.iam.gserviceaccount.com; pnpm ga4:report -- --preset=outcomes --start-date=7daysAgo --end-date=today --limit=30`
- Current external sources reviewed: [Supabase Changelog](https://supabase.com/changelog), [Managing Indexes in Postgres | Supabase Docs](https://supabase.com/docs/guides/database/postgres/indexes), and [Use Mail Privacy Protection on iPhone | Apple Support](https://support.apple.com/guide/iphone/use-mail-privacy-protection-iphf084865c7/ios).
- Checks and data surfaces that could not be fully reviewed: this session did not run an authenticated browser walkthrough of Client Finance or Admin Emails, so route-level payload validation remains source-backed rather than browser-captured. This pass also did not inspect live email delivery logs because current schema, function source, aggregate counts, and advisors were enough to preserve the remaining recommendation.
