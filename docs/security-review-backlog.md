# Trusted Bums Security Review Backlog

_Last updated: 2026-06-21 by Codex daily security engineer automation._

## Executive Read

Exact head `5af32edeb0cc1290cdbae808207e75276d22a4d6` has clean hosted functional proof on the primary host: GitHub `QA` run `27885457568`, DreamHost deploy run `27885457565`, and deploy-triggered `E2E Smoke` run `27885474019` all completed `success` on 2026-06-20 UTC against `https://trustedbums.com`. The separate same-head `Deep QA Hotfix Audit` run `27894244168` failed, but the retained artifacts currently point to QA harness auth-bootstrap and preflight gaps rather than a newly reproduced security boundary failure.

Live Supabase validation stayed strong throughout this session. Project `vaoqvtxqvbptyxddpoju` is `ACTIVE_HEALTHY`, and live Edge Function inventory plus deployed source reads confirmed `admin-access-requests` v5, `client-team` v3, `invite-bum` v4, `send-admin-email` v11, `send-website-email` v2, `profile-bootstrap` v5, `bum-extension-download` v3, and `extension-api-v1` v7. Those Clerk-backed flows still pin the allowed Clerk issuer and verify session tokens against trusted JWKS material inside the handler, while `send-website-email` still requires service-role or internal-secret caller proof before it will send mail.

Direct SQL also confirmed `claim_client_notification_previews` and `admin_email_engagement_summary` both use `security_invoker=true`, `claim_client_notification_previews` remains authenticated-only, `bum_inner_circle_companies` still has RLS enabled, `sync_admin_scrum_item_owner_fields()` still keeps `search_path=public`, and no exposed public tables with `anon` or `authenticated` grants and RLS disabled were returned. The active security queue still narrows to `TB-0023` only. `TB-0089` and `TB-0111` remain closed on current live function and catalog evidence. The remaining exact-head concern is control-plane auditability, not a newly reproduced auth bypass: the live migration ledger still returned no rows for `20260619120000`, `20260620151519`, or `20260620152414`.

## Active Recommendations

### P1 - [TB-0023] Keep leaked-password protection blocked only with a current explicit decision
- Evidence: this run had exact-head hosted proof on `5af32ed`, stable live function inventory, deployed source reads for the current auth-bearing Edge Functions, and current tracker-row confirmation that `TB-0023` is still `BLOCKED`, but it still did not have direct Auth-settings visibility to confirm whether leaked-password protection is enabled or unavailable on the current plan. Current official Supabase password-security guidance still presents leaked-password protection as the expected baseline hardening path for password-backed auth.
- Why it matters: if any password-backed accounts remain active without leaked-password protection, previously compromised passwords can still be accepted unless another control fully compensates.
- Recommendation: enable leaked-password protection in Supabase Auth if the current plan and settings permit it. If it still cannot be enabled, keep `TB-0023` blocked only with a current owner, explicit accepted-risk decision, and compensating controls captured in the tracker and access docs.
- Acceptance criteria: live Auth settings show leaked-password protection enabled, or `TB-0023` records a current explicit owner, reason, and compensating-control decision.

## Business Rule Alignment

- `Claim notification previews`: the earlier business-rule mismatch remains closed. Live SQL still proves `claim_client_notification_previews` uses `security_invoker=true` with no `anon` select grant, so the current read path respects caller RLS instead of bypassing it.
- `Client company identity review`: live `profile-bootstrap` v5, `client-team` v3, and `admin-access-requests` v5 still treat Clerk signup metadata as onboarding input, route public-email and same-domain client access through review or approved-domain checks, and require proof metadata before risky admin approvals. That matches the current business rules that company identity and domain authority are not self-asserted by end users.
- `Managing Bum recruiting`: live `invite-bum` v4 now requires a named referral source plus explicit trust confirmation before sending an invite and preserves that context in the audit trail and pending team attachment notes. That aligns with the invite-only, trust-confirmed Bum recruiting rule.
- `Extension and Bum download access`: live `extension-api-v1` v7 and `bum-extension-download` v3 still pin the allowed Clerk issuer before serving extension context or package bytes, so the earlier issuer-drift closure remains consistent with the current business rule that only authenticated, approved role holders may use those privileged paths.
- `Admin email metrics`: live `send-admin-email` v11 still keeps `get_metrics`, campaign lists, delivery lists, schedules, and trigger rules behind admin-only verification or internal-edge sends. This run did not surface a new email-table exposure or RLS bypass.
- `Admin scrum helper security`: the helper hardening remains closed. Live SQL still proves `sync_admin_scrum_item_owner_fields()` keeps `search_path=public`, and the earlier mutable-search-path advisor finding does not reappear in current catalog evidence.

## Time-Sensitive Threat Notes

- Runtime dependency risk remains low in this run: `corepack pnpm audit --prod --json` returned zero runtime advisories across `229` production dependencies.
- Supabase's April 28, 2026 Data API exposure change is now directly relevant to this repo's current migration pattern: new `public` tables need explicit grants alongside RLS instead of relying on old defaults. Keep shipping `GRANT` plus RLS and policies together for any new exposed table. [Supabase Changelog](https://supabase.com/changelog)
- Current official Supabase password-security guidance still keeps leaked-password protection as the expected baseline hardening path for password auth. That continues to justify leaving `TB-0023` open until the live control is visibly enabled or explicitly waived. [Supabase Password Security](https://supabase.com/docs/guides/auth/password-security)
- Current official Supabase guidance for auth-bearing Edge Functions still expects trusted authorization headers and server-owned verification flow rather than token-selected trust roots. That remains the rationale for keeping issuer pinning closed only on live deployed source, not repo grep alone. [Supabase Auth Headers](https://supabase.com/docs/guides/functions/auth-headers) [Supabase Securing Edge Functions](https://supabase.com/docs/guides/functions/auth)
- Current official Supabase Clerk guidance still treats Clerk session-token claims as the basis for RLS and third-party auth integration, which keeps Trusted Bums' live issuer-pinned Clerk verification relevant even though `verify_jwt` stays `false` on these handlers. [Supabase Clerk Guide](https://supabase.com/docs/guides/auth/third-party/clerk)
- Clerk's current metadata guidance still says `unsafeMetadata` is frontend-writable, which keeps signup metadata in the onboarding-input category rather than the authorization-source-of-truth category. [Clerk User Metadata](https://clerk.com/docs/guides/users/extending)
- Clerk's current manual JWT-verification guidance still expects backends to verify with trusted key material or JWKS configuration and to validate authorized parties when that claim is present. That continues to justify the current issuer-pinned verification path while keeping seeded live allow-or-deny token proof on the access-gap list. [Clerk Manual JWT Verification](https://clerk.com/docs/guides/sessions/manual-jwt-verification)

## Watchlist

- `TB-0027` stays relevant as the release and control-plane umbrella for same-head Supabase provenance. This run's direct SQL did not find migration-ledger rows `20260619120000`, `20260620151519`, or `20260620152414`, so release verification remains correct to keep the head out of `GO` even though sampled live auth and RLS evidence are clean.
- `TB-0102` remains a workflow watchlist item, not an active security finding. `admin-shared-mailbox` is no longer in the issuer-pinning risk class, but the live mailbox/category/access proof should keep moving with the product-ops queue.

## Access Requests And Evidence Gaps

- This run had strong project-scoped Supabase access: project health, live Edge Function inventory, deployed source reads for seven security-sensitive functions, direct SQL for current reloptions and grants, direct SQL for current tracker rows, and a clean current scan for exposed public tables with grants but no RLS.
- The remaining gaps are now narrower and more specific than basic project reachability: direct Auth-settings visibility for leaked-password protection, same-head deployment ledger or migration-parity visibility, a seeded allow-or-deny token matrix for the highest-risk service-role paths, project-scoped security advisor output that returns reliably in this shell, and dependable tracker-write capacity when a status change is actually needed.
- This run did not refresh tracker rows because the live `TB-0023`, `TB-0089`, and `TB-0111` statuses already matched current evidence. Keep the item open from current source-backed plus live tracker proof until Auth-settings visibility or an explicit risk decision closes it.

## Agent Inputs

- Date of run: 2026-06-21 (`America/New_York`)
- Files and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-security-engineer.toml`; `docs/agents/consultant-team-rules.md`; `docs/agents/company-wide-rules.md`; `docs/agents/consultant-access-needs.md`; `docs/agents/business-access-rules.md`; `docs/consultant-team-rules.md`; `docs/company-wide-rules.md`; `docs/consultant-access-needs.md`; `docs/business-access-rules.md`; `docs/codex-edit-log.md`; prior `docs/security-review-backlog.md`; `docs/release-verification-backlog.md`; `package.json`; `supabase/config.toml`; the current security-sensitive function source set under `supabase/functions/`; and the focused security tests under `src/test/`.
- Live Supabase inputs reviewed for project `vaoqvtxqvbptyxddpoju`: `list_projects`; `list_edge_functions`; deployed source reads for `admin-access-requests`, `client-team`, `invite-bum`, `send-admin-email`, `send-website-email`, `profile-bootstrap`, `bum-extension-download`, and `extension-api-v1`; direct SQL checking `supabase_migrations.schema_migrations` for versions `20260619120000`, `20260620151519`, and `20260620152414` (returned no rows); direct SQL checking `claim_client_notification_previews`, `admin_email_engagement_summary`, and `bum_inner_circle_companies`; direct SQL checking `sync_admin_scrum_item_owner_fields()` configuration and execute grants; direct SQL checking current tracker rows `TB-0023`, `TB-0089`, and `TB-0111`; and a direct SQL scan for exposed public relations with `anon` or `authenticated` grants but no RLS (returned only the expected `security_invoker` views).
- GitHub evidence reviewed in this run: `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 25 --json databaseId,workflowName,status,conclusion,headSha,createdAt,updatedAt,displayTitle`.
- Local commands and checks reviewed: `git rev-parse HEAD`; `git status --short`; `git log --oneline --decorate -n 12`; targeted `git diff` against `supabase/functions/send-website-email/index.ts`, `src/test/websiteEmailFunction.test.ts`, and the current deep-QA auth-bootstrap edits; targeted `rg`; targeted `sed`; `corepack pnpm audit --prod --json`; and `corepack pnpm exec vitest run src/test/supabaseHelperSecurity.test.ts src/test/serviceRoleAuthorization.test.ts src/test/websiteEmailFunction.test.ts src/test/managingBumInvites.test.ts src/test/adminAccessReviewWorkflow.test.ts src/test/deploymentProvenance.test.ts`.
- Results: hosted exact-head QA, deploy, and E2E are green on `5af32ed`; standalone `Deep QA Hotfix Audit` failed on the same head because of harness auth-bootstrap and preflight gaps rather than a new security regression; runtime dependency audit returned zero production advisories; the focused security Vitest pack passed `30/30`; live function and catalog evidence keeps `TB-0089` and `TB-0111` closed; `TB-0023` stays blocked pending Auth-settings visibility or explicit risk acceptance; and migration-ledger parity remains a watchlist and provenance problem rather than a newly reproduced auth defect. The current local worktree also contains an unpublished `send-website-email` owner-notification change, but live project inventory still serves `send-website-email` v2, so that local diff was not treated as release evidence.
- Internet sources reviewed: current official Supabase changelog guidance on Data API exposure; current official Supabase password-security guidance; current official Supabase auth-header and function-auth guidance; current official Supabase Clerk integration guidance; current official Clerk metadata guidance; and current official Clerk manual JWT-verification guidance.
- Checks that could not run and why: this shell still could not inspect live Supabase Auth settings directly, so leaked-password protection remains a tracker or dashboard question rather than a freshly verified config fact. Project-scoped security advisor output was not available in the current callable tool set, and `supabase db advisors --linked` did not return usable output from this shell. This run also did not have a seeded live allow-or-deny token matrix for the current privileged-function set.
