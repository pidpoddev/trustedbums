# Trusted Bums Security Review Backlog

_Last updated: 2026-06-08 by Codex._

## Executive Read

The highest-confidence public mail issue is now closed in live Supabase source. Fresh deployed edge-function source confirms `send-website-email` rejects untrusted callers, and a follow-up live check on 2026-06-07 confirms `email-track` is deployed as version `2` with the same hardened allowlist logic as local source.

The top active security queue now shifts back to seeded service-role allow/deny proof, extension API authenticated proof, and one remaining Supabase Auth dashboard setting. The root Clerk dependency path has been upgraded so the lockfile no longer resolves `js-cookie` `3.0.5`; only patched `js-cookie` `3.0.7` remains. Bum saved-target reads align with the business rule in source and live Supabase via migration `20260607234751 remove_saved_target_read_entitlement`. Invitation redirect hardening is implemented, deployed, and source-verified on `client-team` and `invite-bum` version `2`; it still needs one safe authenticated invite smoke after release. Follow-up live QA found that migration `20260607235839 restrict_security_definer_helper_execute` over-revoked helpers used by RLS policies, causing non-admin terms checks to fail with `permission denied for function current_company_id` and `permission denied for function is_admin`. Migration `20260608002426 restore_rls_helper_execute_grants` restored policy-time execution, hosted authenticated role smoke passed all five roles, migration `20260608013000 move_rls_helpers_to_private_schema` moved those RLS helpers out of the exposed `public` RPC schema, and migration `20260608013500 qualify_private_rls_helper_references` fixed lingering policy/function references after hosted smoke exposed a `public.is_admin()` lookup regression. Final hosted authenticated role smoke passed all five roles. Refreshed live security advisors now report only leaked-password protection disabled, and a Chrome dashboard save attempt on 2026-06-08 confirmed Supabase blocks that setting on the current Free plan because leaked-password protection requires Pro or higher.

## Active Recommendations

### P1 - Add seeded allow/deny tests for service-role authorization paths
- Evidence: [supabase/config.toml](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/config.toml) still lists many authorization-sensitive functions with `verify_jwt = false`, including `client-team`, `profile-bootstrap`, `invite-bum`, `admin-access-requests`, `extension-api-v1`, and `send-admin-email`, so their trust boundary depends on in-function validation. New source-level regression coverage in [src/test/serviceRoleAuthorization.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/serviceRoleAuthorization.test.ts) now locks down Clerk verification, client-team cross-company/self-admin protections, profile-bootstrap non-authoritative metadata handling, admin-only access review, extension destination entitlement checks, and admin-email restrictions. Seeded live coverage is still missing for first-domain claim, public-email review, same-domain approval, related-domain gating, cross-company denial, and valid invite smoke.
- Why it matters: Trusted Bums increasingly relies on service-role edge functions for authorization-bearing workflows. Without explicit allow/deny regression tests, a future refactor can bypass the intended RLS or business-rule boundary without tripping the current test set.
- Recommendation: Add fixture-backed API/live coverage for first-domain claim, public-email company review, same-domain approval, denied cross-company client-team changes, denied direct auth-field mutation, and valid invite handoff. Keep the source-level service-role contract test as the regression guard for future refactors.
- Acceptance criteria: Each authorization-bearing service-role path has at least one seeded allowed and denied case, with audit-event expectations where state changes occur.

### P1 - Enable Supabase Auth leaked-password protection
- Evidence: Refreshed live Supabase security advisors on 2026-06-08 show only `auth_leaked_password_protection` after migration `20260608013000 move_rls_helpers_to_private_schema`. The advisor says leaked password protection is disabled and links to Supabase Auth password-security guidance. Chrome dashboard verification on project `vaoqvtxqvbptyxddpoju` found `PASSWORD_HIBP_ENABLED` off; attempting to save it on returned `Failed to update auth configuration: Configuring leaked password protection via HaveIBeenPwned.org is available on Pro Plans and up.`
- Why it matters: Trusted Bums uses Supabase Auth infrastructure alongside Clerk/Supabase surfaces, and enabling compromised-password checks reduces account-takeover risk for any Supabase password-auth paths that remain available now or later.
- Recommendation: Upgrade the Supabase org/project to Pro or higher, then enable leaked-password protection in the Supabase Auth dashboard or approved project configuration path and rerun security advisors.
- Acceptance criteria: Live Supabase security advisors no longer show `auth_leaked_password_protection`; no intended auth flow regresses.

## Business Rule Alignment

- `Public contact intake`: current local and deployed `send-website-email` source now requires trusted internal caller proof, which is materially closer to the business rule that public contact intake must go through validated server-side abuse controls. `submit-contact` still performs origin checks, Turnstile verification, and rate limiting first, so the prior public-mail-sender finding is no longer current.
- `Tracked admin email links`: local source and live deployed `email-track` now align with the stricter trust posture. Live version `2` matches the hardened implementation that validates delivery existence, constrains destinations to approved `https` hosts, returns `400` for disallowed destinations, and returns `404` for unknown deliveries.
- `Invitation redirects`: current source and deployed `client-team`/`invite-bum` version `2` now use shared allowlist logic for Client Team and Bum invitation redirects, normalize approved origins to `/login`, fall back server-side when the caller omits or supplies a disallowed redirect, and validate configured fallback origins. Public no-auth smoke confirms both deployed functions boot and return the expected `Missing bearer token.` error. One safe authenticated invite smoke remains post-release proof.
- `Profile bootstrap`: current [supabase/functions/profile-bootstrap/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/profile-bootstrap/index.ts) still reads Clerk `signupIntent` and company-name hints from public and unsafe metadata as onboarding input. That remains acceptable only if those fields stay non-authoritative and server-side approval paths remain the only way to assign role, company, client access role, and admin state.
- `Service-role edge functions`: source-level coverage now verifies the high-risk service-role contracts for Clerk token verification, client-team authorization, profile bootstrap metadata handling, admin access review, extension destination scoping, and admin email operations. Seeded live allow/deny proof remains open for the business workflows that need role accounts and multi-company fixtures.
- `Clerk dependency chain`: root web `@clerk/react` now resolves to `6.7.1`, dev `@clerk/testing` now resolves to `2.0.35`, and the current lockfile contains only `js-cookie` `3.0.7`, not the affected `3.0.5` path.
- `Customer targets`: the business rule denies saved-target-only visibility unless Product Ops explicitly approves it, and current source plus live Supabase migration `20260607234751` now limit Bum target reads to accepted target responses or scheduled/completed meetings.
- `Internal Supabase helpers`: live migration `20260607235839` removed direct Data API execution from internal security-definer RLS/trigger helpers, but it also broke policy-time execution for helpers used by RLS. Live migration `20260608002426` restored `anon` and `authenticated` grants for `current_company_id()`, `is_admin()`, `is_bum()`, and the conversation/customer-target RLS helpers, while keeping `prevent_profile_self_authorization_mutation()` closed to direct callers. Hosted authenticated role smoke passed afterward. Live migration `20260608013000` then moved those seven RLS helpers into the `private` schema, and `20260608013500` qualified policies plus dependent functions to `private.*` after smoke caught unresolved `public.is_admin()` lookups. Refreshed Supabase advisors no longer flag the helpers as public or signed-in security-definer RPCs.
- Required direct checks before closing current access work: allowed approved-host tracked click with a safe seeded delivery; allowed first-domain claim; pending public-email company request; approved same-domain request; denied cross-company client-team action; denied direct `profiles.company_id` or `profiles.client_access_role` mutation; allowed explicit-entitlement Bum target read; denied saved-item-only target read.

## Time-Sensitive Threat Notes

- GitHub advisory `GHSA-qjx8-664m-686j` for `js-cookie`, updated May 21, 2026, marks `<= 3.0.5` affected and `3.0.7` patched. Current `pnpm-lock.yaml` now resolves only `js-cookie` `3.0.7`. [Source](https://github.com/advisories/GHSA-qjx8-664m-686j)
- GitHub advisory `GHSA-w5hq-g745-h8pq` for `uuid`, updated May 21, 2026, marks `< 11.1.1` and certain `12.x`/`13.x` ranges affected. Trusted Bums still resolves `uuid@8.3.2` transitively in the current lockfile, so keep that dependency path visible until the owning package is assessed or updated. [Source](https://github.com/advisories/GHSA-w5hq-g745-h8pq)
- GitHub advisory `GHSA-2j2x-hqr9-3h42`, updated June 3, 2026, affects `react-router` `< 6.30.4`, but GitHub explicitly says Declarative Mode with `<BrowserRouter>` is not impacted. Current app source still uses `<BrowserRouter>` in [src/App.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/App.tsx). [Source](https://github.com/advisories/GHSA-2j2x-hqr9-3h42)
- Current Supabase API guidance says functions are controlled by `EXECUTE` grants rather than RLS, recommends reviewing any `SECURITY DEFINER` functions carefully, and recommends an opt-in exposure model or dedicated API schema. The current live helper cleanup reflects this guidance. [Source](https://supabase.com/docs/guides/api/securing-your-api)
- Current Supabase RLS guidance warns against using end-user-modifiable metadata for authorization decisions and recommends keeping internal helper surfaces out of exposed paths when possible. [Source](https://supabase.com/docs/guides/database/postgres/row-level-security)
- Current Clerk docs still say `unsafeMetadata` can be updated from the frontend with `User.updateMetadata()`, so Trusted Bums must continue treating it as onboarding input only, never as authoritative authorization state. [Source](https://clerk.com/docs/guides/users/extending)
- Current Cloudflare Turnstile guidance still expects server-side validation plus checks on expected `action` and hostname. [Source](https://developers.cloudflare.com/turnstile/get-started/server-side-validation/)

## Watchlist

- Recheck `send-website-email` after every deploy because it is now hardened and should not regress back to a direct anonymous mail path.
- Keep `email-track` on the release smoke list. Live version `2` was verified on 2026-06-07, an off-domain click returned `400`, and an allowed-host click with an unknown delivery returned `404`; the only remaining proof gap is a seeded valid-delivery allowed redirect that records a click without using a real recipient.
- Keep Client Team and Bum invitation redirects on the release smoke list until a safe valid invite confirms the expected Clerk `/login` handoff with an approved origin.
- Keep the React Router advisory on watchlist only unless the app moves away from the current `<BrowserRouter>` declarative routing mode.
- Keep the `verify_jwt = false` function inventory on watchlist because many Trusted Bums trust boundaries now depend on in-function Clerk verification and caller validation rather than Supabase JWT enforcement.
- Recheck live security advisors after every helper/RLS migration because advisor output is the fastest proof that Data API helper exposure narrowed.
- Track leaked-password protection as a Supabase Auth dashboard/config item blocked by current Supabase plan level until the project is upgraded, the setting is enabled, or the risk is explicitly accepted.

## Access Requests And Evidence Gaps

- This run had live Supabase access through the generic project-id connector for project `vaoqvtxqvbptyxddpoju`; a current table inventory confirmed the connector was scoped to Trusted Bums. Project-scoped advisor tooling was not available in this follow-up, so security advisors should still be rerun after the next connector session exposes them.
- `.env.qa` is present locally and supports hosted authenticated role smoke. The sourced `qa:env` contract now intentionally fails because `.env.qa` configures `QA_EXTENSION_API_BASE_URL` but does not yet include `QA_EXTENSION_API_TOKEN`.
- Clerk dashboard/API policy visibility, invitation redirect policy configuration, extension release settings, and environment/secret inventory by variable name are still missing.
- Seeded multi-company allow/deny fixtures are still missing for same-domain approval, public-email review, cross-company client-team denial, saved-target-only target reads, and live service-role edge-function authorization.
- CI/CD deploy history is still missing. The earlier `email-track` drift is resolved live, but deployment provenance remains needed to explain why live and local source diverged and to prove future function deployments are caught by release checks.

## Agent Inputs

- Date of run: 2026-06-08
- Files and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-security-engineer.toml`; `docs/agents/consultant-team-rules.md`; `docs/agents/company-wide-rules.md`; `docs/agents/consultant-access-needs.md`; `docs/agents/business-access-rules.md`; `docs/codex-edit-log.md`; previous `docs/security-review-backlog.md`; `package.json`; `pnpm-lock.yaml`; `supabase/config.toml`; `supabase/functions/send-website-email/index.ts`; `supabase/functions/submit-contact/index.ts`; `supabase/functions/email-track/index.ts`; `supabase/functions/client-team/index.ts`; `supabase/functions/invite-bum/index.ts`; `supabase/functions/profile-bootstrap/index.ts`; `supabase/migrations/20260529001000_add_admin_dashboard_summary.sql`; `supabase/migrations/20260529180000_restrict_bum_customer_target_reads.sql`; `supabase/migrations/20260531143000_harden_profile_bootstrap.sql`; `src/App.tsx`; `src/lib/contactApi.ts`; `src/lib/portalApi.ts`; `scripts/verify-qa-env.mjs`.
- Live Supabase checks reviewed: `mcp__supabase_trustedbums.get_project_url`; `mcp__supabase_trustedbums.list_edge_functions`; `mcp__supabase_trustedbums.get_edge_function` for `send-website-email`; `mcp__supabase_trustedbums.get_edge_function` for `email-track`; follow-up generic Supabase connector `_get_project_url` and `_get_edge_function` for `email-track`.
- Local commands reviewed: `git status --short`; targeted `rg`; targeted `sed`; `/Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run qa:env`; `/Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm run lint`; `/Users/macdaddy/.local/share/codex-node/node-v24.16.0-darwin-arm64/bin/corepack pnpm exec vitest run src/test/extensionApiContract.test.ts src/test/routeGuards.test.tsx src/test/customerTargetRules.test.ts src/test/authData.test.ts src/test/paymentCommission.test.ts src/test/termsContractRules.test.ts src/test/opportunityModel.test.ts`; `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`; `curl -i -L --max-time 20 "https://vaoqvtxqvbptyxddpoju.supabase.co/functions/v1/email-track/click?d=00000000-0000-0000-0000-000000000000&u=https%3A%2F%2Fevil.example%2Fbad"`; `curl -i -L --max-time 20 "https://vaoqvtxqvbptyxddpoju.supabase.co/functions/v1/email-track/click?d=00000000-0000-0000-0000-000000000000&u=https%3A%2F%2Ftrustedbums.com%2F"`.
- Results: targeted Vitest passed 28 tests across 7 files; lint passed with 7 existing `react-hooks/exhaustive-deps` warnings and no errors; `qa:env` failed because the expected QA variable names were not present in the shell during the original security run; `curl` to `https://rcdl.tplinkdns.com` failed with `curl: (60) SSL certificate problem: unable to get local issuer certificate`; live edge-function source confirmed `send-website-email` is hardened; follow-up live source confirmed `email-track` version `2` now matches local hardening; an off-domain click returned `400`; and an allowed-host click with an unknown delivery returned `404`.
- Internet sources reviewed: GitHub advisories for `js-cookie`, `uuid`, and `react-router`; Supabase API security guidance; Supabase RLS guidance; Clerk metadata guidance; Cloudflare Turnstile server-side validation guidance.
- Checks that could not run and why: live Supabase SQL/catalog checks could not run because `mcp__supabase_trustedbums.execute_sql` was not callable in this session; authenticated browser or role-token allow/deny checks could not run because the QA env contract was not present in the shell; `pnpm audit --prod --json` did not complete in a useful way during this run, so dependency risk was assessed from the current lockfile plus current advisory pages instead.
