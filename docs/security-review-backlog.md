# Trusted Bums Security Review Backlog

_Last updated: 2026-06-15 by Codex daily security engineer automation._

## Executive Read

Current `main` head `7ee97c1` still matches the latest hosted proof rechecked in this run. GitHub `QA` run `27469969615`, DreamHost deploy run `27469969636`, hosted `E2E Smoke` run `27469985957`, and exact-head `Visual UI Audit` run `27488973899` all completed successfully for commit `7ee97c121918bba73149748b49f2b28133c7ffbb` between 2026-06-13 and 2026-06-14 UTC.

Live Supabase verification is current in this run. Project `vaoqvtxqvbptyxddpoju` is still `ACTIVE_HEALTHY`, the project URL still resolves to `https://vaoqvtxqvbptyxddpoju.supabase.co`, live security advisors still flag the standing `TB-0023`, `TB-0081`, `TB-0085`, and `TB-0087` issues, live SQL still shows exposed-role `EXECUTE` on the customer-lead and admin-scrum helper functions, and live Edge Function inventory, deployed source, and current logs still show `sync-claim-decision-replies` version `3` active with `verify_jwt = false` and repeated recent `POST | 200` runs while the code still fails open when `CLAIM_DECISION_SYNC_SECRET` is absent.

There is no new shipped security delta since the previous security pass. `git log -1 --format='%ci %h %s'` still resolves to `2026-06-13 10:48:09 -0400 7ee97c1 Fix GA4 consent page view dispatch`, and `git diff --name-only -- src supabase tests .github package.json pnpm-lock.yaml` is empty in this working tree. `corepack pnpm audit --prod --json` returned zero production advisories, the focused security regression pack passed `33/33`, raw `qa:env` still fails until `.env.qa` is sourced, sourced `qa:env` plus `qa:target-preflight` pass against `https://trustedbums.com`, `https://trustedbums.com` still returns `HTTP/2 200` with the expected security headers, and `https://rcdl.tplinkdns.com` still fails TLS validation while `http://rcdl.tplinkdns.com` still returns `403`.

## Active Recommendations

### P1 - [TB-0087] Remove public EXECUTE from customer-lead duplicate helper and narrow exposed grants
- Evidence: Live security advisors on 2026-06-15 still flag `public.find_customer_lead_duplicate(p_vendor_company_id uuid, p_customer_domain text)` as a `SECURITY DEFINER` function callable by both `anon` and `authenticated`. Live SQL in this run revalidated `anon_execute = true`, `authenticated_execute = true`, `service_role_execute = true`, and `proconfig = 'search_path=public'` on that helper, while current source in [supabase/migrations/20260611120000_add_customer_lead_duplicate_check.sql](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260611120000_add_customer_lead_duplicate_check.sql) and [supabase/migrations/20260611124500_match_customer_lead_domain_aliases.sql](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260611124500_match_customer_lead_domain_aliases.sql) still grants `EXECUTE` on that helper to `anon` and `authenticated`. The companion helper `public.normalize_customer_domain(text)` also still shows exposed-role execute with no explicit `search_path`, matching the live `function_search_path_mutable` warning.
- Why it matters: `SECURITY DEFINER` bypasses RLS on the underlying tables. That lets a public or signed-in caller probe whether an arbitrary vendor company already has a matching customer target or customer lead for a domain, which conflicts with the business rule that Bums manage only their own leads and unmatched clients must not browse lead details.
- Recommendation: Move duplicate detection behind a Portal Domain API or make the helper invoker-safe and caller-scoped by deriving the vendor company from the authenticated session instead of accepting an arbitrary company id. Revoke anonymous execute, keep any RLS-bypassing helper out of the public RPC surface, and add an explicit `search_path` to `normalize_customer_domain()` if it remains callable.
- Acceptance criteria: Direct RPC calls with an arbitrary vendor company id no longer return cross-company lead or target metadata; exposed-role `EXECUTE` is removed or the helper proves caller-scoped company binding; source or live tests cover anonymous deny, foreign-company deny, and legitimate same-company allow paths; and `normalize_customer_domain()` no longer shows a search-path warning in live review.

### P1 - [TB-0081] Revoke public EXECUTE on admin scrum audit helpers
- Evidence: Live security advisors on 2026-06-15 still flag both `public.record_admin_scrum_item_audit_event()` and `public.set_admin_scrum_item_audit_fields()` as `SECURITY DEFINER` functions callable by both `anon` and `authenticated`. Live SQL in this run revalidated `anon_execute = true`, `authenticated_execute = true`, and `service_role_execute = true` on both helpers. Current source in [supabase/migrations/20260609100000_harden_admin_scrum_tracker_audit.sql](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260609100000_harden_admin_scrum_tracker_audit.sql) still creates both helpers in the exposed `public` schema without revoking exposed-role `EXECUTE`.
- Why it matters: Admin-only tracker table RLS does not stop `/rest/v1/rpc` execution when the helper functions themselves remain callable by public or signed-in roles. That leaves admin-only audit logic on a broader external surface than the business rule allows.
- Recommendation: Revoke `EXECUTE` on both helpers from `PUBLIC`, `anon`, and `authenticated`, or move them to a non-exposed schema while keeping the triggers intact. Add a regression that locks the revoke-or-private-schema contract so later migrations cannot silently reopen the RPC surface.
- Acceptance criteria: Live grant review no longer shows exposed-role `EXECUTE` on either helper; `/admin/scrum` create and update flows still work through the trigger path; and source tests assert the helpers are not publicly executable.

### P1 - [TB-0085] Require fail-closed auth for claim decision sync
- Evidence: Live Edge Function inventory on 2026-06-15 still shows `sync-claim-decision-replies` active as version `3` with `verify_jwt = false`. Live edge-function logs in this run include repeated recent `POST | 200` activity for that slug, and live deployed function source still only rejects callers when `CLAIM_DECISION_SYNC_SECRET` exists. Current scheduler source in [supabase/migrations/20260609124500_add_claim_decline_reasons_and_email_decisions.sql](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260609124500_add_claim_decline_reasons_and_email_decisions.sql) still posts to the public function URL every five minutes and sends `x-sync-secret` as `coalesce(..., '')`, which means missing-secret state still degrades into an empty header rather than a hard failure. This run still did not have vault-secret visibility, so the secret-presence question remains unverified even though the fail-open code path is confirmed live.
- Why it matters: This function uses service-role access plus Microsoft Graph mailbox access to process client claim decisions. If the secret is absent, removed, or misconfigured, anonymous callers could trigger mailbox-backed claim mutation work on a public endpoint.
- Recommendation: Make the function fail closed by requiring a non-empty sync secret at startup or on every request, or move the invocation to a JWT-protected internal-only path. Mirror that fail-closed requirement into the scheduler path so cron setup also errors when the secret is missing, then add a regression test that proves missing-secret deployments are rejected.
- Acceptance criteria: Deploy or startup fails when the sync secret is missing, or the function rejects all calls without a valid internal auth mechanism; scheduled invocations prove they use non-empty auth material; and a regression test asserts the function cannot run with `verify_jwt = false` plus an empty or missing secret.

### P1 - [TB-0091] Harden extension API rate limits and negative-path contract coverage
- Evidence: The Chrome extension API remains the only current partner-style API contract. Exact-head source review of [supabase/functions/extension-api-v1/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/extension-api-v1/index.ts) still shows Clerk verification, entitlement checks, idempotency, and audit behavior, but no explicit per-user, per-token, per-origin, or equivalent abuse-control posture for `GET /context` or `POST /page-captures`. The focused security regression pack passed `33/33` in this run, including [src/test/extensionApiContract.test.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/test/extensionApiContract.test.ts), but current coverage still does not prove wrong-issuer JWT rejection, stable deny envelopes across malformed requests, or direct Data API deny behavior for `extension_page_captures`.
- Why it matters: A valid or stolen Clerk token could repeatedly create draft captures containing URLs, selected text, notes, and audit events. The partner-API contract now requires rate limits, idempotency, audit, and stable error behavior; the current extension API only satisfies part of that contract.
- Recommendation: Add rate-limit or equivalent abuse-control enforcement for extension API calls, and add contract tests for invalid destination combinations, mismatched `destinationType` and id fields, invalid or missing `sourceUrl`, invalid `captureType`, idempotent retry behavior, stable error envelopes, CORS allow/deny, unknown endpoint `404`, forged or wrong-issuer JWT rejection, cross-company denial, and direct Data API allow/deny behavior for `extension_page_captures`.
- Acceptance criteria: Extension API v1 has explicit abuse-control or rate-limit behavior; negative-path tests prove invalid requests and forged or wrong-tenant tokens fail closed; direct table writes remain disallowed outside intended paths; and hosted extension API smoke covers at least one authenticated allow path plus stable deny paths before any future native, client, or partner consumer depends on the API.

### P1 - [TB-0023] Keep leaked-password protection blocked only if the plan decision is explicit
- Evidence: The standing leaked-password protection gap is still live by current security-advisor output and tracker state. Live advisors on 2026-06-15 still report `auth_leaked_password_protection`, and this shell still did not have Auth dashboard-setting visibility, so it could not freshly prove whether the control can now be enabled or remains blocked by plan.
- Why it matters: If password-backed accounts remain enabled without leaked-password protection, previously compromised passwords stay eligible unless another control fully compensates.
- Recommendation: Enable Supabase Auth leaked-password protection if the active plan and dashboard access now allow it. If it is still unavailable on the active plan, keep `TB-0023` open or waive it only with an explicit accepted-risk decision and documented compensating controls.
- Acceptance criteria: Live Auth settings show leaked-password protection enabled, or the tracker and security backlog record an explicit accepted-risk or plan-upgrade decision with current rationale and compensating controls.

## Business Rule Alignment

- `Customer Leads`: [docs/business-access-rules.md](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/docs/business-access-rules.md) says a Bum may create and manage only their own customer leads, Admin may review or match them, and unmatched clients must not browse lead details. `TB-0087` is a direct mismatch because the live `find_customer_lead_duplicate()` RPC is publicly callable and accepts an arbitrary vendor company id while returning customer-lead or target metadata.
- `Admin scrum tracker`: current business rules say only Admins and approved internal agent workflows may manage `public.admin_scrum_items`. `TB-0081` remains a direct mismatch because the two audit trigger helpers are still callable as public RPCs.
- `Opportunities, Claims, And Intro Requests`: current business rules allow claim review and approval only for the relevant client company users and Admins. The `sync-claim-decision-replies` workflow is internal automation acting on that same boundary, so `TB-0085` remains a trust-boundary hardening gap until the caller auth path fails closed.
- `Client opportunity pipeline details and claimed quick edits`: the only exact-head diff since the previous security run is the GA consent page-view fix in [src/components/GoogleAnalytics.tsx](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/src/components/GoogleAnalytics.tsx) plus its test update. No current-head source diff touched the company-scoped client opportunity data path, so there is still no new business-rule mismatch beyond the standing helper exposures.
- `Profile bootstrap`: current source in [supabase/functions/profile-bootstrap/index.ts](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/functions/profile-bootstrap/index.ts) still reads signup intent and company hints from Clerk metadata. Current Clerk guidance still allows frontend updates to `unsafeMetadata`, so the existing business rule remains correct: role, company, `client_access_role`, and admin status must stay server-owned and auditable.
- `Public contact intake`: current source plus sourced `qa:target-preflight` still support the intended public-intake rule. Anonymous extension requests still return the expected `401`, and public contact traffic still relies on Turnstile-backed server-side validation rather than a direct public mail-send surface.

## Time-Sensitive Threat Notes

- Production dependency risk remains low from the package-audit surface in this run. `corepack pnpm audit --prod --json` returned zero production advisories.
- Current Supabase API guidance says grants determine whether `anon`, `authenticated`, or `service_role` can reach a table, view, or function over the Data API, while RLS determines which rows they can touch. That is the live rationale behind `TB-0087` and `TB-0081`. [Source](https://supabase.com/docs/guides/api/securing-your-api)
- Current Supabase docs say privileged functions do not need to live in an exposed schema to participate in RLS logic, and hook examples explicitly revoke `EXECUTE` from `anon`, `authenticated`, and `public`. That is the live rationale for keeping admin-scrum and duplicate-check helpers off the public RPC surface. [Source](https://supabase.com/docs/guides/troubleshooting/do-i-need-to-expose-security-definer-functions-in-row-level-security-policies-iI0uOw)
- Current Supabase Edge Function guidance says `verify_jwt = false` is for genuinely public functions or handlers with an intentional alternate auth model, not for internal-only jobs that should silently accept missing auth. That is the live rationale behind `TB-0085`. [Source](https://supabase.com/docs/guides/functions/function-configuration)
- Current Supabase platform guidance says new projects no longer expose `public` tables to the Data API automatically, while existing objects keep their current grants until reviewed. Trusted Bums predates that default shift, so `TB-0087` remains real even though the platform default is tightening. [Source](https://supabase.com/changelog/45329-breaking-change-tables-not-exposed-to-data-and-graphql-api-automatically)
- Current Supabase password-security guidance says leaked-password protection checks passwords against the HaveIBeenPwned corpus and should be enabled where possible. That is the live rationale behind the still-open `TB-0023` warning. [Source](https://supabase.com/docs/guides/auth/password-security)
- Current Clerk metadata guidance still says frontend code can update `unsafeMetadata`, and signup `unsafeMetadata` is copied onto the user record after sign-up. Trusted Bums should continue treating that surface as onboarding input only, never as the authority for role, company, or admin access. [Source](https://clerk.com/docs/guides/users/extending)
- Current OWASP API Security 2023 guidance says endpoints that accept object identifiers need explicit object-level authorization because attackers can manipulate ids in request paths or payloads. That is the live rationale for `TB-0087` accepting an arbitrary vendor company id. [Source](https://owasp.org/API-Security/editions/2023/en/0xa1-broken-object-level-authorization/)
- Current OWASP API Security guidance still treats missing or weak resource and rate-limit controls as an API abuse risk. That is the live rationale for requiring explicit abuse controls on `extension-api-v1` before more consumers depend on it. [Source](https://owasp.org/API-Security/editions/2023/en/0xa4-unrestricted-resource-consumption/)
- Current Cloudflare Turnstile guidance still says server-side validation is mandatory because tokens are single-use, can expire, and can be forged. Keep that contract on every public-intake path change. [Source](https://developers.cloudflare.com/turnstile/get-started/server-side-validation/)

## Watchlist

- Keep the broader `verify_jwt = false` Edge Function inventory on watch because several important workflows still rely on in-function verification rather than Supabase JWT enforcement. `TB-0089` still needs shared Clerk verification that pins allowed issuer and audience instead of deriving trust from an untrusted token issuer.
- Keep seeded-proof follow-up on watch for Clerk-backed mutation paths such as `client-team`, `invite-bum`, and `admin-access-requests`. This run revalidated source contracts and exact-head preflight, not a fresh seeded live mutation matrix.
- Keep `rcdl.tplinkdns.com` in the trust and release watchlist. `https://rcdl.tplinkdns.com` still fails TLS verification from this runner, while `http://rcdl.tplinkdns.com` still returns `403`.

## Access Requests And Evidence Gaps

- Live Supabase access in this session included project metadata, project URL confirmation, live security-advisor output, live Edge Function inventory, direct SQL against `pg_proc` and `public.admin_scrum_items`, deployed `sync-claim-decision-replies` source, live edge-function logs, and tracker reads plus current-row refreshes for `TB-0023`, `TB-0081`, `TB-0085`, `TB-0087`, and `TB-0091`. Like the 2026-06-14 security shell, this run did not hit a same-session `RATE_LIMITED` burst while pulling advisors, SQL, function source, logs, and tracker rows. The remaining Supabase gaps are narrower: no Auth dashboard setting visibility for leaked-password protection, no vault-secret visibility for `CLAIM_DECISION_SYNC_SECRET`, and no fresh seeded allow or deny matrix executed against the live data path.
- Raw-shell QA env remains intentionally separate from sourced QA env. `corepack pnpm run qa:env` failed in an unsourced shell because `QA_BASE_URL`, Clerk keys, and QA role email variables were not exported. After sourcing `.env.qa`, both `qa:env` and `qa:target-preflight` passed against `https://trustedbums.com`.
- The required external DNS target remains TLS-broken from this runner: `curl -I -L --max-time 20 https://rcdl.tplinkdns.com` still fails with `curl: (60) SSL certificate problem: unable to get local issuer certificate`, while `http://rcdl.tplinkdns.com` returns `403`.
- Extension API QA remained intentionally source-backed in this run because `QA_EXTENSION_API_EXPECTATION=skip` was set for hosted preflight. The exact-head hosted base target is healthy, but authenticated extension allow or deny proof is still missing.

## Agent Inputs

- Date of run: 2026-06-15 (`America/New_York`)
- Files and docs reviewed: `docs/agents/automation-prompts/trusted-bums-daily-security-engineer.toml`; `docs/agents/consultant-team-rules.md`; `docs/agents/company-wide-rules.md`; `docs/agents/consultant-access-needs.md`; `docs/agents/business-access-rules.md`; `docs/business-access-rules.md`; `docs/consultant-access-needs.md`; `docs/codex-edit-log.md`; previous `docs/security-review-backlog.md`; `package.json`; `src/lib/portalApi.ts`; `src/test/serviceRoleAuthorization.test.ts`; `src/test/supabaseHelperSecurity.test.ts`; `src/test/adminScrumTracker.test.ts`; `src/test/claimDeclineWorkflow.test.ts`; `src/test/extensionApiContract.test.ts`; `src/test/supabaseTokenMode.test.ts`; `src/test/websiteEmailFunction.test.ts`; `supabase/config.toml`; [supabase/migrations/20260609100000_harden_admin_scrum_tracker_audit.sql](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260609100000_harden_admin_scrum_tracker_audit.sql); [supabase/migrations/20260609124500_add_claim_decline_reasons_and_email_decisions.sql](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260609124500_add_claim_decline_reasons_and_email_decisions.sql); [supabase/migrations/20260611120000_add_customer_lead_duplicate_check.sql](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260611120000_add_customer_lead_duplicate_check.sql); [supabase/migrations/20260611124500_match_customer_lead_domain_aliases.sql](/Users/macdaddy/CodexWork/TrustedBums/trustedbums/supabase/migrations/20260611124500_match_customer_lead_domain_aliases.sql); `supabase/functions/extension-api-v1/index.ts`; and the live deployed `sync-claim-decision-replies` function source from project `vaoqvtxqvbptyxddpoju`.
- Live Supabase inputs reviewed for project `vaoqvtxqvbptyxddpoju`: project lookup; project URL lookup; live security advisors; live Edge Function inventory; deployed `sync-claim-decision-replies` source; live edge-function logs; direct SQL for helper grants; direct SQL for tracker rows; and live tracker refreshes for `TB-0023`, `TB-0081`, `TB-0085`, `TB-0087`, and `TB-0091`.
- GitHub evidence reviewed in this run: `/Users/macdaddy/bin/gh-trustedbums run list --repo Pidpoddev/trustedbums --limit 20 --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27469969615 --repo Pidpoddev/trustedbums --json ...`; `/Users/macdaddy/bin/gh-trustedbums run view 27469985957 --repo Pidpoddev/trustedbums --json ...`; and `/Users/macdaddy/bin/gh-trustedbums run view 27488973899 --repo Pidpoddev/trustedbums --json ...`.
- Local commands and checks reviewed: `git status --short`; `git rev-parse HEAD`; `git rev-parse --short HEAD`; `git log -1 --format='%ci %h %s'`; `git diff --name-only -- src supabase tests .github package.json pnpm-lock.yaml`; targeted `rg`; targeted `sed`; `corepack pnpm audit --prod --json`; raw `corepack pnpm run qa:env`; sourced `QA_EXTENSION_API_EXPECTATION=skip corepack pnpm run qa:env`; sourced `QA_EXTENSION_API_EXPECTATION=skip corepack pnpm run qa:target-preflight`; `corepack pnpm exec vitest run src/test/supabaseHelperSecurity.test.ts src/test/adminScrumTracker.test.ts src/test/claimDeclineWorkflow.test.ts src/test/serviceRoleAuthorization.test.ts src/test/extensionApiContract.test.ts src/test/supabaseTokenMode.test.ts src/test/websiteEmailFunction.test.ts`; `curl -I -L --max-time 20 https://trustedbums.com`; `curl -I -L --max-time 20 https://rcdl.tplinkdns.com`; and `curl -I -L --max-time 20 http://rcdl.tplinkdns.com`.
- Results: Production dependency audit returned zero advisories; the focused security Vitest pack passed `33/33`; raw-shell `qa:env` failed until `.env.qa` was sourced; sourced QA preflight passed against `https://trustedbums.com`; exact-head hosted `QA`, deploy, `E2E Smoke`, and `Visual UI Audit` still show `success` on `7ee97c1`; live security advisors still flag leaked-password protection disabled, exposed `SECURITY DEFINER` helper executes, and mutable `search_path` on `normalize_customer_domain()`; live SQL still confirms exposed-role `EXECUTE` on `find_customer_lead_duplicate()`, `record_admin_scrum_item_audit_event()`, `set_admin_scrum_item_audit_fields()`, and `normalize_customer_domain()`; live deployed source plus current logs still show the fail-open `TB-0085` pattern on `sync-claim-decision-replies`; and the required external DNS target is still not safe for HTTPS validation from this runner.
- Internet sources reviewed: current official Supabase docs and changelog; official Clerk metadata guidance; OWASP API Security 2023 guidance for object-level authorization and resource consumption; and Cloudflare Turnstile server-side validation guidance.
- Checks that could not run and why: live Auth dashboard setting visibility, vault-secret visibility for `CLAIM_DECISION_SYNC_SECRET`, authenticated extension API smoke, and a fresh seeded live mutation or cross-company deny matrix were not available in this run.
