# Trusted Bums Project Notes

- Project files are stored on an SMB mount NAS. Install any executable files on the local system to keep the NAS mounts clean.
- If the Supabase CLI gets stuck waiting, cancel it and try the MCP service. If the MCP path is also stuck or unavailable, consider the change blocked rather than waiting indefinitely.
